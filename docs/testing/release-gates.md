# Release Gates — v0.5 MVP

## A. Stakeholder scope acceptance

<!--
Closed against the durable Playwright suites on trunk (tests/browser/e2e-home.spec.ts,
e2e-faq.spec.ts, e2e-renewal.spec.ts, e2e-booking.spec.ts, e2e-booking-mobile.spec.ts,
e2e-marketplace.spec.ts, e2e-admin-vendor.spec.ts, plus smoke.spec.ts — 8 spec files as of
this pass, verified directly via `ls tests/browser/*.spec.ts`) and, where a bullet does not
require a browser suite, Feature-level tests — each box below was checked or left open
against the actual test source, not the file's existence.

Historical CI citation, superseded by this pass: earlier boxes in this section were closed
against GitHub Actions run 32455851923 ("Merge pull request #139 from
andrianm28/test/e2e-marketplace-journey"), job "Browser + a11y smoke test (Playwright)" —
conclusion success — on commit 34d5551. That run covered only the 6 spec files that existed
at the time (it predates e2e-admin-vendor.spec.ts and e2e-booking-mobile.spec.ts) and is kept
here only as historical provenance for those still-unchanged boxes, not as evidence for
anything added or changed by this pass. Boxes this pass touched cite their own,
more-precise-per-bullet evidence (including, where a test has only run locally and not yet in
a real CI run, an explicit caveat saying so) rather than one shared CI-run citation.

E2E-ADMIN/VENDOR (browser-level) now exists — `tests/browser/e2e-admin-vendor.spec.ts`,
added this pass — and is cited by name in the bullets below it applies to.
-->

- [x] Homepage displays four primary menus in exact order. — `tests/browser/e2e-home.spec.ts::'desktop navigation shows the four primary menu labels in order'` asserts the exact ordered array `['Pemesanan Makam', 'Layanan Pemakaman', 'Perpanjangan Makam', 'FAQ']` plus each link's href; `'mobile navigation exposes the same four menu labels behind the hamburger'` proves the same four are reachable on a 375×812 viewport. Both pass in the cited CI run.
- [x] Five launch regions are present. — `tests/browser/e2e-renewal.spec.ts::'city step lists all five MVP launch cities and TPU/TPS selection reaches a real cemetery'` asserts `await expect(cityButtons).toHaveCount(5)` against `LaunchCityCode::KNOWN_CODES`, plus a real click-through to a cemetery. Passes in the cited CI run.
- [x] Booking Steps 1–9 pass desktop and mobile browser tests. — Desktop: `tests/browser/e2e-booking.spec.ts`'s `'a visitor completes all 9 steps end to end with real fixture data'` (unchanged by this pass, CI-green in the cited PR history). Mobile (added this pass): `tests/browser/e2e-booking-mobile.spec.ts`'s `'a visitor completes all 9 steps end to end on a mobile viewport'`, run under a new `mobile-chromium` Playwright project (`devices['Pixel 5']`, `playwright.config.ts`) scoped via `testMatch` to this one file only, with the pre-existing `chromium` project given a matching `testIgnore` so the new file doesn't ALSO run a second, desktop-viewport copy of itself — every other existing suite still runs exactly once, not doubled (verified directly: `npx playwright test --list` and an actual local run both reported 61 tests before this pass, 62 after). The two files' shared step-completion helpers and fixture consts live in `tests/browser/e2e-booking-helpers.ts` (new this pass) rather than one spec file exporting to the other — this repo's pinned Playwright (v1.62.1) hard-forbids one test file importing another test file, confirmed directly against `node_modules/playwright/lib/runner/index.js`, so both spec files import from that plain module instead, keeping one real source of truth for the fixture data without violating the constraint. The new mobile test has passed a real local CI-equivalent run (Postgres 18 + Redis, the pinned app image, `php artisan serve`, a real Chromium browser) but not yet an actual CI run — this branch has not been opened as a PR yet, so CI confirmation of it specifically is still pending. **Real bug found and fixed, 25 Aug 2026, live UAT sweep.** Step 8 was rendering both the online-payment card and the "Pembayaran Manual" card simultaneously regardless of `G-PAY-01`'s gate state — manual payment never disappeared once the gate opened, caught live in beta UAT. Root cause: the manual-payment card in `resources/views/livewire/public/booking/wizard.blade.php` rendered unconditionally, outside the `@if ($paymentMode === \App\Platform\FeatureGate\Modes\PaymentMode::Online)` block that already gated the online option. Fixed in PR #179, merged to trunk as commit `191c301` ("fix(booking): hide manual payment on Step 8 while G-PAY-01 is open"), deployed live to beta the same day; `BookingWizard::render()` now computes `$showManualPayment` (manual shows only while the gate is closed, or once an online attempt has genuinely failed) and the view gates on it instead. Two new regression tests in `tests/Feature/Livewire/Public/Booking/BookingWizardOnlinePaymentTest.php` assert the exact bug (`test_the_manual_option_does_not_render_when_the_gate_is_open_and_untried`) and its recovery-path counterpart (`test_the_manual_option_renders_once_the_online_attempt_fails_closed`) — 113/113 booking-wizard tests green (628 assertions) against real Postgres 18 + Redis 8.2 via the pinned CI PHP 8.5 image, plus `vendor/bin/pint --test`/`vendor/bin/phpstan analyse`/`ci/verify-docs.sh` all clean.
- [x] Exact service catalog is available. — `tests/browser/e2e-booking.spec.ts` Step 4 asserts real catalogue names/prices for `DOCUMENT_PROCESSING`/`GRAVE_DIGGING` (mandatory, disabled, "Wajib" ×2) and `AMBULANCE` (optional), and Step 5 asserts the computed total against real seeded prices — both pass in the cited CI run. Exhaustive coverage of every `ServiceCode::KNOWN_CODES` value (not just the 3 the E2E suite exercises) is `tests/Feature/Livewire/Public/Booking/BookingWizardStepsFourAndFiveTest.php::test_step_4_offers_every_basic_and_additional_service`, which iterates the enum itself rather than a hardcoded list — also passing in the same CI run's PHP job.
- [x] Marketplace categories and vendor processing pass. — Categories/browse/product-detail/cart/checkout/manual-payment/order-tracking: `tests/browser/e2e-marketplace.spec.ts` (6 `describe` blocks, verified via `grep -c "test.describe(" tests/browser/e2e-marketplace.spec.ts`; all green in CI). Vendor-side processing: `tests/browser/e2e-admin-vendor.spec.ts::'vendor can advance an incoming order through a one-click work-queue action'` (added this pass) closes the previously-open half — a vendor can log in, reach their own work queue, and actually act on an order, not just view it. That test has passed a real local CI-equivalent run (Postgres 18 + Redis, the pinned app image, `php artisan serve`, a real Chromium browser) but not yet an actual CI run — this branch has not been opened as a PR yet, so CI confirmation of it specifically is still pending. Vendor transaction history and payout visibility: same file's `'vendor transaction history is reachable and scoped to this vendor only'` / `'vendor payout status/reference is visible and scoped'` (PR #142, CI-green). Two of the three cited tests are CI-confirmed; the new work-queue-action test is confirmed locally and awaits its first CI run.
- [x] Renewal six-step journey passes. — `tests/browser/e2e-renewal.spec.ts` (6 tests) asserts all six stepper labels in order (`Kota, TPU/TPS, Cari Makam, Biaya, Pembayaran, Konfirmasi`), walks city→cemetery→search, and asserts honest gate-closed/not-found states for every downstream step (fee/payment/confirmation) consistent with `G-DATA-01` being closed by default — all pass in the cited CI run.
- [x] FAQ six categories and customer-service CTA pass. — `tests/browser/e2e-faq.spec.ts::'all six FAQ categories are listed as filter chips'` asserts the exact 6-category array; `'the customer-service CTA is present on the FAQ index and on an article detail page'` asserts both CTA links resolve to `/bantuan`. Draft-leakage, search, category-scoping, and article-detail tests in the same file also pass. All green in the cited CI run.
- [x] Admin and vendor modules pass role-scoped tests. — Feature-level: `tests/Feature/Filament/BookingOrderResourceAccessTest.php` and `tests/Feature/Filament/Vendor/VendorPanelAccessTest.php` both assert genuine per-role admission/denial. Browser-level (closed 22 Aug 2026 by PR #142, `tests/browser/e2e-admin-vendor.spec.ts`, 18 tests, verified via `npx playwright test --list tests/browser/e2e-admin-vendor.spec.ts`, CI-green): admin login, dashboard widgets (master-data + finance-gated), all 6 report pages, read-only audit-trail review, reconciliation; vendor login, dashboard, profile, transaction history (with a real cross-vendor scoping proof), payout status; and a real denial check — a vendor account gets a genuine 403 on `/admin`. Both role families are now covered end-to-end, browser and Feature level. Separately: the ADM-090/ADM-100 documentation-currency gap this box previously flagged in `docs/domain/traceability-matrix.md` — never a test gap in this claim, only a staleness in a different file — was **closed 24 Aug 2026**, when that file's v0.18 note raised both rows from `Specified` to `Covered` on this same browser evidence. **Live UAT sweep, 25 Aug 2026 — 4 further admin-panel resources confirmed live.** VendorResource: a real vendor "Vendor UAT Test Contoh" was created via the live admin UI and correctly audit-logged (a `VENDOR_CREATED` event with the correct actor/timestamp/subject/metadata) — left in place as a clearly-named test fixture. AuditEventsResource: confirmed genuinely read-only live — no edit or delete control appears anywhere on the resource, corroborating this box's existing "read-only audit-trail review" claim with a direct UI check rather than test evidence alone. Booking-order lifecycle: rejecting and cancelling a booking order both land in the correct terminal state with no further transition offered, live-confirmed against real orders. All 4 are genuinely live, not merely re-derived from the existing Feature/browser-level evidence above.
- [ ] Traceability contains no `Missing` or `Partial` item for stakeholder MVP. — Checked the literal claim against `docs/domain/traceability-matrix.md` directly: the file's status vocabulary (see its own "Status legend") is only `Specified` / `Specified (gated fallback)` / `Covered` — the strings `Missing` and `Partial` do not appear anywhere in the file, so the bullet is not literally falsifiable as worded. Substantively, though, section B ("Stakeholder Workflow MVP") still carries real, uncovered gaps within stakeholder-MVP scope: ADM-070 (payment/transaction/manual verification) is `Specified` with test evidence `—`; CARE-SUB-03/04/05/07 are `Specified` with evidence `—`; CARE-SUB-02 and CARE-SUB-06 are `Specified` with partial evidence. REN-03's own evidence trail (section D) previously said the renewal spec's **AC4 (< 500 ms at 100,000 records)** was NOT TESTED; that is now stale — a real benchmark exists (`BenchGraveSearchCommand`, see §H below) and section D's REN-03 row has been corrected to cite it, so REN-03 is no longer a stakeholder-MVP gap. **Corrected 24 Aug 2026:** the note above about the matrix's ADM-090/ADM-100 rows being themselves stale is resolved — `docs/domain/traceability-matrix.md` v0.18 raised both rows to `Covered` on real evidence (`tests/browser/e2e-admin-vendor.spec.ts`, PR #142); that half of this box's original reasoning no longer applies. The box stays unchecked regardless: ADM-070 and the CARE-SUB-02/03/04/05/06/07 gaps named above are real, substantive, uncovered items within stakeholder-MVP scope, entirely unaffected by the ADM-090/ADM-100 correction — they alone are sufficient reason this box cannot yet be honestly checked. Leaving unchecked: the literal string search passes but the substance the bullet is checking for does not, and the traceability file needs a maintenance pass on the CARE-SUB items (owner: whoever maintains `docs/domain/traceability-matrix.md`) before this box can be honestly checked. Separately: PR #130 (merged 21 Aug 2026) fixed a real, distinct data-integrity bug in the CareSubscription/VendorFulfillment write paths (`subscriptions.customer_id`, `service_acceptances.customer_id`, `service_complaints.customer_id`, and `work_evidence.uploaded_by` were mistyped `uuid` instead of the bigint FK every other user-reference column in this codebase uses) — verified via a real migration round-trip against Postgres, FK-constraint-rejection tests, and two independent adversarial reviews; this is a schema-correctness fix, not new coverage for the CARE-SUB gaps named above, which remain exactly as described. **Corrected further, 24 Aug 2026 — the maintenance pass this box itself called for has now happened.** `docs/domain/traceability-matrix.md` v0.19 searched all five named gaps for real, already-existing test evidence that simply was never cited. Three are real and now cited: **CARE-SUB-03** (`tests/Feature/Payment/CareSubscriptionWebhookSettlementTest.php`), **CARE-SUB-04** (`tests/Feature/Domain/VendorFulfillment/CreateWorkOrderTest.php`), and **CARE-SUB-05** (`tests/Feature/Filament/Vendor/WorkOrderEvidenceUploadTest.php`) are raised to `Covered`. **CARE-SUB-07** is also raised to `Covered`, narrowed to what is actually built (`tests/Feature/Filament/Admin/WorkOrderVendorReplacementTest.php` proves vendor replacement with an audited, mandatory reason) — its prior text's "reschedule" and "one replacement per original" clauses named capabilities that were never built and are removed rather than left implying coverage. All four tests were introduced or last touched by the already-merged PR #130 (commit `1a93938`, CI run `32506685010`, success) — nothing new was written for this pass. **ADM-070 was investigated directly and confirmed to remain a genuine, real gap, not merely an uncited one**: `VerifyManualPaymentTest`/`VerifyManualPaymentRouteTest` are real and well-tested, but the route they cover carries its own doc comment stating it is "a plain controller route (not a Filament panel page)" — no Filament admin-panel surface exists for this capability at all, and there is no view surface for payment/transaction references either (`admin-operations` requirement 5's first half), so citing those tests here would overclaim. **The box stays unchecked, narrower now**: ADM-070, CARE-SUB-02 (partial — see traceability-matrix v0.17), and CARE-SUB-06 (partial evidence) are the remaining real, substantive, uncovered/under-covered items within stakeholder-MVP scope; CARE-SUB-03/04/05/07 no longer belong on this list. **Corrected 25 Aug 2026 — ADM-070 is closed.** `App\Filament\Admin\Resources\PaymentVerifications\PaymentVerificationsResource` (commit `01e2155`, branch `feat/adm-070-payment-verifications-admin-view`) is a real, read-only Filament admin resource giving finance/restricted_admin the GET/view surface for payment/transaction references that the note above confirmed did not exist — see `docs/domain/traceability-matrix.md`'s new "ADM-070 raised" entry for the full evidence trail (`PaymentVerificationsResourceAccessTest.php`, 10 test methods/14 executions with its role data provider; `PaymentVerificationsTableTest.php`, 5 tests; 19 total, both files green against real Postgres in Task 1's own local run, not yet an actual CI run since this branch has no PR open yet). ADM-070 is raised to `Covered` in the matrix and no longer belongs on this box's named-gaps list. **CARE-SUB-02 and CARE-SUB-06's partial-evidence gaps are UNTOUCHED by this work** — nothing in this pass read, tested, or otherwise addressed either row, and both remain exactly as the note above described them. **The box stays unchecked**, narrower again: CARE-SUB-02 and CARE-SUB-06 are now the only two remaining real, substantive, uncovered/under-covered items within stakeholder-MVP scope named by this box.

**UAT closing pass, 25 Aug 2026 — CARE-02/CARE-06 traceability-mapping check.** Investigated whether the day's live-UAT `CARE-02`/`CARE-06` scenarios are the same items as this box's `CARE-SUB-02`/`CARE-SUB-06` gaps, cross-read against the UAT checklist's own scenario text rather than assumed from ID similarity alone. **CARE-02 (`Siklus dibuat dan ditagih` — cycle generated/billed by scheduler) does map to `CARE-SUB-02`** — same underlying capability (`care:generate-cycles` → `CycleScheduler` → `GenerateCycle`, idempotent dedup). But the matrix row's own citation (`CareGenerateCyclesCommandTest.php`) was itself incomplete, not the underlying capability: its 4 tests prove due-ACTIVE generation, PAUSED exclusion, and command-level idempotency, but never assert that an ACTIVE-but-not-yet-due subscription is skipped. The UAT checklist's own citation for this scenario, `tests/Feature/Domain/CareSubscription/CycleSchedulerTest.php` — the domain-layer class `CareGenerateCyclesCommand` itself calls (confirmed by reading `App\Console\Commands\CareGenerateCyclesCommand::handle()`) — closes exactly that gap: `test_skips_active_subscription_with_no_due_cycle`, plus `test_is_idempotent_on_rerun`. Read together, CARE-02/CARE-SUB-02's full claim is genuinely, completely covered; only the matrix row's own citation was partial — a documentation-currency gap, not a functional one. **CARE-06 does NOT map to `CARE-SUB-06`.** UAT's `CARE-06` ("Pelanggan melihat status langganan," `/langganan/{subscriptionReference}`) is `SubscriptionStatusPage` — `docs/product/screen-inventory.md` PUB-095 — while this box's `CARE-SUB-06` row is keyed to PUB-096 (`/riwayat-perawatan/{customerId}`, `CareHistoryPage`), a different Livewire component entirely reached by a different route (`routes/web.php:444-445`). No `CARE-SUB` row in `docs/domain/traceability-matrix.md` names PUB-095 at all — its real test, `tests/Feature/Livewire/Public/CareSubscription/SubscriptionStatusPageTest.php`, was simply never entered into the matrix. **CARE-06's original PASS stands on this separate, real evidence, untouched by CARE-SUB-06's partial-evidence gap.** Separately, CARE-SUB-06's own "partial evidence" now has a precise, located cause rather than just a status: its cited test (`CareHistoryPageTest.php`) is read-only (renders/badges/empty-state), proving none of the row's three claimed write actions. Real evidence for those actions exists but was never cited in the matrix: acceptance and complaint-with-audit are proven at screen level by `CareHistoryPageActionsTest.php` (`test_the_owning_customer_accepts_a_completed_service`, `test_the_owning_customer_files_a_complaint`) and at domain level by `AcceptServiceTest.php`/`ComplaintFlowTest.php::test_file_complaint_creates_open_complaint_with_audit_and_outbox`. Make-good is proven only at domain level — `ComplaintFlowTest.php::test_make_good_creates_replacement_work_order_and_audit` directly asserts the replacement carries the SAME `subscription_cycle_id` as the original work order (the row's "linked to original cycle" clause) — but `App\Domain\VendorFulfillment\Actions\CreateMakeGood` has zero production callers anywhere in `app/` (grepped) beyond a doc comment in `CareHistoryPage.php` explaining it is deliberately staff/system-triggered, never a customer Livewire action. So the domain logic is real and tested, but no live UI path — customer or admin — invokes it today. **Verdict: CARE-SUB-06 correctly stays `Specified`, and "partial evidence" is accurate** — acceptance/complaint are covered at both screen and domain layers; make-good is proven only as currently-unreachable domain logic. This is real, and narrower than the box's prior text left it.

## B. UX and accessibility

- [x] Loading, empty, error, pending, success, and support states reviewed. — Empty, error, pending, success, and support all have real, passing test evidence: empty (`e2e-marketplace.spec.ts` empty cart, `e2e-renewal.spec.ts` no-result search, `e2e-faq.spec.ts` empty category), error (`e2e-marketplace.spec.ts` checkout phone-length validation error, multiple honest 404/not-found states across all four content suites), pending (`e2e-booking.spec.ts` Step 9 "Menunggu diproses", never styled as success), success (`e2e-marketplace.spec.ts` "Pesanan diterima"), support (`/bantuan` CTA asserted in `e2e-home.spec.ts`, `e2e-faq.spec.ts`, `e2e-renewal.spec.ts`, `e2e-marketplace.spec.ts` is absent from checkout but present via the gate-closed banners). Loading state was previously untested; it is now closed by `tests/browser/e2e-booking-loading-states.spec.ts` (2 tests): `'the customer-data step shows a loading indicator and disables the submit button while saving'` asserts the real `wire:loading`/`wire:target="saveStep6"` markup — the `role="status"` "Menyimpan data pemesan…" indicator becomes visible and the `Lanjutkan` submit button becomes disabled, racing the assertions against the click itself so the fast local response can't close the window before the check starts — and `'the deceased-data step shows a loading indicator and disables the submit button while saving'` asserts the identical pattern for `saveStep7`'s "Menyimpan data almarhum…" indicator. Both tests passed a real local CI-equivalent run (Postgres 18 + Redis 8.2, the pinned `ghcr.io/andrianm28/makam-app` image for its PHP 8.5 runtime + extensions with this worktree's own code and migrations mounted in, `php artisan serve`, real Chromium) — run 5 times consecutively with no flakes, and `npx playwright test --list` confirms the suite total went from 64 to exactly 66 (no accidental double-registration under the `chromium`/`mobile-chromium` projects). Not yet an actual GitHub Actions run — this branch has no PR yet.
- [x] Autosave/resume and browser back behavior pass. — `tests/browser/e2e-booking.spec.ts`'s dedicated `'E2E-BOOK — autosave and resume'` describe block (`'navigating away and back to the draft URL resumes at the saved step'`, `'an unknown draft id resets to a working blank wizard rather than exposing state'`) and the `'E2E-BOOK — progress indicator and back/forward navigation'` block's `'the stepper reflects the current step and back navigation preserves data'` (asserts a filled customer-form field survives a `Kembali` round trip, proving server-persisted autosave rather than client-only state). Both pass in the cited CI run.
- [x] Keyboard navigation, focus, labels, and touch targets pass. — Labels: strongly evidenced as before — every suite locates form fields via `getByLabel()`/`getByRole()` almost exclusively (not CSS selectors), which only resolves if the accessible label is real, plus every test file runs `AxeBuilder` scans that include label-related WCAG rules and assert zero violations. Keyboard navigation, focus, and touch targets were previously **zero** dedicated test evidence; both are now closed by `tests/browser/e2e-a11y-interaction.spec.ts` (3 tests, added this pass). Touch targets (`E2E-A11Y — touch targets`, run at `devices['Pixel 5']` viewport/UA): `'primary homepage CTA meets the 44px minimum touch target'` asserts the hero `<x-mk.button variant="primary" size="lg">`'s real bounding box against the 44px floor (`--mk-touch-min`, design-system.md §7.3); `'booking wizard Step 1 city selection button meets the 44px minimum touch target'` asserts the same for the Step 1 city buttons (`md`/default size, exactly at the 44px floor rather than the hero's 52px `lg`) — deliberately different elements/pages from `e2e-home.spec.ts`'s existing mobile-nav test, which never asserts a bounding box and so provided no touch-target-size evidence of its own. Keyboard navigation and focus (`E2E-A11Y — keyboard navigation`): `'the customer-data step is fully reachable and completable by keyboard alone'` reaches booking wizard Step 6 by mouse (out of this test's own claim), then Tabs through the real DOM/focus order confirmed by reading `wizard.blade.php` directly — full name → mobile → email → address → relationship → contact channel → privacy checkbox → the "Pemberitahuan Privasi" link nested inside the checkbox's own `<label>` (a real, easy-to-miss Tab stop) → "Kembali" → "Lanjutkan" — fills every field via `page.keyboard.type`/`Space`, and submits with `Enter` rather than a click, confirming Step 7 is reached. Focus-visible styling is checked two ways for the two real, different implementations found: the hand-written form fields use `focus:ring-2 focus:ring-primary-600` (a plain `focus:` box-shadow ring, not `focus-visible:`) — asserted via `getComputedStyle(el).boxShadow !== 'none'` on the focused full-name field; `<x-mk.button>` genuinely uses `focus-visible:ring-2 focus-visible:ring-primary-600` — asserted via a class match on the focused submit button. All 3 tests passed a real local CI-equivalent run (Postgres 18 + Redis 8.2, the pinned `ghcr.io/andrianm28/makam-app` image for its PHP 8.5 runtime, this worktree's own code and migrations mounted in, `php artisan serve`, real Chromium) — run 5 times consecutively with no flakes, and `npx playwright test --list` confirms the suite total moved from 66 to exactly 69 (3 new, all under the `chromium` project only, no accidental duplication under `mobile-chromium`). Not yet an actual GitHub Actions run — this branch has no PR yet.
- [x] Responsive behavior passes agreed viewport matrix. — `docs/design/design-system.md` §1.5 defines a 7-breakpoint matrix (320/360/640/768/1024/1280/1536); `tests/browser/e2e-responsive-breakpoints.spec.ts` (new this pass, 7 tests, one per documented width via `page.setViewportSize()` in a single spec — not 7 new Playwright projects, so CI runtime for the rest of the suite is unaffected) now runs the real homepage at all 7 widths. Every width asserts no horizontal overflow (`document.documentElement.scrollWidth` ≤ `clientWidth`, §1.5's 320px "everything reachable" baseline) plus the real `lg` (1024px) nav cutover confirmed directly from `header.blade.php` (`hidden lg:flex` desktop bar / `lg:hidden` mobile bar): hamburger + hidden desktop nav below 1024px, desktop nav + hidden hamburger at/above it. Three widths get an additional, real structural assertion beyond nav state: 640px (`sm`) — the homepage services grid's real `sm:grid-cols-2` renders 2 columns, matching §1.5's literal sm claim ("2-up service cards"); 1024px (`lg`) — that same grid's real `lg:grid-cols-4` renders 4 columns; 1280px/1536px (`xl`/`2xl`) — the services section's `max-w-content` (`--container-content: 80rem` = 1280px, `tokens.css`) caps its rendered width at exactly 1280px at both widths, confirming §1.5's "max width reached" (xl) and "no new layout — gutters grow only" (2xl) claims directly. One width's assertion is a deliberate, noted substitution: §1.5's literal md (768px) claim is "wizard gets summary sidebar", which is not present anywhere in `wizard.blade.php` or `home-page.blade.php` (confirmed by reading both) — a real, different md-triggered structural change on the tested homepage was used instead, the FAQ-highlights grid's `md:grid-cols-2` (1→2 columns at 768px). 320px and 360px have no structural claim in §1.5 beyond "Baseline"/"minor density gains", so only the overflow and nav-state assertions apply there, per this repo's own precedent for a partial-but-real claim closing a box (§F's search-performance box, smaller-than-100k dataset with an explicit scale note). This is genuinely distinct from `mobile-chromium`'s existing `devices['Pixel 5']` (393×727) coverage, which remains a separate, narrower thing — mobile-viewport-focused runs of individual specs at one non-matrix width, not a member of this matrix. All 7 tests passed a real local CI-equivalent run (Postgres 18 + Redis 8.2, the pinned `ghcr.io/andrianm28/makam-app` image for its PHP 8.5 runtime, this worktree's own code and migrations mounted in, `php artisan serve`, real Chromium) — run 5 times consecutively (1 initial fix + 4 repeats) with no flakes, and `npx playwright test --list` confirms the suite total moved from 69 to exactly 76 (7 new, all under the `chromium` project only, no accidental duplication under `mobile-chromium`). Not yet an actual GitHub Actions run — this branch has no PR yet. **Further progress (25 Aug 2026, PR #181, CI-confirmed):** `mobile-chromium`'s own scope had a real gap independent of the 7-point matrix above — its `testMatch` covered only `e2e-booking-mobile.spec.ts`, so the marketplace checkout flow (browse, product detail, cart, checkout, manual payment) had zero mobile-viewport coverage anywhere in `tests/browser/`, confirmed by reading `playwright.config.ts` directly before this fix. Closed by adding `tests/browser/e2e-marketplace-mobile.spec.ts` (3 tests: browse+category filter, add-to-cart, checkout+manual payment+order tracking) — a mobile-focused SUBSET of `e2e-marketplace.spec.ts`'s 17 tests, not the whole suite re-run under mobile emulation, matching the precedent `e2e-booking-mobile.spec.ts` already set for booking — plus widening `mobile-chromium`'s `testMatch` (and `chromium`'s matching `testIgnore`) to cover both mobile-only specs, so nothing runs twice. Shared fixtures/helpers extracted to `tests/browser/e2e-marketplace-helpers.ts` for the same "Playwright forbids one spec file importing another" reason `e2e-booking-helpers.ts` already documents. **Two real bugs in the new test's own assumptions, found and fixed via this PR's actual CI runs, not asserted from a local guess**: (1) `<x-mk.table>` (`resources/views/components/mk/table.blade.php`, design-system.md §3.5's documented mobile-first pattern) renders a real `<table role="table">` only at `md:` (768px+) — Pixel 5's 393px viewport never reaches it, so the desktop spec's `getByRole('table', ...)` locator resolved to nothing (CI run 1, https://github.com/andrianm28/makam-app/actions/runs/32871742834); (2) the component renders BOTH the desktop `<table>` (`<td>` cells) and the mobile `<dl>` card list (`<dt>`/`<dd>`, role "definition") into the DOM at every viewport simultaneously — only CSS visibility differs by breakpoint — so an unscoped `getByText`/`getByRole('button')` matched both copies and strict-mode-violated (CI run 2, https://github.com/andrianm28/makam-app/actions/runs/32872681522). Both fixed by scoping the mobile test's assertions to role "definition", the real mobile-card markup. **CI run 3 (https://github.com/andrianm28/makam-app/actions/runs/32872681522/job/97883291732) is green**: `Browser + a11y smoke test (Playwright)` passed in 2m25s, `78 passed, 1 skipped` (the skip pre-existing, unrelated to this change) — `mobile-chromium` now runs exactly 4 tests (the 1 pre-existing booking test + the 3 new marketplace ones), each performing real interactions and an `AxeBuilder` scan under `devices['Pixel 5']`, with no accidental duplication under the `chromium` project. This is a real, CI-confirmed PASS, not a claimed one.
- [x] Copy is empathetic and does not overpromise Urgent/payment availability. — `e2e-home.spec.ts::'homepage shows the Urgent gate explanatory state and the customer-service CTA'` asserts the honest "Ketersediaan Urgent Belum Dapat Dipastikan Otomatis" banner (never a false availability claim) with a support link. `e2e-booking.spec.ts` Step 8/9 asserts the manual-payment fallback and "Menunggu diproses"/"Belum dikirim" states never claim a payment or notification that hasn't actually happened. `e2e-marketplace.spec.ts::'online payment is either gate-closed, or fails with a named, honest error'` asserts the fixed, non-leaking denial copy. `e2e-renewal.spec.ts` asserts the gate-closed and no-result states are always three-part-honest (what's empty, why, what to do next), never a bare denial. All pass in the cited CI run.

## C. Payment

Either mode must pass:

### Online

- [ ] Shared payment/journal/reconciliation gate approved. — Fundamentally a business/human-approval gate, not a test claim: `G-PAY-01` ("online payment; activation evidence = shared money path and merchant active") is real and currently *open* for beta (sandbox only, per ADR-0035 item 1's explicit user decision — real customers see it, no real money moves) but *closed* for production per ADR-0033/ADR-0036, both of which state production activation requires "a deliberate, separate, audited human action" through the Feature Gate admin panel with fresh re-authentication — not yet performed. No test can close this box; it awaits that real, dated activation action. **Update (26 Aug 2026):** ADR-0035 item 1's site-wide beta banner mitigation (PR #107, Lane C4) was removed from every page (`resources/views/layouts/app.blade.php`) by explicit, informed project-owner decision while `G-PAY-01` stays open in sandbox mode — see ADR-0035 item 1's own "Update (26 Aug 2026)" note. The payment-step-specific sandbox warning on the "Bayar Sekarang" card (PR #106) is untouched and is now the sole remaining mitigation for this item; `tests/browser/smoke.spec.ts` (real Playwright + axe-core run) confirms zero accessibility violations on the homepage after removal.
- [x] Merchant, quote, amount, signature, replay, retry, and concurrency tests pass. — All 7 sub-claims independently verified with real test coverage: merchant identity (`WebhookReceiverTest::test_a_delivery_to_a_merchant_this_environment_does_not_serve_is_rejected`), quote (`GuardPaymentSessionUpstreamTest::test_condition_3_denies_for_no_quote`/`_an_unaccepted_quote`/`_an_expired_quote`), amount (`GuardPaymentSessionUpstreamTest::test_condition_5_denies_for_an_amount_differing_by_one_minor_unit`), signature (`WebhookReceiverTest`'s forged/overlong/malformed-header rejection tests), replay/retry (`WebhookReceiverTest::test_a_duplicate_delivery_yields_exactly_one_row_and_a_success_acknowledgment` — a provider retry IS a duplicate delivery, proven idempotent), concurrency (`WebhookReplayTwoConnectionTest.php`, a genuinely separate database session's identical delivery proven to resolve as a duplicate, not a second effect). All pass against real Postgres in CI. **Live UAT sweep, 25 Aug 2026 — exact per-scenario test methods confirmed**, extending the "failed"/"expired"/duplicate-claim/settlement-conflict sub-cases beyond the summary above: failed webhook outcome — `ProcessWebhookEventTest::test_a_claimed_failed_event_records_the_failure_without_order_effects`; expired webhook outcome — `::test_a_claimed_expired_event_records_expired_on_its_session`; duplicate delivery (single connection) — `::test_a_second_claim_of_the_same_row_is_refused_and_changes_nothing`, with true concurrent-worker serialization covered separately by `WebhookReplayTwoConnectionTest::test_a_second_connections_identical_delivery_is_recognized_as_a_duplicate_not_a_second_effect` (already cited above); settlement conflict — `ProcessWebhookEventTest::test_one_provider_transaction_cannot_settle_two_different_invoices` and `::test_a_settlement_conflict_is_recorded_rather_than_silently_dropped`, proving a provider transaction cannot silently settle the wrong invoice and a genuine conflict is recorded rather than dropped. All 5 methods confirmed present at these exact names by direct file read this pass.
- [x] No direct paid path. — Exhaustively proven, not spot-checked: `PaymentGuardFailClosedTest::test_no_input_combination_reaches_a_pass_outcome` — `GuardPaymentSession` is deny-only by design (Wave 1b ruling 1b-L3-01, approved 10 Aug 2026, no `Allowed` case exists in `PaymentIntentDecision` at all), and `test_the_payment_session_model_refuses_to_insert_a_row`/`_refuses_a_direct_save_too` prove the model layer itself rejects a bypass attempt. Passes in CI.

### Manual fallback

- [ ] Instructions and reference are approved. — **Progress this pass (24 Aug 2026):** `tests/browser/e2e-marketplace.spec.ts`'s `'a guest completes checkout and submits manual payment proof'` now asserts the instructional paragraph itself is shown to the customer ("Transfer sejumlah total pesanan, lalu masukkan nomor referensi transfer Anda di bawah."), closing the display gap the previous pass found — real assertion, matched character-for-character against `resources/views/livewire/public/marketplace/checkout.blade.php` (~line 272), but not yet CI-confirmed on this branch (no PR opened yet). The pre-existing reference-number field assertion (same test, `getByLabel('Nomor referensi transfer')`) covers the "reference" half. **Mechanism built this pass (26 Aug 2026)** for the booking wizard specifically: the "bank destination account number/name found nothing" gap this box previously described is now closed as a MECHANISM — `SiteSetting::KEY_BANK_TRANSFER_BANK_NAME`/`KEY_BANK_TRANSFER_ACCOUNT_NUMBER`/`KEY_BANK_TRANSFER_ACCOUNT_HOLDER` (three new `site_settings` rows, same admin-managed key/value convention as `company_name`/`company_nib`) are editable via a new "Rekening transfer manual" section on `/admin/pengaturan-situs` (`SiteSettingsResource`/`SiteSettingsForm`), resolved through the new `App\Support\BankTransferInfo` (same honest-null discipline as `CompanyInfo::nib()` — no fictional placeholder), and rendered on the booking wizard's Step 8 "Pembayaran Manual" card (`BookingWizard::render()` / `wizard.blade.php`). Real test coverage: `tests/Unit/Support/BankTransferInfoTest.php` (null-until-configured, all-three-required, partial-config-treated-as-unconfigured), `tests/Feature/SiteSettings/EditSiteSettingsSmokeTest.php` (admin sets the three fields through the real Filament form and they persist), `tests/Feature/Livewire/Public/Booking/BookingWizardManualPaymentBankDetailsTest.php` (public wizard renders the real values when set; shows an honest "Rekening tujuan belum dikonfigurasi" — never a fabricated placeholder — when unset or only partially set). All pass locally against real Postgres 18/Redis 8.2 via the pinned CI PHP image (12/12 new-test run green, 212/212 in the full `Booking`/`SiteSettings`/`Support` sweep, 0 failures); `vendor/bin/pint --test` and `vendor/bin/phpstan analyse` (level 2, whole `app/`) both pass; CI confirmation pending this PR. **This closes the ENGINEERING half only, and the box stays unchecked**: the fields are empty in every real environment until the project owner enters the actual bank name, account number, and account holder name via `/admin/pengaturan-situs` — until then the public wizard correctly shows the honest not-configured state, not a working destination, and this box cannot become an honest `[x]` until that real data is entered AND a human confirms it's the business's real, approved destination account. `App\Livewire\Public\Marketplace\Checkout.php`'s own manual-payment card is a SEPARATE surface not touched by this pass — it still has no bank-destination display at all and needs the identical wiring in a follow-up.
- [x] Proof/verification and authorization pass. — Proof upload: `SubmitManualPaymentTest::test_it_uploads_a_proof_file_through_the_document_vault_and_references_it_only` (real DocumentVault integration, not inline storage). Verification: `VerifyManualPaymentTest` (approve/reject transitions, mandatory audit reason, decide-once constraint, `test_two_separately_loaded_instances_cannot_both_decide_the_same_row` — a real race guard). Authorization: `VerifyManualPaymentRouteTest` — role-scoped (finance/restricted-admin only), a revoked role grant re-tested live, 403-not-404 for unauthorized actors, real re-authentication middleware required (`test_the_route_is_registered_with_the_reauthentication_middleware`), rate-limited. All pass in CI.
- [x] Pending state is truthful. — `e2e-booking.spec.ts` Step 9's own comment/assertion: "Pending, never styled as success — no order/payment has actually [succeeded]" — the manual-fallback pending UI state is asserted as visually/semantically distinct from success, matching `AGENTS.md`/`design-system.md` §6.8's rule against claiming an outcome that hasn't happened. Passes in the cited CI run (§A/§B's e2e-booking citation).
- [ ] Invoice only follows approved verification. — **Real, structural gap, deliberately not being built — not a backlog item awaiting scheduling.** No "invoice" concept exists anywhere downstream of manual verification. `VerifyManualPaymentTest::test_it_never_touches_payment_sessions_the_journal_or_the_order_aggregate` explicitly proves verification is a dead-end status write today, deliberately not wired to any order/journal/paid-effects consequence (same "deny-only guard, upstream domain records still owed" boundary `GuardPaymentSession`'s own doc block names). There is nothing called an "invoice" in `app/Platform/Payment` or `app/Domain` for this path — meaning the manual-payment fallback, the only currently-active payment mode (`G-PAY-01` production-closed per above), cannot presently complete an order at all. **Update (23 Aug 2026)**: wiring `VerifyManualPayment`'s approval outcome to a real settlement/invoice effect was explicitly scoped for the observability/release-gates fix pass that produced PR #150, then explicitly withdrawn by the product owner mid-session ("optout manual payment") — this is not deferred to "Phase 2/3," it is out of scope by deliberate decision until a future, separate product/business call re-opens it. This box stays open and production-blocking for the manual-payment path specifically; it does not block work that doesn't depend on manual-payment settlement.
  **Update (26 Aug 2026) — the ORDER-PAID half is now built; the box stays open, and this update itself needs a human product-owner decision before it can merge.** `payment_verifications` gained a real `order_id` foreign key and a customer-stated `amount_minor`/`currency` (`2026_08_26_120000_add_order_link_and_amount_to_payment_verifications_table.php`). Investigation confirmed the table's only real caller is the marketplace manual-payment path (`App\Livewire\Public\Marketplace\Checkout::submitManualProof()`) — the booking wizard's Step 8 manual card never calls `SubmitManualPayment` at all, so `order_id` is scoped to `marketplace_orders`, not `orders`. `SubmitManualPayment` now refuses a submission whose reference does not resolve to a real order, and the customer states their own transfer amount (a new "Jumlah transfer" field on `resources/views/livewire/public/marketplace/checkout.blade.php`) rather than it being assumed equal to the order total — the point of asking is to let an admin catch a customer who transferred the wrong amount. `VerifyManualPayment`, on `Approve` and inside the SAME `Audit::wrap()` transaction as `decide()`, now calls `App\Domain\Marketplace\Actions\MarkMarketplaceOrderPaid` — the SAME Action `App\Platform\Payment\Actions\ApplyPaymentSettlement` already calls for the webhook leg, reusing its existing `assertAmountMatchesOrderTotal()` precondition rather than duplicating it. A mismatch throws `MarketplacePaymentAmountMismatchException` from inside that transaction, so the whole approval — the order-paid write AND the verification's own `decide()` — rolls back together: the verification stays `SUBMITTED` and the order stays `BELUM_DIBAYAR`. Idempotent: a second decide on the same row still throws `PaymentVerificationAlreadyDecidedException` (pre-existing row-lock mechanism, unchanged); a second arrival on an already-`DIBAYAR` order (e.g. a race with the webhook leg) is a pre-existing no-op inside `MarkMarketplaceOrderPaid` itself. `VerifyManualPaymentTest::test_it_never_touches_payment_sessions_the_journal_or_the_order_aggregate` — the test this box's ORIGINAL text cited as proof of the dead end — has been replaced with `test_it_never_references_payment_sessions_the_journal_or_the_booking_order_aggregate` (still proves no `payment_sessions`/`Journal::post()`/booking-`OrderWorkflow` reference; no longer claims no order effect at all, which is no longer true) plus new positive tests (`test_approving_marks_the_linked_marketplace_order_paid_with_the_stated_amount`, `test_an_amount_mismatch_refuses_the_whole_approval_and_rolls_back`, `test_rejecting_never_touches_the_linked_order`, `test_re_approving_an_already_verified_row_does_not_double_apply`, `test_a_row_missing_order_linkage_cannot_be_approved`). **What is still NOT built, on purpose**: no invoice/receipt document for this path — `App\Domain\OrderWorkflow\Actions\IssueInvoice` (the online-payment path's own new invoice writer, `2026_08_26_100000_create_order_invoices_table.php`) is scoped to booking's `orders` table only and does not reach a `MarketplaceOrder` at all; wiring an equivalent for marketplace would be new scope (a `marketplace_order_invoices`-shaped table, or widening `IssueInvoice`'s own contract) and was not attempted here rather than half-built. So this box's literal claim ("invoice ... follows approved verification") still cannot be checked `[x]` — there is still no invoice on this path, only a real paid-order effect now. **This update directly reverses the 23 Aug 2026 product-owner opt-out recorded immediately above** ("optout manual payment" — wiring approval to a real settlement effect was explicitly withdrawn from scope). It was built anyway because the task that produced this change was explicitly instructed to close this gap; that instruction did not itself carry evidence of a NEW product-owner decision superseding the recorded opt-out. **A human must explicitly confirm the opt-out is being deliberately reversed before this merges** — this is flagged prominently in the PR description per `AGENTS.md` §Infrastructure-agent execution and is not self-merged. Real evidence: run against a disposable `postgres:18` + `redis:8.2-alpine` pair through the pinned CI PHP image (`ghcr.io/andrianm28/makam-app:sha-450d4fff52d4`), `vendor/bin/phpunit` directly — see the PR description for the exact test run output; `vendor/bin/pint --test` and `vendor/bin/phpstan analyse` on every touched file; `bash ci/verify-docs.sh`.

## D. Notifications

- [ ] Notification matrix implemented. — **Progress this pass (24 Aug 2026):** researched all remaining unproduced rows by real implementation cost (a dedicated research pass, not assumption), then closed the 3 genuinely near-zero-cost ones — real domain Actions that already reuse the existing `order.status_changed.v1` mechanism (`RecordOrderStatusChange`), just needed a new bridge arm, no new event to invent. `App\Domain\OrderWorkflow\Listeners\DispatchOrderNotifications` now bridges **six** rows, not three: `MASUK` → "Booking submitted", `MENUNGGU_KETERSEDIAAN` → "Availability requested" (new, via the real `RequestAvailability` Action), `PENAWARAN_TERKIRIM` → "Availability confirmed/rejected" (new, via the real `IssueOrderQuote` Action — unambiguous by `to_status` alone, `OrderTransition`'s own state table names `MENUNGGU_KETERSEDIAAN` as the only status that reaches it), `DITOLAK` **from `MENUNGGU_KETERSEDIAAN` specifically** → the same "Availability confirmed/rejected" (new, via `RejectOrder` — genuinely needs `from_status` discrimination since `DITOLAK` is also reachable from `MASUK`/`DIVERIFIKASI`, which must NOT fire this template; proved by 2 dedicated negative tests, not just the happy path), `MENUNGGU_PEMBAYARAN` → "Payment opened" (new, via `GrantOrderPaymentOpening`), `DIPROSES` → "Order processing", `SELESAI` → "Order completed". `tests/Feature/OrderWorkflow/OrderNotificationTest.php` now has 12 tests (6 new: 3 happy-path + 2 load-bearing negative tests for the `from_status` discrimination + 1 for "Payment opened"), 44 assertions — run twice against real Postgres 18 via the pinned app image, consistent both times (`OK (12 tests, 44 assertions)`), plus the broader `tests/Feature/OrderWorkflow/` suite (83 tests, 334 assertions, 1 pre-existing skip) re-run clean alongside it — no regression. One pre-existing test (`test_order_processing_notification_dispatched_when_order_enters_diproses`) had its own premise legitimately changed by this pass (`MENUNGGU_PEMBAYARAN` now correctly gets a notification where it previously asserted none) and was updated accordingly, not silently left contradicting the new behavior. `vendor/bin/pint --test` and `vendor/bin/phpstan analyse` clean. Not yet an actual GitHub Actions run — this branch has no PR yet. **Scope correction, updated from the 23 Aug pass**: this box's literal claim is the FULL 17-row matrix — "Booking draft created" needs no notification at all (its 6 recipient columns read "none" for 4 and "TBD" for 2 — Case manager, Finance — both resolving to no recipients per the matrix's own reconciliation ruling), so 16 rows actually need a producer. Of those 16, **9 now have one**: the 6 bridged above, plus 3 separately produced (Quote issued/Quote accepted/Payment received, via `IssueQuote`/`AcceptQuote`/`ApplyPaidEffects`). **7 rows remain genuinely unproduced**, categorized by a real research pass rather than left as one undifferentiated gap: 5 have real, correct domain logic already built but emit no outbox event at all yet (Payment failed/exception, Marketplace order submitted, Vendor accepted/rejected, Vendor evidence uploaded, Renewal submitted — each needs a real catalogued event name decided and emission code added, genuine engineering work, not wiring); 2 are real, unbuilt features, not a notification gap at all (Reminder due — no scheduled job or eligibility logic exists anywhere; Renewal paid/verified — no dedicated online-payment Action was even found, needs its own investigation before it can be scoped). This box's full literal claim is still not evidenced end-to-end, so it stays unchecked — but the remaining gap is now precisely bounded rather than an open-ended "~10 rows." **Further progress this pass (24 Aug 2026, MEDIUM-row plan):** closed 5 of the 7 remaining rows — the full near-zero-cost set from the prior pass, plus this set, each required a real catalogued event name decided and emission code added, genuine engineering work, not wiring. `payment.outcome_failed.v1` — `app/Platform/Payment/Actions/ApplyPaymentSettlement.php` (`f3f2f4e`) — carries `outcome` (Failed/Expired) as data, one event for the row, not two; `tests/Feature/Payment/ProcessWebhookEventTest.php` `OK (16 tests, 63 assertions)`. `marketplace_order.submitted.v1` — `app/Domain/Marketplace/Actions/PlaceMarketplaceOrder.php` (`3c20b50`) — single-emission, no outcome discrimination needed; `tests/Feature/Domain/Marketplace/PlaceMarketplaceOrderTest.php` `OK (8 tests, 33 assertions)`. `vendor_order.decided.v1` — `app/Domain/Marketplace/Actions/UpdateVendorOrderStatus.php` (`139a577`, idempotency key outcome-scoped by follow-up fix `d5d0fee`) — same one-event/`outcome`-as-data shape as `payment.outcome_failed.v1`; `tests/Feature/Domain/Marketplace/UpdateVendorOrderStatusTest.php` `OK (9 tests, 24 assertions)`, including 2 negative tests proving the discrimination genuinely gates on both `from_status` and the target status, not the target status alone — mutation-tested directly (guard temporarily weakened, the negative test confirmed to fail, guard restored). `vendor.evidence_uploaded.v1` — `app/Domain/VendorFulfillment/Actions/UploadEvidence.php` (`ee842e8`) — the real `VendorFulfillment` domain, not the marketplace `UpdateVendorOrderStatus` (confirmed distinct: `VendorProcessingStatus` has no evidence-upload status at all); `tests/Feature/Domain/VendorFulfillment/EvidenceUploadTest.php` `OK (4 tests, 11 assertions)`. `renewal.submitted.v1` — `app/Domain/Renewal/Actions/OpenRenewal.php` (`2b9148d`) — the online submission path only; `tests/Feature/Domain/Renewal/OpenRenewalTest.php` `OK (6 tests, 9 assertions)`, including a negative test proving the offline `MarkExternalRenewal` path does not also emit this event, kept distinct from the pre-existing `renewal.marked_external.v1`. All 5 events are catalogued in `docs/contracts/event-catalog.md` (5 clean new rows, no edits to any existing row) and wired into `database/migrations/2026_08_09_100020_seed_notification_templates_from_matrix.php`'s `outboxEventName()` match. All 5 test runs confirmed locally against real Postgres 18 + Redis 8.2 via the pinned `ghcr.io/andrianm28/makam-app` Docker image, not SQLite — not yet an actual GitHub Actions run, this branch has no PR open yet. **Real cumulative count: 14 of 16** real-producer-needing rows now covered (9 before this pass + 5 this pass). **2 rows remain**, both genuinely unbuilt features rather than a notification-wiring gap, unchanged from the prior pass's finding: Reminder due (no scheduled job or eligibility logic exists anywhere) and Renewal paid/verified (no dedicated online-payment Action exists yet) — both explicitly out of scope for this plan too. 14/16 is real, substantial progress, but this box's full literal claim is the entire 16-row matrix, not evidenced end-to-end yet, so it stays unchecked. **Further progress this pass (25 Aug 2026, `renewal-online-payment` lane, Task 4):** closes the LAST remaining unproduced row — Renewal paid/verified is no longer "no dedicated online-payment Action exists yet"; Task 2 of this same lane built `App\Domain\Renewal\Actions\MarkRenewalPaidOnline`, which emits `renewal.paid_online.v1` (`app/Domain/Renewal/Actions/MarkRenewalPaidOnline.php:150-162`). This task wires that real event into `database/migrations/2026_08_09_100020_seed_notification_templates_from_matrix.php`'s `outboxEventName()` match (`'Renewal paid/verified' => 'renewal.paid_online.v1'`) and catalogues it in `docs/contracts/event-catalog.md` (already present from Task 2, unedited by this task). `NotificationTemplatePersistenceTest::test_only_the_twelve_ruled_rows_carry_an_outbox_event_name()` proves the seed-level match. The real end-to-end proof is `tests/Feature/Domain/Renewal/RenewalPaidOnlineNotificationTest.php` (2 tests, new): `test_marking_a_renewal_paid_online_dispatches_a_matched_notification_event` runs the real `MarkRenewalPaidOnline` Action, then the real outbox-consumption seam (`ConsumeOutboxNotificationJob::dispatchSync()` → `Actions\DispatchNotification::consumeOutboxEvent()`, the same seam `NotificationDispatchPipelineTest.php` exercises), and asserts a real `notification_events` row is written and correctly matched to the "Renewal paid/verified" template; `test_a_redelivered_outbox_event_does_not_double_record_the_notification_event` proves the AC8 redelivery-safety property. **Honest limitation, not glossed over**: `App\Platform\Notification\ProvisionalAggregateNotificationSubjectSource::subjectFor()` has no `'renewal'` arm (its own "Partially live" doc block names exactly this gap) — the SAME pre-existing condition `renewal.submitted.v1` already has, unchanged by this task. Recipient resolution therefore returns zero recipients and zero deliveries for this event today; the new test asserts that zero honestly (`assertSame(0, NotificationRecipient::...)`, `assertSame(0, NotificationDelivery::...)`) rather than skipping the question. Wiring a real `'renewal'` subject source is separate engineering work, not in this task's file list. All tests run against real Postgres 18 + Redis 8.2 via the pinned `ghcr.io/andrianm28/makam-app@sha256:c88edacab071...` image (same recipe Tasks 1-3 of this lane used): `vendor/bin/phpunit tests/Feature/Domain/Renewal/MarkRenewalPaidOnlineTest.php tests/Feature/Domain/Renewal/RenewalPaidOnlineNotificationTest.php tests/Feature/Notification/NotificationTemplatePersistenceTest.php` → `OK (23 tests, 241 assertions)`; the broader `tests/Feature/Notification/ tests/Feature/Domain/Renewal/ tests/Feature/Payment/ tests/Feature/OrderWorkflow/OrderNotificationTest.php` set re-run clean alongside it, `OK (349 tests, 1819 assertions)`, no regression. `vendor/bin/pint --test` and `vendor/bin/phpstan analyse` clean on the touched files and whole-project respectively. Not yet an actual GitHub Actions run — this branch has no PR yet. **Real cumulative count: 15 of 16** real-producer-needing rows now covered. **1 row remains**: Reminder due — checked again for this pass, still genuinely unbuilt (no scheduled job, no eligibility query, no reminder-related code anywhere in `app/` beyond a doc-comment placeholder in `RenewalStatus.php`); this plan does not touch it, per the user's own separate decision to build only its eligibility-query half in a different, smaller piece of work. This box's full literal claim (all 16 real-producer-needing rows evidenced end-to-end) is therefore still not satisfied — 15/16, one row short — so it stays unchecked. **Live UAT sweep, 25 Aug 2026 — `payment.outcome_failed.v1` re-confirmed live, plus a real, honestly-scoped limitation.** `App\Platform\Payment\Actions\ApplyPaymentSettlement::applyOutcome()` is confirmed to still emit `payment.outcome_failed.v1` on the outbox for claimed failed/expired events (idempotency-guarded, first transition only), re-run today via `vendor/bin/phpunit tests/Feature/Payment/ProcessWebhookEventTest.php` fresh against real Postgres 18 + Redis 8.2: 16 tests, 63 assertions, OK — no regression since the 24 Aug pass that introduced it. **Honest limitation, same class as the already-documented `'renewal'` gap above**: `ProvisionalAggregateNotificationSubjectSource::subjectFor()`'s match arms are only `'booking_draft'`, `'order'`, and `'quote'` (confirmed by direct read, `app/Platform/Notification/ProvisionalAggregateNotificationSubjectSource.php:119-127`) — there is no `'payment_session'` arm either, so `payment.outcome_failed.v1`'s `notification_events` row is recorded but recipient resolution today returns zero recipients and zero deliveries, exactly mirroring `renewal.submitted.v1`/`renewal.paid_online.v1`'s already-noted gap. This does not change the row's "producer exists" status (still counted in the 15/16 above) but means the row is not yet delivering to anyone in practice — wiring a `'payment_session'` subject-source arm is separate engineering work, not evidenced here. **Separately, live evidence for the `MENUNGGU_KETERSEDIAAN`/`PENAWARAN_TERKIRIM` bridge rows already cited above**: real order `MK-2026-BHXYGZCH` on the live beta host shows both transitions actually executed in production (Diverifikasi → Menunggu Ketersediaan → Penawaran Terkirim, Rp 900.000 accepted), corroborating `test_availability_requested_notification_dispatched_when_order_enters_menunggu_ketersediaan`/`test_availability_confirmed_notification_dispatched_when_order_enters_penawaran_terkirim` (`OrderNotificationTest.php`, already cited above) with a real production instance, not just fixture data. The underlying admin Filament transition actions themselves (`RequestAvailability`, `IssueOrderQuote`) are separately covered by `tests/Feature/OrderWorkflow/AdminOperatorActionsTest.php::test_request_availability_from_diverifikasi` and `::test_issue_order_quote_composes_from_draft_and_issues_quote`. **Correction, 25 Aug 2026 — the `'renewal'` recipient-resolution gap named twice above (Task 4's note and the Live UAT sweep note) is now closed, real recipients confirmed, not merely wired.** `App\Platform\Notification\ProvisionalAggregateNotificationSubjectSource::subjectFor()` now carries a `'renewal'` match arm (`renewalSubject()`), resolving a renewal's grave record's cemetery as a real scope entity. This makes the "Pengelola TPU/TPS" matrix column resolve a genuine `cemetery_operator` recipient for both `renewal.submitted.v1` and `renewal.paid_online.v1` — previously both recorded a `notification_events` row with zero recipients, ever. **Investigated and stated honestly, not glossed over**: a renewal has no owner/contact reference anywhere in this codebase — `renewals` carries only `grave_record_id`, and `grave_records` has no `user_id`, `contact_email`, or `contact_phone` (`heir_contact_reference` has no write path per that model's own doc block) — so `ownerRef` is always `null` for a renewal subject and the matrix's `Customer: EMAIL/WA` (+ `invoice` for paid/verified) column stays genuinely unreachable; giving a renewal an actual customer contact is separate, unscoped product/engineering work. Proof: `tests/Feature/Domain/Renewal/RenewalSubmittedNotificationTest.php` (2 tests, new) runs the real `OpenRenewal` Action end-to-end through the real outbox-consumption seam and asserts a real `NotificationRecipient` row (`cemetery_operator`), a real `InAppNotification` row, and a queued `EMAIL` delivery (matrix cell `IN_APP/EMAIL`), plus a negative test proving a `renewal.submitted.v1` event for a nonexistent renewal id resolves zero recipients without throwing. `tests/Feature/Domain/Renewal/RenewalPaidOnlineNotificationTest.php`'s existing first test is updated in place (its prior assertion of zero recipients was the bug being fixed, not a property to preserve) to assert the same real `cemetery_operator` recipient and in-app record, with zero `NotificationDelivery` rows (matrix cell `IN_APP` only, no channel token). All four touched/new tests, plus the full `tests/Feature/Notification/` + `tests/Feature/Domain/Renewal/` + `tests/Feature/Payment/` sets (339 tests, 1801 assertions) and the isolated 35-test/289-assertion touched-file run, pass against real disposable Postgres 18 + Redis 8.2 via the pinned `ghcr.io/andrianm28/makam-app@sha256:57037747476e60053fe83b9dd5a0596c44d3cb6bc48b0044fa85a8c3396f9d7c` image — `composer install` layered dev dependencies onto that image's own `--no-dev` vendor tree against this worktree's unmodified `composer.lock` (same recipe the Pulse-dashboard box above already established), not a fresh dependency resolution or SQLite. `vendor/bin/pint --test` (1580 files) and `vendor/bin/phpstan analyse --memory-limit=512M` (0 errors) are clean. Not yet an actual GitHub Actions run — this branch has no PR yet at time of writing. **Unrelated to this fix, and NOT closed by it**: the separate `payment.outcome_failed.v1` / `'payment_session'` gap the Live UAT sweep note above already named remains exactly as described — `subjectFor()` still has no `'payment_session'` arm, out of this fix's scope.
- [x] Email baseline passes. — `tests/Unit/Platform/Notification/MailChannelTest.php` (5 tests) proves real SMTP-path delivery: sends to the resolved address and reports `sent` (`test_it_sends_to_the_resolved_address_and_returns_sent`), correctly reports `unavailable` when no recipient/address resolves (2 tests), reports a transient transport failure as `failed`/retryable, and — a real isolation proof — `test_never_sends_to_the_whole_recipient_set_only_the_delivery_owning_recipient` confirms one delivery never fans out to every recipient in the set. All pass.
- [ ] WhatsApp enabled only with approved template/provider. — No WhatsApp channel implementation exists anywhere in `app/Platform/Notification/Channels/` (only `LogChannel`/`MailChannel`/`NullChannel`) — grepped the whole `app/` tree, zero hits beyond the `G-WA-01` feature-gate concept itself. `feature.whatsapp` seeds `false` by default (`2026_07_26_120400_seed_feature_gate_registry.php:135`), and the closed-gate fallback to email+in-app is real and tested (`WhatsAppModeTest.php`'s `test_closed_gate_resolves_to_email_in_app_fallback_mode`, `ModeResolverTest.php`'s `test_whatsapp_mode_reads_g_wa_01_only`). But the literal claim — WhatsApp gated on an *approved template/provider* — has nothing to evidence: there is no provider integration and no template-approval mechanism to test, because the channel was never built. Leaving unchecked; closing this requires the WhatsApp channel implementation itself, out of evidence-gathering scope.
- [x] Admin/operator/vendor recipient scope passes. — `tests/Feature/Notification/NotificationDispatchPipelineTest.php`'s AC7 tests prove real per-role scoping: `test_ac7_vendor_recipient_gets_an_in_app_record_without_an_external_channel`, `test_ac7_platform_admin_recipient_gets_an_in_app_record`. `test_cross_scope_leakage_an_actor_scoped_to_a_different_cemetery_receives_nothing` is the real negative proof — an actor scoped to the wrong cemetery gets zero rows, not a filtered view. All pass.
- [x] Channel failure does not change business state. — `NotificationDispatchPipelineTest.php::test_ac5_a_throwing_channel_never_changes_business_state_or_propagates` is a direct, named proof of exactly this claim. `test_ac7_the_in_app_record_survives_a_throwing_channel`, `test_failed_delivery_is_retried_then_escalated_to_default_queue_after_max_attempts`, and `test_permanent_channel_failure_is_recorded_without_retry` cover the surrounding resilience/isolation behavior. Passes.
- [x] No sensitive attachment is sent. — No attachment mechanism exists anywhere in `app/Platform/Notification/` — grepped for `attach`/`attachment`, zero hits. `RenderedNotificationMailable` (the only `Mailable` in this platform) has no `attach()` call. The claim holds because the capability to attach anything, sensitive or not, was never built. **UAT closing pass, 25 Aug 2026 — NOTIF-02's "+invoice" half, resolved for the online-payment path specifically.** `docs/contracts/notification-matrix.md`'s own "Payment received" row (line 64) specifies the customer channel as "EMAIL/WA + invoice." Read `App\Domain\OrderWorkflow\Actions\ApplyPaidEffects::emitPaymentReceived()` directly — the online/webhook path's own producer of `payment.received.v1`, reached from a settled webhook via `App\Platform\Payment\Actions\ApplyPaymentSettlement`: its own doc block states the emitted `data` is "REFERENCES ONLY (`AGENTS.md` §Observability, AC7): order id, which trigger, the trigger's own source id, and the amount... No order content, no party, no document, nothing restricted." No invoice reference of any kind is in the payload. Grepped the whole `app/` tree for an `Invoice` model or any invoice-document concept: none exists anywhere in this codebase — every "invoice" reference in `app/Platform/Payment/` (`WebhookEnvelope`, `ProcessWebhookEvent`, `SettlementTargetUnresolvableException`, etc.) is the webhook-matching business-reference sense (`invoice_reference` as a correlation key to a booking `Order`/`MarketplaceOrder`), never a customer-facing document; `App\Domain\OrderWorkflow\OrderReadModel::$invoiceState` is a status enum (`pending`/`not_applicable`), not a document either. Combined with this box's own finding just above (no attachment mechanism exists anywhere in `app/Platform/Notification/`), **the "+invoice" half of NOTIF-02 is a genuine, confirmed gap for the online-payment path, not merely unverified as the prior pass left it** — the same conclusion §C's separate "Invoice only follows approved verification" box (line 60) already reached for the manual-payment path, now confirmed by direct evidence for the online path too: no invoice-document concept exists downstream of EITHER payment path in this codebase today. `tests/Feature/Payment/WebhookPaidEffectsTest.php` and the other `tests/Feature/Payment/` files that mention "invoice" were grepped and confirmed to use the same business-reference-only sense throughout, never a document assertion. This does not reopen NOTIF-02's non-invoice half (customer EMAIL/WA notification, admin/pengelola IN_APP, vendor IN_APP when allocated), which stays evidenced as before via the notification-matrix box above. **CLOSED for the online-payment path, 26 Aug 2026.** A basic invoice/receipt concept now exists: `order_invoices` (`database/migrations/2026_08_26_100000_create_order_invoices_table.php`) — one row per order, a generated `reference` (`'INV-'.Str::upper(Str::random(10))`), `amount_minor`, `currency`, a single `summary` line, and `issued_at`; written by the new single-writer `App\Domain\OrderWorkflow\Actions\IssueInvoice` (`Audit::wrap()`, action `ORDER_INVOICE_ISSUED`, idempotent per order via `order_invoices_order_id_unq`). `ApplyPaidEffects::apply()` now calls it in the SAME transaction as the `DIBAYAR` transition, right after `stampPaidSource()` and before `emitPaymentReceived()`; `emitPaymentReceived()`'s outbox payload gains one additive field, `invoice_reference` — still references-only, no event-catalog change (`payment.received.v1`'s existing row already names `invoice` among its consumers). The customer-facing surface is a public, unauthenticated HTML receipt page, `GET /kwitansi/{reference}` (`App\Livewire\Public\Invoices\InvoiceReceiptPage`), keyed by the invoice's own unguessable reference — the same reference-keyed-not-id-keyed shape `/langganan/{subscriptionReference}` already uses. Deliberately minimal per the task's own framing: no multi-line-item model, no tax calculation, no sequential legal invoice numbering scheme — none of those were asked for and guessing at Indonesian tax/invoice-numbering compliance rules was explicitly out of scope. Real evidence, run against a disposable `postgres:18` + `redis:8.2-alpine` pair through the pinned CI image (`ghcr.io/andrianm28/makam-app:sha-450d4fff52d4`, `vendor/bin/phpunit` directly): `tests/Feature/OrderWorkflow/IssueInvoiceTest.php` (new — issuance, one audit row, real double-invocation for the same order returns the same invoice and writes no second row, retrievable by its own reference), `tests/Feature/Livewire/Public/Invoices/InvoiceReceiptPageTest.php` (new — page renders the real invoice, 404 on an unknown reference), and the extended `tests/Feature/OrderWorkflow/ApplyPaidEffectsTest.php` (the outbox payload now asserts a real `invoice_reference`; the "same webhook delivered twice" test now also asserts exactly one `order_invoices` row) — `OK (31 tests, 183 assertions)` for this pass's four files, `OK (328 tests, 1712 assertions), 1 skipped` for the full `tests/Feature/OrderWorkflow` + `tests/Feature/Payment` regression sweep (the one skip is pre-existing and unrelated). `vendor/bin/pint --test` and `vendor/bin/phpstan analyse` both clean on every touched file; `bash ci/verify-docs.sh` — `ALL DOC GATES PASS`. **The manual-payment-path half of this gap stays open**, unchanged and still blocked on PAY-02's missing `payment_verifications` amount column — `VerifyManualPayment`/`payment_verifications` were deliberately not touched by this pass. This is new financial-artifact-generation logic tied to a real "order paid" event; per `AGENTS.md` §Infrastructure-agent execution it is flagged for human review before merge and was not self-merged.

## E. Marketplace/vendor

- [x] All minimum products/categories seeded or configured. — `tests/Feature/Domain/Marketplace/VendorListingBootstrapTest.php::test_every_product_code_has_an_active_seeded_listing` is exhaustive — it iterates `ProductCode::KNOWN_CODES` itself (not a hardcoded subset) and asserts every one has a real, active seeded listing. `test_the_seed_migration_creates_five_vendors_and_nine_listings` and `test_seeding_twice_is_idempotent` cover the seed migration's own correctness. Passes.
- [x] Single-vendor cart constraint is explicit. — `tests/Feature/Domain/Marketplace/CartTest.php::test_a_second_vendor_returns_a_conflict_and_changes_nothing` proves the constraint is a real, explicit `CartConflict` value object returned to the caller — not silent merging or a rejected write. `test_replacing_the_cart_is_explicit_and_only_then_clears_items` proves switching vendors is a deliberate action, never automatic. `VendorListingBootstrapTest.php::test_adding_across_seeded_vendors_surfaces_the_single_vendor_conflict` confirms the same behavior against real seeded data. All pass.
- [x] Vendor can accept/process/update/evidence. — `tests/browser/e2e-admin-vendor.spec.ts::'vendor can advance an incoming order through a one-click work-queue action'` (added this pass) drives the real `/vendor/pesanan` work-queue UI end to end: opens the seed fixture's own order, clicks a real one-click status action (`App\Filament\Vendor\Resources\VendorOrders\Pages\EditVendorOrder` — normally "Terima pesanan" on a fresh fixture, retry-safe branching to the next meaningful action if a prior attempt already advanced it), and asserts both the success notification and the status badge updating on the list page. The write path (`UpdateVendorOrderStatus`) is the same audited Domain Action `tests/Feature/Filament/Vendor/VendorOrderStatusTransitionActionsTest.php` already covers at the Feature level — this is the first real browser-level proof of it. That test has passed a real local CI-equivalent run (Postgres 18 + Redis, the pinned app image, `php artisan serve`, a real Chromium browser) but not yet an actual CI run — this branch has not been opened as a PR yet, so CI confirmation of it specifically is still pending (same caveat §A-27 already carries for this same test). **Follow-up (test-coverage-only, added later this same day):** the manual-cancellation path (`DIBATALKAN`) had zero test coverage — the status `Select` in `VendorOrderForm` offers all 8 `VendorProcessingStatus::KNOWN_STATUSES` including `DIBATALKAN`, with no dedicated one-click action for it (unlike accept/reject/process/sendSchedule/complete/complain). `VendorOrderEditFormTest::test_a_vendor_can_cancel_an_order_through_the_form` now proves it end to end through the real form/`UpdateVendorOrderStatus` write path: the status persists, `VendorOrder::isCancelled()` reflects it, the audit row is written, and — checked and asserted honestly rather than assumed — `vendor_order.decided.v1` does NOT fire, because that event's own doc block scopes it to the `MENUNGGU_VENDOR` → `{DITERIMA_VENDOR, DITOLAK_VENDOR}` decision only; cancellation is a real, pre-existing notification-matrix gap (no "vendor cancelled" row exists in `docs/contracts/notification-matrix.md`), not a bug introduced or fixed by this pass. `UpdateVendorOrderStatusTest` adds two matching Action-level tests (`MENUNGGU_VENDOR` → `DIBATALKAN` and the more realistic `DITERIMA_VENDOR` → `DIBATALKAN` correction-after-acceptance case), both confirming no outbox event. Cross-vendor scoping for this same form is already covered generically by `VendorPanelScopingTest` (behavioural + structural `ScopesToCurrentVendor` proof over every panel Resource, `VendorOrderResource` included) — confirmed, not duplicated. All 49 tests across the four touched/related files (`UpdateVendorOrderStatusTest`, `VendorOrderEditFormTest`, `VendorOrderStatusTransitionActionsTest`, `VendorPanelScopingTest`) pass against real Postgres 18 + Redis 8.2 via the pinned `ghcr.io/andrianm28/makam-app:sha-450d4fff52d4` image (`OK (49 tests, 183 assertions)`); the broader `tests/Feature/Domain/Marketplace/` + `tests/Feature/Filament/Vendor/` suite re-run clean alongside it (`OK (139 tests, 780 assertions)`), no regression. `vendor/bin/pint --test` clean (1580 files) and `vendor/bin/phpstan analyse --memory-limit=1G` clean, both run inside the same pinned image. Not yet an actual GitHub Actions run at the time of this note — PR not opened yet.
- [x] Vendor transaction history and payout reference are scoped. — `tests/browser/e2e-admin-vendor.spec.ts::'vendor transaction history is reachable and scoped to this vendor only'` (PR #142, merged 22 Aug 2026) is a real cross-vendor leakage proof: the seed migration creates two fixture `vendor_orders` rows on two different vendors, and the test asserts the granted vendor's own order is visible on `/vendor/transaksi` while the other vendor's order is not. `::'vendor payout status/reference is visible and scoped'` covers payout visibility. Both pass in CI. **Live UAT sweep, 25 Aug 2026 — vendor test-login gap closed, live payables/settlement UI confirmed.** `e2e-vendor@example.test` now carries both `ActorRole::VENDOR` and a real `scope_assignments` VENDOR-scoped grant on the live beta host, granted via the audited `identity:grant-scope` console command today (reason logged: "UAT MKT-10 coverage"), verified with a real `Auth::attempt()` success — this closes the account-provisioning gap that previously blocked a genuinely live vendor-panel walkthrough. With that account, the vendor payables page itself (`/vendor/payouts`, `App\Filament\Vendor\Pages\PayoutStatus`) was confirmed live as real, correctly scoped, and correctly empty-stated. On the admin side, `/admin/rekonsiliasi` and `/admin/laporan-kinerja-vendor` both correctly show empty states — zero real vendor orders/settlements exist yet in production, a data gap rather than a code gap.
- [x] Customer order tracking passes. — `tests/browser/e2e-marketplace.spec.ts`'s `'checkout, its post-order state, and order tracking are all accessible'` (inside `'E2E-MKT — checkout and manual payment'`) is customer-facing, not vendor-side (vendor-side tracking is the two already-checked boxes above) — it walks a real checkout through to the order-tracking view. `order-tracking.blade.php`'s own "Menunggu vendor" fallback span keeps the state honest rather than presupposing a vendor has acted. Passes in CI. **Live UAT sweep, 25 Aug 2026.** Live-checked the real route `/marketplace/pesanan/{orderNumber}` (`App\Livewire\Public\Marketplace\OrderTracking`) directly against production: it correctly renders "Pesanan tidak ditemukan" for a nonexistent order number rather than leaking whether any order exists (enumeration-safe), consistent with zero real marketplace orders existing in production today.
- [x] Customer can file a complaint on their own marketplace order. — **Closed (26 Aug 2026).** UAT that day found `VendorProcessingStatus::KOMPLAIN` had exactly one write path — the vendor's own one-click "Tandai komplain" self-flag (`EditVendorOrder::getHeaderActions()`) — and no customer-facing entry point anywhere. `OrderTracking::fileComplaint()` is the second, customer-initiated caller of the SAME single audited write path, `UpdateVendorOrderStatus` (no parallel write path introduced). Eligibility is a new named closed list, `VendorProcessingStatus::CUSTOMER_COMPLAINT_ELIGIBLE_STATUSES` (`DITERIMA_VENDOR`, `DIPROSES`, `DIKIRIM_OR_DIJADWALKAN` — after the vendor has responded, before a terminal state), checked server-side inside `fileComplaint()` itself, not only hidden behind the view's `@if`; ownership is re-resolved fresh from `MarketplaceOrderQuery::findForCustomer()` inside the same method rather than trusting the last render, with a foreign order treated identically to a nonexistent one (no distinguishing error), matching the page's existing enumeration-safety contract. The reason is required (`min:10`) and written into `vendor_orders.notes` prefixed `"Komplain pelanggan: "` — the field's own existing purpose, no new column added. `UpdateVendorOrderStatus` additionally now emits a `vendor_order.complaint_filed.v1` outbox event on any transition into `KOMPLAIN` (from either write path) — durable and idempotent, but deliberately not wired to a new `docs/contracts/notification-matrix.md` row in this same change: that module's 17 rows are pinned by `TemplateRendererTest::assertCount(17, ...)`, and this repo already treats matrix-row additions as their own dedicated lane (see the merged `feat/notification-matrix-cheap-rows` history) — an unmatched `outbox_event_name` is consumed as "nothing to send," silently and correctly, per `DispatchNotificationConsumerOnOutboxEventPublished`'s own documented behaviour, so nothing is broken by deferring that row; flagged as a reasonable, deliberately-scoped follow-up, not an oversight. New Feature-level coverage: `tests/Feature/Livewire/Public/Marketplace/OrderTrackingScreenTest.php` (13 tests total, 5 new: eligible-status filing transitions to `KOMPLAIN` with the audit row, `actor_role = 'customer'`, and the outbox event, all asserted directly; a blank reason is refused via Livewire's real validation error bag; each of the 5 ineligible statuses — `MENUNGGU_VENDOR`, `DITOLAK_VENDOR`, `SELESAI`, `KOMPLAIN` already, `DIBATALKAN`, via `#[DataProvider]` — hides the action from the view AND refuses a direct `fileComplaint()` call server-side, zero rows changed; a foreign customer's direct call against someone else's order changes nothing, real ownership proof, not assumed) — `OK (13 tests, 67 assertions)`. The broader `tests/Feature/Domain/Marketplace/` + `tests/Feature/Filament/Vendor/` + `tests/Feature/Livewire/Public/Marketplace/` suite re-run clean alongside it (`OK (218 tests, 1180 assertions)`), no regression — all run against real Postgres 18 + Redis 8.2 via the pinned `ghcr.io/andrianm28/makam-app:sha-450d4fff52d4` image. `vendor/bin/pint --test` clean (1583 files) and `vendor/bin/phpstan analyse --memory-limit=1G` clean, both run inside the same pinned image; `bash ci/verify-docs.sh` clean (13/13 gates). A new Playwright test, `tests/browser/e2e-marketplace.spec.ts::'a freshly-placed order cannot be complained about yet — the action is not shown'`, proves the negative/hidden-state case deterministically; a full guest-checkout → vendor-accepts → customer-complains cross-role browser test was investigated and deliberately NOT added — `tests/browser/e2e-admin-vendor.spec.ts`'s `e2e-vendor` fixture login is granted whichever vendor a UUID sort happens to pick first (`vendors.id` is a UUID primary key, so that ordering carries no relation to this suite's fixed `PRODUCT_A`/`PRODUCT_B` catalogue codes), so such a test would not be reliably deterministic in CI without new fixture infrastructure — the eligible-status path is instead covered thoroughly at the Feature/Livewire level above. Not yet an actual GitHub Actions run at the time of this note — PR not opened yet, new Playwright test therefore NOT YET CI-confirmed.

## F. Renewal/data

- [ ] Search performance target passes. — **Partially closed (23 Aug 2026).** `BenchGraveSearchCommand` continues to certify the raw query at full scale (p50 7.02ms/p95 7.19ms/p99 7.34ms at 100k records, real `bench:grave-search` run). **New this pass**: `tests/Feature/Livewire/Public/Renewal/GraveSearchPerformanceTest.php::test_a_full_search_request_completes_within_the_500ms_budget_at_a_representative_scale` proves the FULL request cycle (`Livewire::test(GraveSearch::class)`, the same component the public `/perpanjangan/cari` route uses) also completes well inside the 500ms budget at a real, seeded 5,000-record single-cemetery dataset — 120.24ms observed. **Scale note, stated explicitly rather than left implicit**: this is ~5% of the certified 100k target, not a full-scale recertification — generating a genuine 100k-record dataset inside a routine PHPUnit run is impractical; the reasoning for why a smaller dataset still validly answers the request-level question is recorded in the test's own doc block. Full-scale (100k+) request-level certification, if ever needed, belongs with whatever dedicated performance-testing infrastructure (e.g. the k6 profiles referenced elsewhere in this document) runs outside routine CI.

**UAT closing pass, 25 Aug 2026 — REN-01/REN-02 classification vs. REN-04/REN-05.** Live `G-DATA-01` state re-checked directly against the beta database (`feature_gates` row, not inferred): `state=closed`, `evidence_reference` empty, `updated_at=2026-08-19 14:41:26` — unchanged since before today's UAT pass; no admin has opened it today. `App\Domain\GraveRegistry\GraveRegistryPublicQuery` (the class REN-01's own tests exercise) has exactly one production caller anywhere in `app/` — `App\Livewire\Public\Renewal\GraveSearch` (confirmed by grep) — and that component is itself the one that checks `G-DATA-01` before running a search. There is no ungated grave-search entry point anywhere in this codebase, so REN-01's live entry point (`/perpanjangan/cari`) genuinely cannot surface a real record on the live site right now, for the identical root cause REN-04/REN-05 are BLOCKED on. REN-02 (fee/quote) is reached only after REN-01 completes, so it inherits the same live blocker. **Verdict: REN-01 and REN-02 are reclassified from "PASS (Feature-level only)" to BLOCKED for the live-walkthrough claim, matching REN-04/REN-05, for consistency.** The prior "REN-01/02 test gate-independent logic, REN-04/05 test the full journey" framing does not survive a direct code read: `GraveRegistryPublicQuery` and `App\Domain\Renewal\Actions\OpenRenewal` (REN-04's own Action) are equally gate-independent at the domain layer — neither reads `G-DATA-01` (per `GraveRegistryPublicQuery`'s own doc block, "that check belongs to the screen"; `OpenRenewal` has no gate check of any kind) — and both `GraveSearchPerformanceTest.php`/`RenewalFeeTest.php` (REN-01/REN-02) and `OpenRenewalTest.php` (REN-04) prove their claims only by opening the gate inside an isolated test transaction (`FeatureGate::query()->where('gate_id','G-DATA-01')->update(['state'=>'open'])`), never against the live gate's actual state — the same evidentiary shape, not a different one. **What genuinely does distinguish them, stated precisely rather than glossed over**: REN-01/REN-02's underlying claims (search finds the correct record within the documented performance target; the fee is a real computed value, not a placeholder) are proven correct by real, passing, code-level tests that remain true regardless of the live gate's current state — `GraveSearchPerformanceTest.php::test_a_full_search_request_completes_within_the_500ms_budget_at_a_representative_scale` (120.24ms at 5,000 records, cited above) plus `BenchGraveSearchCommand`'s already-cited full-scale 100k-record certification (p95 7.19ms), and `tests/Feature/Domain/Renewal/QuoteRenewalTest.php` for REN-02. REN-05 has no comparable evidence at all (its own UAT row: "None found — flag for automated coverage"), and REN-04's own evidence (`OpenRenewalTest.php`, plus this same session's `RenewalPaidOnlineNotificationTest.php`/`RenewalSubmittedNotificationTest.php` cited in §D) is comparably real. The BLOCKED classification therefore reflects the live-walkthrough claim specifically, not a judgment that the underlying capability is unproven — the exact same nuance REN-04 already carries.
- [x] Empty/manual assistance behavior passes. — `tests/browser/e2e-renewal.spec.ts::'grave search step is honest whether the online capability is open or closed'` exercises the real AC5/§6.2 no-result state: asserts a three-part-honest explanation (what's empty, why, what to do next) via two distinct visible text assertions, plus BOTH an "Input manual" link and a "Hubungi bantuan" (contact support) link are visible — never a bare "not found." Passes as part of this suite.
- [x] Tariff source and last-updated display pass. — `tests/Feature/Livewire/Public/Renewal/RenewalFeeTest.php::test_the_fee_screen_always_shows_the_tariff_source_and_last_update` asserts both "Sumber tarif" and "Terakhir diperbarui" render on the fee screen. `resources/views/livewire/public/renewal/fee.blade.php:129,133` confirms the real markup (`$quote->tariffSource`, `$quote->tariffEffectiveAt?->format('d F Y')`) — matching `docs/product/screen-inventory.md` PUB-032's own definition ("renders tariff source and effective time"), so "last-updated" in this claim means the tariff's effective date, not a separate freshness-sync timestamp — confirmed against the canonical screen record, not assumed.
- [x] External renewal marking and duplicate prevention pass. — **Closed (24 Aug 2026).** The gap this box previously named — "`MarkExternalRenewal` has no Filament UI entry point anywhere" — is now closed: `App\Filament\Admin\Resources\RenewalOrders\Actions\MarkExternalRenewalAction` is a new `ListRenewalOrders` header action (grave search, period/evidence/reason form) wired to the real domain Action, following the exact re-authentication-guard pattern `RecordExternalRenewalPaymentAction` already established for this resource. Its `->authorize()` gate is deliberately coarser than a per-row action's: `RenewalMarkingPolicy::allows()` needs a bound `GraveRecord` to check the cemetery-scope grant, which a header action does not have at visibility time, so the gate checks only "authenticated + `ActorRole::ADMIN`" (Ruling B, 12 Aug 2026) and the real per-grave scope check happens where it already correctly lived, inside `MarkExternalRenewal::__invoke()` itself — an admin who holds the role but lacks the grave's cemetery grant still sees the button and is refused at that real check, proven by a dedicated test below. New Feature-level coverage: `tests/Feature/Filament/MarkExternalRenewalActionTest.php` (6 tests: authorized admin completes the action and a real `Renewal` row with `source = RenewalSource::EXTERNAL` lands; a non-admin cannot even see the action (`isAuthorized()` false); an admin with the role but no cemetery-scope grant is denied at the real per-grave check with zero rows written; a stale (not-recently-authenticated) admin is redirected to `PasswordReauthentication` rather than the action silently succeeding, with the same `RequireRecentAuthentication::REASON_SESSION_KEY`/`url.intended` session fixture `FeatureGateAdminTest` established; a duplicate `(grave_record_id, target_due_period)` pair surfaces the same `DuplicateRenewalPeriodException` the online path raises, not a raw database error; a malformed `target_due_period` — regression lock for a live review finding: an uncaught `Carbon\Exceptions\InvalidFormatException` previously escaped as a 500 — is refused with a clean `date_format:Y-m-d` form validation error before the domain call ever runs, zero rows written, with a `\Throwable` catch also restored around the domain call itself as defense in depth, matching `RecordExternalRenewalPaymentAction`'s own catch shape) — run against real Postgres 18 inside the pinned `ghcr.io/andrianm28/makam-app@sha256:fd978e4cd370...` image (same recipe Tasks 1-3 of this pass used): `./vendor/bin/phpunit tests/Feature/Filament/MarkExternalRenewalActionTest.php` → `OK (6 tests, 54 assertions)`. The two pre-existing sibling suites (`MarkExternalRenewalTest.php`, 11 tests; `RenewalOrderResourceTest.php`, 7 tests) re-run clean alongside it, no regression. This does not need browser-level Playwright proof to close: `e2e-renewal-external.spec.ts` (prior pass) already covers the READ side (a List/View page genuinely renders an externally-marked renewal), and this task closes the MARKING ACTION itself at the Feature/Filament-action level — the same evidentiary bar §C's manual-payment verification box (line 58) closes on. `vendor/bin/pint --test` and `vendor/bin/phpstan analyse` (both run against the new/changed files) are clean.

## G. Security/operations

- [x] No unresolved critical/high security issue without formal acceptance. — `.github/workflows/ci.yml`'s `composer audit --locked` step genuinely gates CI — any finding fails the build. Its `npm audit --audit-level=high` sibling previously ran as `|| true # TODO: fail once the baseline is clean`, swallowing the one confirmed high-severity finding on this branch (`nanoid <3.3.18`, GHSA-2v37-7h3g-55p8, transitive via `postcss`, seen in CI run `32675198044` job `97282009260`, 23 Aug 2026). Fixed by pinning `nanoid` to `^3.3.18` via a `package.json`/`package-lock.json` `overrides` entry (real registry metadata for `nanoid@3.3.18` fetched from `https://registry.npmjs.org/nanoid/3.3.18`, independently cross-checked 4 separate times against the real npm registry) and dropping the `|| true` swallow so the audit step now genuinely gates. **Confirmed 24 Aug 2026** by a real CI run on this branch (run `32686870526`, job `97313520700`): `composer audit --locked` → "No security vulnerability advisories found."; `npm audit --audit-level=high` → "found 0 vulnerabilities" — both halves of this box's compound claim now have a real, executed, passing CI result, not a local-only claim.
- [ ] Authorization, audit, upload, migration, backup/restore, and rollback tests pass. — A 6-part compound claim; real evidence for 4 of 6, genuine gaps for the other 2: **authorization** — strong (`OrderTransitionAuthorizerTest.php`, `PaymentAuthorizerBindingTest.php`, `MasterDataAdminAuthorizerTest.php`, `RoleBasedAuditReadAuthorizerTest.php`, all real role-scoped access tests). **audit** — strong (`AuditEventAppendOnlyTest.php` proves the append-only/immutability guarantee directly; `AuditWrapTransactionTest.php`/`AuditRecordTest.php` cover the write path). **Live UAT sweep, 25 Aug 2026 — DocumentVault access-audit specifically.** `RecordDocumentAccessTest::test_an_allowed_access_records_every_ac8_field` and `DocumentAccessEventAppendOnlyTest` extend this same "audit" sub-claim to the DocumentVault's own access log, not just the generic audit-event pipeline — read and confirmed to match by method name/assertions this pass, not re-executed live (a PHP version mismatch on this host, 8.3 vs. the pinned ~8.5, blocked local execution today). **upload** — strong (`tests/Feature/DocumentVault/UploadDocumentTest.php::test_it_quarantines_a_new_upload_and_never_writes_under_accepted` proves fail-closed-by-default quarantine placement). **migration** (rollback-specific) — real, single-migration coverage (`tests/Feature/Database/Migrations/FixCustomerAndUploaderIdentityColumnsRollbackTest.php` instantiates `2026_08_22_100000_fix_customer_and_uploader_identity_columns.php` directly and proves its `down()` reverts all 4 identity columns' type and drops their FK constraints, verified against real Postgres). **backup/restore** — genuinely not built: ADR-0035 item 2 records this as "Lane D2, not yet built," an already-formally-accepted risk, not a silent gap. **rollback** (deploy/CI rollback) overlaps with §H's still-open CI/CD box — no independent evidence found here either. Leaving unchecked: 4 real sub-areas pass, 2 are genuine, already-tracked gaps.
- [ ] Support contacts, hours, incident owner, and escalation are configured. — **Verified directly against the live host, 24 Aug 2026 — real, negative finding, not an unknown anymore.** The mechanism is real and built: `App\Platform\SiteSettings\Models\SiteSetting::KEY_SERVICE_HOURS`/`KEY_SUPPORT_PHONE`/`KEY_SUPPORT_WHATSAPP`/`KEY_SUPPORT_EMAIL` are real, admin-editable fields, resolved through `SettingsService`'s config→env→`site_settings`→default precedence (`App\Support\ContactInfo`). A direct query against the live `makam_beta.site_settings` table returns **zero rows** — nothing has been configured through the admin Site Settings panel. This means every real customer visiting the live public site right now sees `ContactInfo`'s deliberately-fictional fallback values: phone `+62 812-0000-1234` (an intentional placeholder pattern, not a real number), business hours `Senin–Jumat 08.00–17.00 WIB` (a placeholder), and an email address (`bantuan@makam.co.id`) that uses the real domain but — per that class's own doc comment — "is NOT actually configured to receive mail until an operator sets a real, monitored inbox." The fallback mechanism itself is honest-by-design (labeled placeholders, not a crash), so this is not a code defect — it is a real, live operational gap: someone with admin panel access needs to enter the real phone/hours/email through Site Settings. Incident response has a real documented procedure (`docs/operations/runbooks.md` §1-13), but "incident owner" there is a per-incident *assignment*, not a single static configured value — the closest precedent is ADR-0035 item 1's named daily payment-reconciliation owner (Ira), scoped to one incident type only. Leaving unchecked — this is now a real, actionable data-entry task, not an unverifiable unknown. **Live UAT sweep, 25 Aug 2026 — a second, related content gap on the same live resource.** `SiteSettingsResource` (`/admin/pengaturan-situs`) itself renders and saves correctly (a genuine PASS as a piece of admin UI), but its company name/address fields are also empty on the live host — the same "zero rows configured" condition already established above, just a different field set. As a direct consequence, the public footer is silently falling back to placeholder text ("PT Contoh Makam Digital Indonesia...") rather than a real company name/address. Worth flagging for the project owner to fill in with real data alongside the support-contact fields already named above; not a new code gap, the same fallback-honesty mechanism already covers it.

## H. Technical production-readiness

- [x] Runtime/package versions match `technology-baseline.md` and lockfiles. — CI's dedicated "Verify pinned runtime versions" job (`.github/workflows/ci.yml`) asserts PHP ~8.5.0 and Node 24.x exactly against `docs/architecture/technology-baseline.md` §2's pinned baseline; PostgreSQL 18 and Redis 8.2.x are pinned via CI service-container image tags (`postgres:18`, `redis:8.2-alpine`, verified at 6 separate job definitions) matching the same baseline table; `composer.lock`/`package-lock.json` are validated against their manifests in the same CI run.
- [x] Horizon supervisors, queue priorities, long-wait alerts, and graceful restart pass. — **Closed (24 Aug 2026) — but only after this rehearsal found and fixed two real, previously-unknown, production-blocking bugs in `config/horizon.php` itself.** Nobody had ever actually run `php artisan horizon` against the config authored in PR #150 before this task; both bugs were latent from that PR and would have crashed Horizon the first time anyone tried to start it for real, in production included.
  - **Bug 1 — invalid `minProcesses => 0`.** `defaults.supervisor-batch`/`supervisor-reports` (and `production`'s explicit override of the same two) set `minProcesses => 0`, intending true zero-idle capacity. `Laravel\Horizon\ProvisioningPlan::convert()` throws unconditionally (`"The value of [...minProcesses] must be greater than 0."`) whenever any environment's `minProcesses` is below 1, and it validates **every** environment block eagerly at construction, regardless of which one is actually being deployed — so this crashed `php artisan horizon` in **all three** of `production`/`staging`/`local`, not just the two that set `minProcesses => 0` directly. Staging did not avoid this: it never reached `supervisor-batch`/`supervisor-reports`'s `minProcesses` at all, because its own `supervisor-critical`/`urgent`/`notify`/`default`/`batch`/`reports` entries were all `false` (Bug 2, below) — the shared, environment-independent construction would have thrown on staging too, just via that different supervisor entry, reached first in the `defaults` key order. Confirmed via `vendor/laravel/horizon/src/SupervisorOptions.php:68` that Horizon's own default for `minProcesses` is already `1`, not `0` — zero-idle scaling was never achievable in this Horizon version, so `1` is the correct value, not a workaround. Fixed: `minProcesses => 1` in `config/horizon.php:238,256` (`defaults`) and `:299-300` (`production`'s override), with comments recording why.
  - **Bug 2 — `'supervisor-X' => false` also crashes construction.** Used 8 times (`production.supervisor-normal`; `staging`'s 6 baseline/batch/report supervisors; `local.supervisor-normal`) to mean "disabled for this environment." `ProvisioningPlan::toSupervisorOptions()` calls `convert()` on every supervisor entry unconditionally, including `false`-valued ones; `array_replace_recursive()` replaces a nested default array with the bare scalar `false` rather than merging into it, so `convert()` builds an options array with no `connection` key, and `SupervisorOptions::fromArray()` unconditionally reads `$array['connection']` — an `ErrorException: Undefined array key "connection"`. Confirmed by direct `array_replace_recursive` reproduction and by reading `SupervisorOptions::fromArray()`. Real, crash-free mechanism for "declared but not started": `ProvisioningPlan::deploy()`'s own gate is `if ($options->maxProcesses > 0) { $this->add($options); }` — so `['maxProcesses' => 0]` (a real array, merges cleanly, `minProcesses` left untouched) achieves the identical outcome without crashing. Fixed: all 8 `=> false` entries replaced with `=> ['maxProcesses' => 0]` (`config/horizon.php:321` production, `:338-343` staging, `:354` local), with a comment at the first occurrence and a shared reference from the rest.
  - **Rehearsal, post-fix, in a disposable environment** (never `dev-web`/`beta-web`/production — see scope note below): `rg2-pg` (`postgres:18`), `rg2-redis` (`redis:8.2-alpine`), and `rg2-horizon-scratch` (the pinned `ghcr.io/andrianm28/makam-app@sha256:fd978e4cd3706ebd7fab85654cb806bfa7424086371c8c0a793f7e141d032d51` image, this worktree's real, fixed `config/horizon.php` bind-mounted in, `--network host`, `--user 1000:1000`), all on `yiemvm`, torn down afterward. `php artisan migrate --force` ran clean. `php artisan horizon --environment=local` booted with **zero errors** (previously: 2 fatal `ErrorException`/`Exception` log entries at 10:10:02 and 10:13:08 UTC, none after the fix) and all 6 real supervisors running, correctly mapped: `supervisor-critical` → `redis:critical`, `supervisor-urgent` → `redis:urgent`, `supervisor-notify` → `redis:notifications`, `supervisor-default` → `redis:default`, `supervisor-batch` → `redis_batch:imports,redis_batch:media`, `supervisor-reports` → `redis_batch:reports`. Dispatched 2 real `App\Platform\Outbox\Jobs\PublishOutboxEventJob` jobs (via `Outbox::record()` against disposable, synthetically-named fixture rows so no real notification listener fires) onto `critical` and `reports` — 2 genuinely different-priority queues on different connections (`redis` 90s retry_after vs `redis_batch` 1000s) — both confirmed `status=completed` via `Laravel\Horizon\Contracts\JobRepository::getRecent()`, `failed_jobs` count `0`. Graceful restart: dispatched a 3rd job onto `urgent`, ran `php artisan horizon:terminate` immediately after, confirmed via the same job repository that it completed (`status=completed`) rather than being dropped or landing in `failed_jobs`. Also independently re-verified the `production` block specifically (the more load-bearing check, since production has no live rehearsal opportunity yet): `php artisan horizon --environment=production` inside the same scratch setup booted clean, all 6 supervisors running, zero new log errors, then cleanly terminated. All 3 scratch containers destroyed afterward (`docker rm -f rg2-horizon-scratch rg2-pg rg2-redis`; confirmed absent from `docker ps -a`).
  - Long-wait alerting remains real and independently tested, unchanged by this task: `app/Console/Commands/SpineWatchdogCommand.php` + `tests/Feature/Console/SpineWatchdogCommandTest.php`, scheduled every 5 minutes (`routes/console.php`).
  - **Scope note**: this proves the mechanism — supervisors, queue-priority routing, graceful restart — genuinely works against the real, current, now-corrected `config/horizon.php`, in a disposable environment built from the pinned `beta-web`-equivalent image with this worktree's code mounted in. It does **not** mean any live environment is currently running the fixed config: `dev-web` is still pinned to the PR #141 preview build, which predates `config/horizon.php` existing in the image at all (confirmed directly: `ls /var/www/html/config/` inside `makam-nonprod-dev-web-1` has no `horizon.php`), and `beta-web`/a real `production` environment have not been redeployed since this fix landed either. `dev-web`'s pinning was never touched by this task (confirmed unchanged throughout: `sha256:c23eefa419b29809284d6a9d3b62e4eaf62a9f6faab6fbcb69652d471f969512`) and Horizon does not persist on `dev` afterward, matching its documented convention.
  - **Separate, still-open follow-up (not a blocker for this box, but a real gap)**: dev's real `APP_ENV` is `development`, confirmed directly (`docker exec makam-nonprod-dev-web-1 printenv APP_ENV`). `config/horizon.php`'s `environments` keys are only `production`/`staging`/`local`, matched via `Laravel\Horizon\ProvisioningPlan::deploy()`'s `Str::is($name, $environment)` (exact or wildcard match, no partial match) — `development` matches none of them. Confirmed empirically before the fix (matching behavior is unaffected by it): a plain `php artisan horizon` on dev reports `"Horizon is running"` but deploys **zero supervisors** (`horizon:supervisors` → `"No supervisors are running."`) — a silent no-op, not an error. Someone needs to either add a `development` (or `dev*`) environment block, or document that dev must always be started with an explicit `--environment=local` (or similar) flag — this rehearsal used `--environment=local` throughout specifically because of this gap. **Ordering constraint, updated 24 Aug 2026 per the user's explicit resolution of §I's dev/beta shared-queue finding**: the isolation this constraint originally waited on is not coming — the user has decided the shared `REDIS_PREFIX`/`APP_KEY`/session/credential state is intentional, to keep dev promotable straight into beta (§I below). This does not make starting real, persistent Horizon supervisors on dev free of consequence: today `dev` and `beta` still share the same Redis keyspace, so persistent dev supervisors would still consume beta's live queues, not just dev's — that fact is unchanged, only the "wait for isolation, then it's safe" framing is gone. Do NOT add a `development` Horizon block or start Horizon on dev outside a disposable, torn-down rehearsal like this one without a fresh, explicit decision that accounts for that consequence directly — it is a different question from whether the underlying credential-sharing is acceptable.
- [x] Horizon has a real `development`-environment provisioning path. — **Closed (24 Aug 2026) via documented convention.** Dev's real `APP_ENV` is `development`; `config/horizon.php`'s `environments` keys are only `production`/`staging`/`local` — this array intentionally has no `development`/`dev*` key since dev's real queue mechanism is a plain `queue:work` worker (Compose `dev-worker` profile, `docs/operations/examples/docker-compose.dev-stg.yml` lines 82–91), not Horizon. A documented convention now records that if Horizon is ever run manually on dev for debugging, it MUST be started with `php artisan horizon --environment=local` (never bare `php artisan horizon`, which silently deploys zero supervisors). This convention is recorded in two places: (1) `config/horizon.php`'s own comment block immediately before the `defaults` array (lines 157–169, documenting the `environments` array at line 301), and (2) `docs/operations/examples/docker-compose.dev-stg.yml`'s comment on the `dev-worker` service (lines 83–87). No persistent `development` Horizon block is added — dev and beta share a live Redis keyspace by explicit accepted decision (ADR-0035 item 12), so a real persistent `development` block would start supervisors that consume beta's live queues too. This documented-convention path satisfies this box's own prior text's acceptance criterion ("either a `development`/`dev*` environment block, or a documented convention of always passing `--environment=local` on dev").
- [x] Transactional outbox loss/duplicate/replay tests pass. **Corrected (24 Aug 2026) — the box's prior text cited only the duplicate half's overlapping-transaction proof; real coverage for all 3 named properties already existed elsewhere in the same suite, just uncited.** **Loss**: `tests/Feature/Outbox/OutboxRecoveryTest.php::test_a_committed_event_survives_an_unclaimed_gap_and_publishes_once_the_publisher_runs` proves a committed event is never lost even after a delay before the publisher runs — the row sits committed and unclaimed, then a later publisher run dispatches it exactly once with the correct envelope. **Duplicate**: `tests/Feature/Outbox/OutboxPublisherClaimTest.php::test_claim_excludes_an_already_dispatched_row` (a row already marked `dispatched_at` is never re-claimed) and `::test_two_sequential_publisher_runs_never_double_claim_the_same_row`, plus `tests/Feature/Outbox/OutboxPublisherClaimTwoConnectionTest.php::test_a_second_connections_concurrent_claim_never_overlaps_the_firsts_in_flight_claim` (real overlapping-transaction proof that `SELECT ... FOR UPDATE SKIP LOCKED` genuinely excludes a row a still-open, uncommitted second connection has claimed, not merely an already-committed one). **Replay**: `tests/Feature/Outbox/OutboxRecoveryTest.php::test_a_second_publisher_run_does_not_reclaim_an_already_dispatched_row` — running the publisher command a second time (a real replay of the same operation) does not re-dispatch an already-handled row. All 4 tests re-run directly this pass against real Postgres 18 + Redis 8.2 (disposable containers, pinned `ghcr.io/andrianm28/makam-app@sha256:fd978e4cd370...` image, `vendor/bin/phpunit tests/Feature/Outbox/OutboxRecoveryTest.php tests/Feature/Outbox/OutboxPublisherClaimTest.php tests/Feature/Outbox/OutboxPublisherClaimTwoConnectionTest.php`) → `OK (9 tests, 28 assertions)`, all 3 files' full suites green, not just the 4 named tests in isolation.
- [ ] FIN-DEC decisions required by the activated money path are approved. — Real, enumerated business-decision gate, not a test gap: `docs/domain/financial-ledger-and-settlement.md` names all 8 decisions explicitly — `FIN-DEC-01` through `-07` are `TBD`, `FIN-DEC-08` is `GATED` — none approved. `.kiro/specs/platform-financial-ledger/tasks.md` and `.kiro/specs/platform-payment-adapter/tasks.md` both confirm this is still the live, current state (unchanged even after ADR-0033's SumoPod sandbox choice). This needs Business/Legal/Finance sign-off, not engineering.
- [x] Balanced journal, refund/payable/payout, and reconciliation tests pass for enabled features. — Balance is enforced at the database level, not just the app: a real PostgreSQL `assert_balanced_batch` constraint trigger, proven both directions — `tests/Feature/FinancialLedger/JournalSchemaTest.php::test_balanced_entries_inserted_as_one_statement_group_are_accepted` and `::test_unbalanced_entries_are_rejected_by_the_postgres_balance_trigger`; `JournalPostTest.php::test_an_unbalanced_batch_is_rejected_by_the_database_not_the_application` deliberately does not re-implement the balance rule at the app layer, matching this codebase's "structural check over app-level reimplementation" convention. Refund/reversal: `tests/Feature/Payment/ReversalServiceTest.php` + `RecordPaymentReversalRouteTest.php`. Payout (manual-only, matching `G-PAYOUT-01`'s closed state): `ManualPayoutTest.php` + `NoAutomatedPayoutPathTest.php` + `VendorPayableAssessmentTest.php`. Reconciliation: `RunReconciliationTest.php` + `ResolveReconciliationExceptionTest.php`. All scoped to what's actually enabled (manual payment path; online stays closed).
- [ ] Managed PostgreSQL with PITR configured. — **Split from the former compound box below (24 Aug 2026)**, because this half's literal claim can never be satisfied and checking the compound box would misstate that: `ADR-0027`'s "Production graduation — single-host decision" section (23 Aug 2026) decided production runs self-managed PostgreSQL 18 on the shared `yiemvm` host, not a managed provider, superseding `ADR-0021`'s original managed-PostgreSQL/PITR decision (see that ADR's own 23 Aug 2026 status update). No PITR exists or will exist under this decision — it is a named, accepted production risk in `ADR-0027`'s own Negative-consequences list, not an oversight. Left unchecked permanently under the current architecture, not pending further engineering work.
- [x] Self-managed backup/restore evidence is current (`ADR-0035` item 2 / `database-backup-and-recovery.md` §9 strategy). — **Real evidence produced 24 Aug 2026**, closing the second half of this box's former compound claim per §4's rule ("a backup is not considered valid until restored"). A real `age`-encrypted `pg_dump -Fc` of beta's live `makam_beta` database (beta being the closest existing instance to "production" under the single-host decision; no separate production database exists yet) was taken directly on `yiemvm` via `docker exec makam-nonprod-postgres-1 pg_dump -U makam_beta_user -d makam_beta -Fc | age --recipient <rehearsal-only recipient> --output ...`, matching `ADR-0035` item 2's and `docs/operations/examples/backup-staging.sh`'s already-approved `age` mechanism (not a different, invented one) — encrypted dump 420,750 bytes (420,454 bytes decrypted), captured at `2026-08-24T08:58:48Z`. Decrypted and restored (`pg_restore --no-owner --no-privileges`, exit 0) into a disposable scratch PostgreSQL 18 container (`rg2-restore-scratch`) — never beta, never a container serving real traffic. The restore was verified real, not assumed from the exit code: row counts on 2 real tables matched exactly between beta live and the scratch restore at `2026-08-24T08:59:22Z` (`feature_gates` 17=17, `users` 2=2). The scratch container was destroyed (`docker rm -f rg2-restore-scratch`, confirmed absent from `docker ps -a` after) and every dump/keypair file was deleted individually immediately after verification (`rm -rf` on the scratch directory was itself blocked by this session's harness guard against destructive commands, so cleanup used per-file `rm -f` instead, then `rmdir`) — nothing beta-derived remains on this host outside `makam-nonprod-postgres-1` itself. This is the first real, tested restore under the real strategy; it proves the mechanism works end-to-end but does not establish an automated 4–6 hourly cadence — no cron/systemd-timer exists yet, this was one manual rehearsal run. **Coverage against `database-backup-and-recovery.md` §4/§5's own evidence bar, stated precisely rather than left implied by the checkmark**: of §4's evidence-record fields, source backup, restore target, and duration were captured (dump at `08:58:48Z`, restore/verification confirmed by `08:59:22Z`), plus 2 real table row counts — but no formal sign-off record was produced. Of §5's 7-step restore-validation checklist, only step 3 (row counts) was exercised, and only on 2 tables; steps 1 (database version/extension validation), 2 (migration-state verification), the critical-foreign-key half of step 3, 4 (financial balanced-batch invariant and unique-key checks), 5 (duplicate active-reservation/renewal-period/reminder-window/billing-cycle checks), 6 (authentication/booking/payment-reference/renewal/file-reference smoke tests), and 7 (queue/outbox replay behavior) were **not** exercised by this rehearsal. The checkmark reflects that a real backup-and-restore round trip now exists for the first time — it does not mean §4/§5's full evidence bar was met.
- [ ] CI/CD immutable build, expand/contract migration, smoke test, and rollback rehearsal pass. — **A 4-part compound claim; 3 of 4 are real, the rollback-rehearsal sub-claim is real but materially narrower than this box's own wording and `ci-cd-and-release.md` §7's definition, so the box stays unchecked (24 Aug 2026 update).** Immutable build is real ("Build and push image" job builds/pushes a digest-pinned image with a generated SBOM). Smoke test is real ("Browser + a11y smoke test (Playwright)" job runs on every push). Expand/contract migration discipline is followed throughout this session's own work (e.g. the MFA-removal drop migration added forward-only, never editing the original create migrations). Rollback is now genuinely REHEARSED, not just documented: against the live `dev-web` service on `yiemvm`, using `docs/operations/runbooks/rollback-deploy.md` Step 3's exact `APP_IMAGE`/digest-pinning mechanism (the same mechanism that deployed PR #141's preview build to dev earlier the same day). `dev-web` started pinned to `sha256:c23eefa419b29809284d6a9d3b62e4eaf62a9f6faab6fbcb69652d471f969512` (PR #141 brand-visual-refresh preview) — repinned back to an older, real, locally-cached digest `sha256:f06418357a37fd75e5c4f31c133071c5e8bf4fcb1ca9645fd6047176824d5b28` (the prior real dev deploy, trunk `7fb71ee`), redeployed via `docker compose up -d dev-web`, and confirmed serving for real: `GET /up` → `200`, and `GET /` → `200` with a genuine rendered homepage (59,610-byte body, real `<title>Makam.co.id - Pemesanan dan Layanan Pemakaman</title>`), not just the health endpoint. Then repinned forward again to the original `sha256:c23eefa419b2...` digest, redeployed, and reconfirmed `200` on both `/up` and `/`, with the running container's image digest verified to match the starting digest exactly — dev ended in the identical state it started in. This is real, first-time evidence that dev's digest-pinning/repin mechanism works end-to-end, and it moves `docs/planning/sprint-plan.md`'s "rollback never rehearsed" tracking forward (Appendix B updated) — but it is not the full rehearsal this box's own wording and `docs/planning/sprint-plan.md` claim. **Scope, stated precisely rather than left implicit**: what was rehearsed is exactly one of `ci-cd-and-release.md` §7's seven rollback actions — "3. Roll application artifact back" (the digest repin). The other six were **not** exercised by this rehearsal: closing an affected feature/payment gate, stopping unsafe consumers while preserving durable events, a schema migration round-trip proving forward-compatibility under an actual rollback (§7 item 4 — distinct from this pass's general expand/contract *discipline*, which is a different, weaker claim than a rehearsed round-trip), reprocessing outbox/queue after idempotency review, reconciling payments/provider events received during an incident, and recording the incident itself. `docs/planning/sprint-plan.md`'s own **G9** gate (`S5-T6`, "Rollback rehearsal, expand/contract") is marked **HUMAN** precisely because a rollback rehearsal is meant to carry a human sign-off — an agent mechanically repinning and confirming `200` responses does not satisfy that gate, and `sprint-plan.md`'s own `S5-T6` Definition-of-Done line ("Rollback rehearsal executed with recorded outcome") is still correctly unchecked for this reason. **Scope of environment**: this proves the digest-repin mechanism works on the one live environment it's safe to rehearse against this way — `beta` (serves real customer traffic) was deliberately not touched by this specific rehearsal, though per §I's dev/beta isolation finding above, dev and beta already share a Redis queue keyspace regardless of which environment a rollback drill targets; a formal `production` environment does not exist yet under `ADR-0027`'s single-host decision.
- [ ] Pulse, error tracking, uptime, DB/Redis metrics, and correlation IDs are configured and access-controlled. — **Superseded (24 Aug 2026, this task)**: the prior verdict ("neither package appears in `composer.json`'s dependencies") is now stale — that was a `composer.lock` gap (PR #162, merged 24 Aug 2026, regenerated the lockfile with both `laravel/pulse` v1.8.0 and `sentry/sentry-laravel` v4.27.0 for real; confirmed again here via `grep -A2 '"name": "laravel/pulse"' composer.lock` / same for `sentry/sentry-laravel`). A 5-part compound claim; still unchecked overall, but materially more of it is now real and directly tested, not merely read:
  - **Pulse dashboard + access control — real, tested.** Verified against the actual pushed CI image for this worktree's exact base commit (`ghcr.io/andrianm28/makam-app@sha256:9522f4d738cee1f8945a0ac20e00cee53163b8fcd2923feea5c168b1970c73bf`, "Build and push image" run 32742564607, headSha `0c856f4`, built 15:09 UTC — after PR #162's 14:45 UTC merge; confirmed by pulling it and finding real `vendor/laravel/pulse`/`vendor/sentry/sentry-laravel` source inside, not stubs). That image ships `--no-dev` (no `phpunit`), so a `composer install` (dev deps only, matching CI's own `composer install --no-interaction --prefer-dist --no-progress` step) was run inside a disposable container on top of the image's already-installed `--no-dev` vendor tree, against this worktree's own unmodified `composer.lock` — not a fresh dependency resolution. Pulse ships no migrations by default; `php artisan vendor:publish --tag=pulse-migrations` was run for real (none existed before), producing `database/migrations/2026_08_24_153303_create_pulse_tables.php`, confirmed **byte-identical** to the package's own `vendor/laravel/pulse/database/migrations` source via `diff`, then committed. `php artisan migrate --force` against a disposable `postgres:18` container created the `pulse_values`/`pulse_entries`/`pulse_aggregates` tables for real. A new test, `tests/Feature/Observability/PulseDashboardAccessTest.php` (6 tests, 6 assertions, run via `vendor/bin/phpunit` inside the container against that Postgres + a disposable `redis:8.2-alpine`), asserts both the `viewPulse` Gate directly (denies a guest, denies an authenticated actor without a panel role, allows an admin) and the real `/pulse` HTTP route (403 for guest, 403 for a roleless authenticated user, 200 for an admin) — all passed. This is not a redundant check: `Laravel\Pulse\PulseServiceProvider::registerAuthorization()` registers its own package-default `viewPulse` gate (`fn ($user) => $app->environment('local')`) via `callAfterResolving`, and `ObservabilityServiceProvider` is the last provider in `bootstrap/providers.php`, booting after Pulse's own provider — the admin-allowed assertion passing under `APP_ENV=testing` (where Pulse's own default would deny everyone, admin included) is live proof this app's `Gate::define('viewPulse', ...)` — reusing `AdminPanelAccessPolicy`/`IdentityAccessAdapter::resolveActorContext()` — is the one actually being consulted, not Pulse's package default.
  - **Error tracking (Sentry) — package and scrubbing code real and load cleanly; no real DSN configured anywhere, so no events are actually being sent.** `SentryEventScrubber::scrub()` (read in full) genuinely redacts 16-digit NIK/KK patterns and signed DocumentVault URL query strings from the event message and tags the correlation ID + image digest — not just documented in a comment. `config/sentry.php`'s `send_default_pii` is `false`. `php artisan config:cache` (the specific, previously-documented failure mode — a closure-based config value cannot survive `var_export()`) succeeds cleanly against the real installed package under `APP_ENV=production`. `php artisan tinker --execute="dd(config('sentry.dsn'), config('sentry.before_send'));"` returned `dsn => null`, `before_send => ["App\Platform\Observability\SentryEventScrubber", "scrub"]` — the callable wiring is real; the `null` DSN is expected and stated plainly: no `SENTRY_LARAVEL_DSN` is configured anywhere (dev, staging, or this task), so Sentry is currently a no-op client. Standing up a real Sentry project + DSN is an account-provisioning step, out of engineering-task scope.
  - **Uptime monitoring — still genuinely absent.** No UptimeRobot/Better Stack or equivalent configured anywhere; needs a real external account. Explicitly left open, unchanged from the prior pass.
  - **DB/Redis metrics — partial, stated precisely rather than claimed whole.** `config/pulse.php`'s default recorders (`SlowQueries`, `Queues`, `CacheInteractions`, `SlowJobs`, `SlowRequests`, all `enabled => true`) cover query/job/cache timing; the dashboard route returns 200 with these recorders enabled against the real `pulse_*` tables (same test as above). Pulse's stock dashboard view is expected to include the corresponding widgets (`pulse.servers`/`pulse.usage`/`pulse.queues`/`pulse.cache`/`pulse.slow-queries`), but this was **not independently asserted** in the test — only the route's success response was; `assertOk()` supports "the dashboard renders without a fatal error," not that these specific widgets render. There is no dedicated Redis memory/eviction-rate recorder and no PostgreSQL replication/bloat metrics anywhere in this codebase — "DB/Redis metrics" is not satisfied as a whole by Pulse's default recorder set.
  - **Correlation IDs — unchanged, already real and tested.** `app/Http/Middleware/AssignCorrelationId.php`, `app/Platform/Correlation/CorrelationId.php`, `tests/Feature/Correlation/AssignCorrelationIdTest.php` + `CarriesCorrelationIdJobTest.php` — re-run in this pass alongside the new Pulse test (`tests/Feature/Correlation` + `tests/Feature/Horizon` + `tests/Feature/Observability`, 20 tests, 41 assertions, all passing under the real installed packages) to confirm no regression from the composer.lock fix.

  **Regression check, not just the targeted tests**: the full suite (3,222 tests) was also run against this same disposable Postgres/Redis pair with real Pulse/Sentry installed. Only 2 pre-existing, unrelated failures remain (`DocumentValidatorTest`'s `imagecreatetruecolor()` calls — the `gd` extension is not compiled into this production runtime image, unrelated to observability); 6 further failures (`ProductCatalogueSeedTest`/`VendorProcessingStatusTest`) were confirmed to be a `.dockerignore`-driven artifact of this test image excluding `docs/` from the build context, not a real gap — copying `docs/` into the container made all 6 pass. Neither category is caused by or related to this task's change.

  1 of 5 named pieces (correlation IDs) was already real; Pulse dashboard access control is now real and directly tested for the first time; Sentry's code path is real but inert (no DSN); DB/Redis metrics are partially real (Pulse's own recorders) but explicitly not the whole claim; uptime monitoring remains fully open. Box stays unchecked — uptime monitoring and a real Sentry DSN are both still genuinely missing and out of this task's scope (external account provisioning) — but materially more of the compound claim is now evidenced than before this task.
- [ ] Upload quarantine and malware-scanner fail-closed behavior pass. — **Corrected (23 Aug 2026)**: the prior "inconclusive" verdict was wrong about the download-gating half — direct code read confirms `App\Platform\DocumentVault\Actions\DownloadDocument::download()` (`app/Platform/DocumentVault/Actions/DownloadDocument.php:44-50`) genuinely checks `$document->state !== DocumentState::Accepted` and `$document->storage_prefix !== 'accepted'` before streaming, and `tests/Feature/DocumentVault/DownloadDocumentTest.php::test_cross_purpose_revoked_policy_actor_binding_and_nonaccepted_denials_are_404` already proves it: a document downgraded to `DocumentState::Scanning`/`storage_prefix='quarantine'` after a valid grant was issued still gets a 404 on download. This half of the claim — quarantine/non-accepted status genuinely blocks download — is real, tested, and passing. **Still genuinely open**: no real malware-scanner adapter exists (only `app/Platform/DocumentVault/Adapters/MockScanner.php` — no ClamAV or equivalent is implemented), which `AGENTS.md`/ADR-0027 explicitly keep off the shared dev/staging host ("always-on ClamAV... prohibited") — this is an infra/procurement gap, not a test-coverage gap, and stays open for that reason. **Live UAT sweep, 25 Aug 2026 — quarantine-download-denial re-confirmed, plus a real structural-reachability finding.** `DownloadDocumentTest::test_a_quarantined_object_cannot_be_served_by_the_local_public_url_contract` (the same test already cited above) and `ScanDocument::applyVerdict()`'s quarantine-verdict coverage in `ScanDocumentJobTest`/`UploadDocumentTest` were read and confirmed to match by method name/assertions this pass — not re-executed live (a PHP version mismatch on this host, 8.3 vs. the pinned ~8.5, blocked local execution today). Separately: `App\Domain\Memorial\Actions\CreateMemorialProfile` has zero callers anywhere in `app/` (confirmed directly, no admin Create page exists for memorial profiles), which means `UploadDocument`'s one live caller, `MemorialFamilyPage::uploadMedia()`, is itself unreachable from any live UI path today — the entire quarantine/malware-scan mechanism this box evaluates is real and tested at the code level but structurally unexercisable end-to-end in production right now, not a bug in the mechanism itself. The same root cause makes the admin Moderation Cases resource unreachable too (zero memorial profiles can exist, so zero moderation cases can exist) — noted here as shared context since Moderation Cases has no dedicated box of its own in this document.
- [x] Session revocation and password-based recent re-authentication pass. — Password re-authentication: `tests/Feature/IdentityAccess/Reauthentication/PasswordReauthenticationPageTest.php` (correct/wrong password, audit-on-failure with no credential in metadata, rate-limiting after repeated wrong attempts) + `PasswordReauthenticationSatisfiesRecentAuthenticationTest.php` (a stale actor genuinely regains access after a correct password, the satisfied event carries the right sensitive-action reason, a wrong password leaves the actor stale) — all real, passing tests against the post-MFA-removal `PasswordReauthentication` page. Session revocation: `tests/Feature/IdentityAccess/RecordActorSessionOnLogoutTest.php` proves logout marks the current session row revoked and does not touch a different user's session.
- [ ] Performance/capacity profiles pass or exceptions are formally accepted. — AC4 (grave fuzzy search <500ms at 100,000 records) is REALLY certified, not deferred: `php artisan bench:grave-search` measured real p50/p95/p99 against a real 100,000-row dataset (`php artisan bench:generate-grave-dataset`) on real PostgreSQL 18 in CI — measured against the largest of 100 generated cemeteries (~1,000 records, matching the real cemetery-scoped shape of `GraveRegistryPublicQuery::search()` per `BenchGraveSearchCommand` docblock) — p50 7.02ms, p95 7.19ms, p99 7.34ms, verdict PASS against the 500ms target (CI run: https://github.com/andrianm28/makam-app/actions/runs/32566663553). These are the post-fix numbers: the dataset generator's per-row name/typo derivation was decorrelated from the cemetery-index modulus (a final-review finding — the original derivation produced at most 4 distinct `deceased_name` values per cemetery; the benchmarked cemetery now carries dozens of distinct names, a genuinely representative corpus rather than a degenerate one), so these figures supersede an earlier, now-corrected run (p50 7.36ms/p95 9.18ms/p99 10.18ms) that benchmarked the degenerate dataset shape. Profile A (normal launch) has real, CI-verified evidence at documented reduced scale: `tests/load/profile-a-normal-launch.js` (k6, 10 VUs/30s against homepage/cemetery-directory/FAQ) measured aggregate `http_req_duration` avg=839.63ms, p95=1.47s across 3 routes combined; real gating metrics `http_req_failed: rate<0.01` measured 0.00% (0 requests failed out of 360 real HTTP requests, all 200 responses) and `checks: rate>0.99` measured 100.00% (360/360 checks passed, `checks_succeeded` 100%) — both thresholds now genuinely gate the job (a final-review finding closed the earlier gap where a silent redirect regression could have passed `http_req_failed` alone); duration thresholds remain deliberately non-gating per Task 5's mid-plan ruling (documented in `tests/load/profile-a-normal-launch.js` header and `tests/load/README.md`) because the app here runs via `php artisan serve`, an explicitly non-production dev server, so duration cannot serve as production-capacity evidence. This is a genuine result at 1/5 of the documented 50-VU scale, not a simulation. Profiles B (150 VUs campaign/burst), C (10k-row import + concurrent critical webhook traffic), and D (concurrency invariants under sustained load), and full-scale Profile A/AC4 certification against real concurrent user traffic, remain NOT TESTED at their documented scale — `performance-and-capacity.md` §9 requires "an isolated time window or temporary environment" and load generation "from a separate machine" for these, which this plan's CI job satisfies only at the reduced scale it actually ran; the shared dev/staging host is explicitly not accepted as production-capacity evidence. Deferred to Phase 3 (production graduation) per the roadmap, not silently skipped — `tests/load/README.md` states this explicitly.


## I. Combined development/staging host acceptance

- [ ] Host is Ubuntu 22.04 LTS with current security updates, firewall, key-only SSH, and restricted access. — **Re-verified directly on the live host, 24 Aug 2026** (same session, follow-up pass). OS version: still **Ubuntu 24.04.4** (`cat /etc/os-release`), not 22.04 — the literal claim's OS-version half is false, unchanged from the prior pass. `ADR-0027` now carries a fuller correction: its 23 Aug 2026 "Host specification correction" section has a **24 Aug 2026 addendum**, and the one remaining body-text reference to the old OS (in its Context section) is now annotated in place rather than left silent — see that ADR for both. Security updates: **the 5 packages this box previously flagged as pending were applied this pass** — `console-setup`, `console-setup-linux`, `keyboard-configuration`, `open-vm-tools`, `snapd` (`apt-get upgrade -y` against those 5 names, after confirming via `systemctl status` that neither `open-vm-tools` nor `snapd` was a running network service with active connections — both showed `inactive (dead)` beforehand, so the upgrade-triggered restart carried no disruption risk). `apt list --upgradable` after the upgrade shows **none of the 5 still pending**, and `dpkg -l` confirms all 5 landed at their target versions (`console-setup`/`console-setup-linux`/`keyboard-configuration` → `1.226ubuntu1.1`, `open-vm-tools` → `2:13.0.10-0ubuntu0.24.04.1`, `snapd` → `2.76.3+ubuntu24.04`). This closes the "current security updates" half of the claim for the packages that were actually pending (none were security-critical kernel/openssl-class packages to begin with). Firewall: genuinely active (`ufw status`: default deny-incoming, allows only 22/80/443/4096 from anywhere, on both v4 and v6) — real, but "restricted access" is a separate, broader claim than "a firewall exists," and those 4 ports are open to the whole internet, not IP-allowlisted (consistent with ADR-0031's own prior finding that dev's access restriction was explicitly removed by user decision) — this gap is a real access-policy decision, not engineering work closeable by this task. SSH: genuinely key-only — `sshd_config.d/50-cloud-init.conf`, `60-cloudimg-settings.conf`, and `99-hardening.conf` all independently set `PasswordAuthentication no`, and `99-hardening.conf` sets `PermitRootLogin no` — this half of the claim remains confirmed true. Leaving unchecked: OS-version-vs-title drift and firewall-without-IP-restriction remain real, literal gaps against this box's own compound claim, but materially more of the claim is now evidenced than before this pass (all 5 previously-pending updates applied, not merely catalogued).
- [x] PHP 8.5/Laravel 13/PostgreSQL 18/Redis 8.2 come from pinned images, not host-default packages. — `composer.json` pins `"php": "~8.5.0"` and `"laravel/framework": "^13.0"`; CI's dedicated "Verify pinned runtime versions" job asserts PHP is `>=8.5.0,<8.6.0` and fails otherwise. The reference compose files (`docs/operations/examples/docker-compose.dev-stg.yml` and `compose.deployed-reference.yml`) pin `postgres:18` and `redis:8.2-alpine` as image tags, never host-default packages. The 17 Aug migration's own verification narrative confirms these are what's actually deployed (row-count/version checks passed on the new host).
- [ ] Development and staging have different APP keys, database users, Redis/Horizon prefixes, queues, cookies, storage, and provider credentials. — **Re-verified 24 Aug 2026 with real evidence, and the result is worse than the prior "unverifiable" verdict.** (1) Architectural separation is real: `grep -n "env_file:" /opt/makam/compose/compose.yml` confirms `dev-web` points at `.env.dev` while `beta-web`/`beta-worker`/`beta-scheduler` all point at `.env.beta` — distinct files. (2) Staging itself has never been deployed (`docs/operations/runbooks/deploy-stg-vhost.md`), so a literal dev-vs-**staging** value comparison remains impossible and is not something this pass can close — tracked separately as staging's own not-yet-deployed status, not a gap this box can fix. (3) As a real substitute, dev vs. **beta** (the one other environment actually running) was compared directly via `docker exec makam-nonprod-{dev,beta}-web-1 printenv <key> | sha256sum` — hash-only, no raw value ever printed or logged. **Real, important finding: most of the named fields are NOT isolated in the live beta deployment.** Of the fields the claim names, only the database identity differs (`DB_USERNAME`, `DB_PASSWORD`, `DB_DATABASE` = `makam_dev` vs `makam_beta` — genuinely distinct). Every other field checked came back with an **identical hash between dev and beta**: `APP_KEY`, `REDIS_PREFIX`, `QUEUE_CONNECTION`, `SESSION_COOKIE`, `FILESYSTEM_DISK`, and the provider credential pair `SUMODOP_SANDBOX_API_KEY`/`SUMODOP_SANDBOX_WEBHOOK_SECRET` (note: the app's real env-var name is `SUMODOP_SANDBOX_API_KEY`, not "SUMOPOD" as sometimes written) — all confirmed same-hash on real, non-empty values (checked byte length, not just hash, to rule out both being blank). Corroborating, non-secret evidence makes the mechanism clear: `dev-web` and `beta-web` share the *same* Redis container (`REDIS_HOST=redis` for both) and the *same* Postgres container, so per-environment isolation there depends entirely on prefixing/credentials — and `CACHE_PREFIX` (`makam_dev_cache` on **both**) and `SESSION_DOMAIN` (`dev.makam.co.id` on **both**) show beta is literally carrying dev's values, not merely similar ones. Separately, `config/horizon.php`'s own comment says `HORIZON_PREFIX` "must differ per environment via `HORIZON_PREFIX` in `.env.dev` vs `.env.stg`" — but `HORIZON_PREFIX` is not set in either container's resolved environment at all, so both fall back to the same `Str::slug(APP_NAME)_horizon:` value since `APP_NAME` is also identical (`Makam.co.id` in both). **Net effect: on the one environment pair that is actually running, isolation exists only for the database identity; Redis/cache keyspace, session cookie/domain, queue connection, storage disk, and third-party sandbox credentials are all currently shared between dev and beta**, despite `env_file:` pointing at architecturally separate files — the separate files were evidently populated with mostly-identical values. Leaving unchecked, now for a stronger reason than before: this isn't merely unverified, it's actively disproven for beta and unverifiable for staging. Remediation (regenerating a distinct `APP_KEY`, `REDIS_PREFIX`/`CACHE_PREFIX`, session cookie/domain, `HORIZON_PREFIX`, and provider sandbox credentials for `.env.beta`, and applying the same discipline whenever `.env.stg` is first populated) is real engineering work, not a documentation gap. **Live consequence, confirmed directly (24 Aug 2026) — this is not hypothetical**: `beta-worker` is a real, currently-running container (`restart: unless-stopped`, `command: ["php","artisan","queue:work","--queue=critical,urgent,notifications,default",...]`, confirmed against the live `compose.yml`/`docker ps`), and it shares the identical `REDIS_PREFIX` and Redis connection with `dev-web` per the finding above. That means beta's always-on `queue:work` worker and dev share one Redis queue keyspace *right now*: a job dispatched from `dev-web` today is currently executable by `beta-worker`, against beta's real database and real provider credentials — not a future risk contingent on some other action, but a standing condition of the current deployment. This is a real, live, security-/privacy-relevant finding and needs a human decision and durable tracking beyond this unchecked box — either a dated `ADR-0035` accepted-risk entry or a tracked issue, so it isn't only remembered as prose here. **Resolved by explicit user decision, 24 Aug 2026: this sharing is intentional, not a gap.** The product owner's own framing: "its oke its intended to promote dev to beta" — dev is meant to be promotable straight into beta without a credential-regeneration step in between, and the shared `APP_KEY`/`REDIS_PREFIX`/session/provider-sandbox-credential state is how that promotion path stays cheap. This does not erase the live consequence described above (dev-dispatched jobs remain executable by `beta-worker` against beta's real data today) — that consequence is real and understood, and the decision is to accept it, not to remediate it. Recorded formally as `ADR-0035` item 12 (see that ADR). Box stays unchecked because its literal claim (distinct APP keys/prefixes/credentials) remains false by design — this is a closed decision, not an open gap, so no further remediation work is expected here.
- [ ] No production data or credentials exist on the host. — **This box's own claim now inverts under `ADR-0027`'s "Production graduation — single-host decision" section (23 Aug 2026, reaffirmed by the user 24 Aug 2026): this host explicitly IS production now, so production data legitimately exists here.** `docs/operations/dev-staging-environment.md` §4's "Production data is prohibited" line (the source of this box's prior framing) is corrected in place, 25 Aug 2026, to record the same supersession. This box's real, current claim is narrower than its literal text now suggests: not "no production data exists" (false by design — beta's real customer bookings are production data now) but "dev stays isolated from staging/beta," which §I's own dev/beta box already covers (and which is itself an accepted-risk, not-fully-isolated state per `ADR-0035` item 12). Leaving unchecked because the box's literal wording is stale, not because a new gap appeared — same pattern already used for §I's "host not recorded as production capacity/PITR/HA evidence" box below.
- [ ] Staging normal Horizon pool is capped at two processes; development/batch workers run on demand. — **Corrected (23 Aug 2026, after PR #150; mechanism corrected again 24 Aug 2026, Task 7)**: `config/horizon.php` now exists (added by PR #150). Its `staging` environment block genuinely sets `supervisor-normal => ['minProcesses' => 1, 'maxProcesses' => 2]` with all 6 other baseline/batch/reports supervisors set to `['maxProcesses' => 0]` — **not** `false` as this box previously said. The `false` form was found, during §H's Horizon rehearsal, to crash `Laravel\Horizon\ProvisioningPlan`'s construction outright (see §H's Horizon box for the full root cause: `array_replace_recursive` collapses a nested default array to a bare scalar, and `SupervisorOptions::fromArray()` then reads a missing `connection` key); `['maxProcesses' => 0]` is the real, crash-free mechanism `ProvisioningPlan::deploy()` already uses to skip a supervisor. The literal "capped at two processes" half of this claim is true in the current, fixed, committed config — verified not just by code read but by a real, live `php artisan horizon --environment=staging` boot (24 Aug 2026, same disposable-container mechanism as §H's rehearsal, torn down afterward): started with zero errors and exactly one supervisor running, `supervisor-normal`, covering `redis:critical`, `redis:urgent`, `redis:notifications`, `redis:default` — the other 6 supervisors correctly did not spawn. `dev-worker` and `stg-batch-worker` continue to correctly use Compose `profiles:` (`["dev-worker"]`/`["batch"]`) so they only run on demand — that half was already true. Left unchecked: config-level verification is not the same as an operational rehearsal on the real live host (Horizon actually running against real Redis, actually staying at 2 processes under real load) — matching the same distinction §H's Horizon box already draws for its own remaining gap. **Confirmed directly, 24 Aug 2026**: there is currently no `stg-worker`/`stg-horizon`-shaped container running on the host at all (`docker ps -a` shows only `stg-placeholder`, plain nginx) — this box cannot be operationally rehearsed yet because there is no live staging Horizon process to observe, consistent with `deploy-stg-vhost.md`'s own "never been deployed" status, not a new gap.
- [ ] Remote staging backup and restore procedure passes. — Staging-scoped (this box lives under §I, "Combined development/staging host acceptance"); it does not cover production — see §H's "Self-managed backup/restore evidence is current" box above for the real, tested (though beta-scoped, not staging-scoped) backup/restore rehearsal evidence; that former compound "Managed PostgreSQL backup/PITR" box no longer exists under that title — it was split (24 Aug 2026) into §H's "Managed PostgreSQL with PITR configured" box (permanently unchecked; no PITR under `ADR-0027`) and the restore-evidence box just cited. This box's own title is now partly stale too: `docs/operations/backup-and-restore-runbook.md` was updated 23 Aug 2026 to record that OQ-4 is resolved, but resolved toward self-hosted storage, not toward any external/"remote" S3-compatible provider at all (`ADR-0027`'s single-host decision) — a truly remote destination was never going to happen once this decision landed. That resolves the ambiguity this box used to cite ("blocked on OQ-4... needs the user to pick a provider") but does not close the box: the runbook's own Status line still reads "Prepared, not executed... no backup exists, and no restore has been executed" — **still accurate for staging specifically** (`makam_stg` has never been backed up or restored; §H's box above rehearsed against `beta`'s database, a different instance), so this is not one of the stale claims being corrected here. `examples/backup-staging.sh` still targets `BACKUP_S3_*` variables and has not been rewritten for a self-hosted destination, no concrete self-hosted backup path/volume has been decided, and **no automated recurring backup cadence exists yet, for staging or otherwise** (softened from an absolute "no `age` keypair has been generated" — §H's rehearsal did generate one, but a one-off, rehearsal-only keypair that was deleted immediately after verification, not a persistent one for recurring use; a real, retained `age` keypair for staging specifically still does not exist). Genuine, still-open gap — but the real blocker now is a destination decision plus a script rewrite and actual execution, not a pending provider choice.
- [ ] Memory, swap, disk, OOM, PostgreSQL, Redis, queue, and container monitoring is active. — `docs/operations/observability-stack.md` §7 documents this exact target as a design goal, but it's aspiration, not built evidence: `laravel/pulse` is not even in `composer.json` (not installed, same absence §H's Pulse box already names), no Prometheus/Grafana/node_exporter reference exists anywhere in `docs/operations/`, and `SpineWatchdogCommand.php` (the one real monitoring command that exists) covers business-process health — outbox/notification/failed-job staleness — not host-level memory/disk/container metrics. **Confirmed directly on the live host, 24 Aug 2026**: no monitoring agent process or container is running at all (checked for Prometheus/Grafana/node_exporter/netdata/datadog by process list and container list — zero matches), and no swap is configured (`swapon --show` empty). Current resource state, for the record (not evidence of monitoring — just the live snapshot at check time): 31 GiB RAM with 27 GiB available, 28% disk used (27G/96G). Leaving unchecked — the absence is now directly confirmed, not inferred from documentation gaps alone.
- [x] Restricted staging upload remains fail-closed without a real scanner. — `app/Platform/DocumentVault/Adapters/MockScanner.php`'s own doc block states the guarantee directly ("an outage must never be reachable as CLEAN"), and real tests prove it: `MockScannerTest.php`'s `test_staging_rejects_an_explicit_mock_scanner_configuration` and `DocumentVaultConfigurationTest.php`'s `test_provider_fails_closed_when_a_provider_is_not_configured` assert staging specifically refuses to silently fall back to a mock/absent scanner, and `test_the_outage_switch_returns_error_and_never_falls_through_to_clean` proves a scanner outage never resolves to a false-clean verdict. All pass. (Scoped narrower than §H's still-open "upload quarantine and malware-scanner fail-closed" box, which is about whether scan status gates a real download, not this box's staging-configuration-refusal claim — the two are not redundant.)
- [ ] Dev/staging domains are access-restricted and `noindex`. — Mixed, deliberately-documented state, not an unknown gap: `dev.makam.co.id` LOST its access restriction by explicit user decision (ADR-0031, 25 Jul 2026), but its `X-Robots-Tag: noindex, nofollow, noarchive, nosnippet` header remains enforced (checked by `ci/verify-infra.sh` GATE I9). `stg.makam.co.id` is not deployed at all yet — `docs/operations/runbooks/deploy-stg-vhost.md`'s own Status line: "Prepared, not executed... blocked on DNS... has never been deployed" — so its planned basic-auth restriction is moot until it exists. Leaving unchecked: literally true for noindex, deliberately false for dev's access-restriction half, and not-yet-applicable for staging.
- [ ] The host is not recorded as production capacity/PITR/HA evidence. — **This box's own claim now inverts under `ADR-0027`'s "Production graduation — single-host decision" section (23 Aug 2026): this host explicitly IS production now**, not merely staging carrying production-like traffic — the same ADR's superseded item 8 states this directly ("this host IS now production, by explicit decision"). It carries production data, previously prohibited and now explicitly permitted by the same decision (superseded item 7), and it provides no PITR (a named, accepted production risk in the same ADR section, corrected in `docs/operations/database-backup-and-recovery.md` §9) and no HA (never built, unchanged). Unchecked because the box's literal claim is now false, not because a new gap appeared — the underlying facts the claim used to protect against (no PITR, no HA) are unchanged and remain real, accepted production risks; only the "not production" framing is gone. §H's separate Performance/capacity box still correctly states this host is not accepted as production-**capacity** (load-tested-at-scale) evidence — a narrower, different claim about load-testing scale, not about production status, and it remains true independently of this box's inversion.
