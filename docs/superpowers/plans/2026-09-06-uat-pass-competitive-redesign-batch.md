# UAT Pass — Competitive-Redesign Batch (PRs #226, #232–238)

**Status:** EXECUTED 2026-09-06. All sections below carry real evidence gathered against the live dev (`makam-nonprod-dev-web-1`, `https://dev.makam.co.id`) and beta/production (`makam-nonprod-beta-web-1`, `https://makam.co.id`) containers, both running commit `53626f59` / digest `sha256:149ac33766fbd1056afa2fc01c19ada139885348d0d46ac5113776d43569c56e` (confirmed via each container's own `org.opencontainers.image.revision` OCI label). No authenticated browser/Filament UI session was available for this pass (Claude in Chrome extension not connected); admin-panel-dependent items were verified by directly invoking the real Filament page classes, Schema classes, and domain Actions via `php artisan tinker` against the live containers — instantiating the exact same PHP objects a real HTTP request would, authenticated as a real admin/finance actor via `Auth::login()` (no credentials entered or handled). Every write performed during this pass was wrapped in `DB::beginTransaction()` / `DB::rollBack()` and confirmed rolled back — no state was left behind in either environment, and `G-MEM-01` was independently reconfirmed closed after the transaction rolled back.

**Scope:** The 8 PRs merged into `docs/design-system-and-planning` on 2026-09-06 and deployed the same day to dev (`http://<dev-host>:8081`) and beta/production (`https://makam.co.id`) at commit `53626f59`, image digest `sha256:149ac33766fbd1056afa2fc01c19ada139885348d0d46ac5113776d43569c56e`:

| PR | Feature |
|---|---|
| #226 | Pre-demo known-gap closure (service complaints admin flow, marketplace listing details, wizard polish) |
| #232 | Auth actor-copy on `/masuk` and `/daftar` |
| #233 | Marketplace product go-live photo gate |
| #234 | Homepage live plot-availability preview |
| #235 | Memorial visit check-in ("Catat kunjungan") |
| #236 | Booking wizard shows selected grave plot on Ringkasan Pesanan |
| #237 | SvelteKit rewrite feasibility analysis (docs only — **no UAT needed**, excluded below) |
| #238 | Recovered UAT hardening fixes (invoices, reports, admin crashes, pricing) |

**Out of scope:** re-testing anything NOT touched by this batch. `docs/testing/release-gates.md` remains the authority for the product's full historical scope; this document only re-verifies what changed today.

## Global constraints

- Every box is checked only against a real, currently-passing automated test (file + test name) **or** a real manual step performed against the actual live dev/beta URL and its actual observed result — never narrated without evidence, per `AGENTS.md` §Infrastructure-agent execution: "Never report PASS for a check that was not executed."
- Manual steps target **dev** first (`http://<dev-host>:8081`, no real customer traffic) and are re-run against **beta/production** (`https://makam.co.id`) only for the items marked "Also verify on beta" — beta is live customer-facing traffic, so its manual pass should be the minimum needed to confirm the same behavior, not a second full click-through.
- `G-MEM-01` (Memorial/QR, including check-in) is seeded **closed**. Every #235 item below is a **code-path verification against dev with the gate temporarily inspected, not a claim that the feature is visible to real users** — this batch does not open the gate, and this UAT pass must not imply it does.
- A partial pass gets its specific remaining gap named in the box's own line — never checked prematurely.

---

## 1. PR #232 — Auth actor-copy

- [x] **Automated coverage already in CI.** `tests/Feature/Livewire/Public/Auth/LoginPageTest.php::test_the_page_names_all_three_account_types_and_links_the_other_two_portals` and `RegisterPageTest.php::test_the_page_states_that_registering_here_creates_a_customer_account_only` — both passed in CI run for commit `8c2026e7` (PR #232).
- [x] **Manual — dev:** `curl https://dev.makam.co.id/masuk` → HTTP 200, body contains "Halaman ini untuk masuk sebagai Pelanggan. Vendor Jasa masuk di..." with real `<a href>`s to `/vendor/login` and `/operator/login`; both independently resolved 200.
- [x] **Manual — dev:** `curl https://dev.makam.co.id/daftar` → HTTP 200, body contains "Akun yang dibuat di halaman ini adalah akun Pelanggan."
- [x] **Also verify on beta:** `https://makam.co.id/vendor/login` and `/operator/login` both return 200; `/masuk` shows the identical actor-copy text on production.

## 2. PR #233 — Marketplace photo go-live gate

- [x] **Automated coverage already in CI.** 184 tests / 1026 assertions across `ProductCatalogueSeedTest.php`, `ProductResourceTest.php`, `MarketplaceIndexRouteTest.php`, `ProductDetailRouteTest.php` — passed in CI for commit `2f93a19b` (PR #233).
- [x] **Manual — dev, model-level guard (substitute for admin-panel click; no authenticated browser session available this pass):** via `tinker` inside a rolled-back transaction, attempted to save a real `FLOWER_BOARD` product with `photo_path = null` and `is_active = true` — confirmed it genuinely throws `App\Domain\Marketplace\Exceptions\ProductMustHavePhotoToActivateException`. Confirmed `is_active = false` with no photo saves fine (draft path unaffected).
- [x] **Manual — dev, public:** `curl https://dev.makam.co.id/marketplace` → 12 real `<img>` tags, 0 "Foto belum tersedia" placeholders — all current listings have real photos.
- [x] **⚠️ Known, flagged-not-fixed gap — confirmed STILL OPEN, not silently fixed:** `ls -la public/storage` run directly on both `makam-nonprod-dev-web-1` and `makam-nonprod-beta-web-1` — the symlink genuinely does not exist on either host. `storage:link` was deliberately NOT run during this pass (it's an explicit human hand-off point per the PR body, not something to silently resolve mid-UAT). Any fresh admin-uploaded photo will still 404 on both environments until a human runs it.

## 3. PR #234 — Homepage live plot-availability preview

- [x] **Automated coverage already in CI.** 22 tests / 102 assertions across `PlotAvailabilityPreviewTest.php`, `PlotAvailabilityPreviewNeverMutatesTest.php`, `HomePageRouteTest.php` — passed in CI for the final commit on PR #234 (`33f46e93`), including the whole-branch review's mutation-tested fixes (Pint violation, vacuous tier-guard test).
- [x] **Manual — dev and beta:** homepage loads normally on both, and shows **no visible plot-preview section** (`grep -c "plot-preview-heading"` → 0). **Correction to this document's own prior assumption:** this is NOT because no cemetery is `granular`-tier — one real cemetery (`tpu-jakarta-menteng`) already is (see next box). The section is absent because the default showcase config (`config('marketing.homepage_plot_preview_cemetery_slugs')` → `tpu-petamburan,tpu-karet-bivak`) names two placeholder slugs that don't match any real cemetery in today's data — a config/seed-data mismatch, not a code defect, and still an honest empty state (no crash, no fabricated data).
- [x] **Manual — dev only, DB-level:** `SELECT slug, plot_tracking_mode FROM cemeteries` on dev shows 9 cemeteries `aggregate` and **1 genuinely `granular`: `tpu-jakarta-menteng`** — a new finding this pass, contradicting the original spec's assumption that none existed yet. It has real data: 1 `CemeteryBlock` (`BLOK-A`), 6 `GravePlot` rows (2 `available`, 4 `reserved`), and 4 active `CemeteryPackage` rows.
- [x] **Manual — dev, real-data activation smoke test — done for real, not the optional/simulated version originally planned:** since `tpu-jakarta-menteng` already has real granular data, no throwaway provisioning was needed. Via `tinker`, overrode `config(['marketing.homepage_plot_preview_cemetery_slugs' => ['tpu-jakarta-menteng']])` in-process (no file/DB change) and called the real `PlotAvailabilityPreview` component's `render()` directly: output HTML (7615 bytes) genuinely contains `plot-preview-heading`, `BLOK-A`, and `TPU Jakarta Menteng` — the full populated-rendering path (array access syntax in the Blade view, `StatusIntent` plot-state mapping) works correctly end-to-end against real data, not just the previously-tested empty-state path.
- [x] **Regression check — this was the one that broke CI (k6 + Playwright jobs) before the cache fix:** hit the homepage twice in a row on dev — both loads returned 200, confirming the cache-miss-then-cache-hit scenario that used to throw `__PHP_Incomplete_Class` now succeeds. Independently reconfirmed in this pass (not just relying on the 2026-09-06 deployment smoke-test).

## 4. PR #235 — Memorial visit check-in ("Catat kunjungan")

**Gate is closed (`G-MEM-01`) — nothing here is visible to a real visitor today.** Every item below verifies the CODE is correct and ready, not that a real user can see it.

- [x] **Automated coverage already in CI.** 83 tests across the full Memorial domain + public/family Livewire + admin suites (final count after the whole-branch-review fix wave) — passed in CI for the final commit on PR #235 (`a534842e`), including the reuse-of-`ResolveMemorialQr` proof, the rate-limit bucket-isolation/decay proof, the family-dashboard note-never-renders proof, and the delete-guard extension.
- [x] **Manual — dev, gate temporarily opened in a throwaway test (DEV ONLY, not beta):** ran the full flow inside one `DB::beginTransaction()`/`rollBack()`: created a throwaway public+published `MemorialProfile` and a real `MemorialQrToken`, opened `G-MEM-01` via the real audited `GateActivationRecorder::record(..., 'open', ...)`, called `ResolveMemorialQr()` (succeeded, correct profile), then `LogMemorialVisitCheckIn()` (succeeded). Confirmed: (a) no exception on the success path; (b) a real row landed in `memorial_visit_checkins` with the correct `visitor_label`/`note`; (c) the family-dashboard query (`MemorialFamilyPage::render()`'s own `visitCheckIns()`/`visitCheckInCount`) returns the right count and `visitor_label`, and the Blade view (`family-page.blade.php`) never echoes `->note` anywhere in the file (static-confirmed); (d) the admin `VisitCheckInsRelationManager` table statically confirmed to include a `note` `TextColumn`. Re-closed the gate via the same recorder, then rolled back the whole transaction — `gate_activations` has **zero** rows for `G-MEM-01` (before and after), and `isOpen('G-MEM-01')` was reconfirmed `false` before the rollback even completed. Nothing was left open, no audit rows were left behind.
- [x] **⚠️ Two flagged, unresolved product/privacy items — confirmed still tracked, not silently forgotten:** (1) retention/deletion policy for `memorial_visit_checkins` rows is genuinely undecided (spec §0); (2) the family dashboard shows unmoderated `visitor_label` text with no removal path (final review finding #2, deliberately not auto-fixed). Neither was touched or resolved by this UAT pass.

## 5. PR #236 — Grave plot detail on Ringkasan Pesanan

- [x] **Automated coverage already in CI.** 3 new tests (Screen 2 with a held plot, Screen 2 with none, CONFIRMATION after draft-to-order conversion) plus the full 168-test booking suite — passed in CI for commit `ed323705` (PR #236).
- [x] **Manual — dev, full flow with a real held plot — correction to this document's own prior assumption:** PR #234's own UAT (Section 3 above) found real granular data DOES now exist (`tpu-jakarta-menteng`), so this was tested against real data rather than marked NOT TESTED. Method: no authenticated browser session was available, so this was driven at the domain-action level (the same actions the real Livewire component calls) rather than a literal click-through — inside a rolled-back transaction: `StartBookingDraft()` → `HoldPlotForDraft()` against the real available plot (`slot 005`, `tpu-jakarta-menteng`, `BLOK-A`) → confirmed the plot flipped `available → reserved` → `PlotReservation::activeForDraft($draft)` found the hold → invoked the real private `BookingWizard::plotDetailFor()` via reflection with that hold and got back the exact expected array (`cemetery_name: "TPU Jakarta Menteng", block_code: "BLOK-A", block_name: "Blok A", slot: "005"`) — this is the literal method the Blade view (`Petak: Blok {{ $selectedPlot['block_code'] }} ({{ $selectedPlot['block_name'] }}), Slot {{ $selectedPlot['slot'] }} — {{ $selectedPlot['cemetery_name'] }}`) consumes. Transaction rolled back — no state left behind.
- [x] **Manual — dev, aggregate-tier / no-hold booking (the common real path today):** confirmed via `tinker` that a fresh `StartBookingDraft()` with no plot hold correctly returns `null` from `PlotReservation::activeForDraft()`, and `plotDetailFor(null)` correctly returns `null` — meaning the "Petak:" line is correctly suppressed for the common (aggregate-tier or no-selection) case, with no regression.

## 6. PR #226 + #238 — Service complaints, reports, admin fixes, pricing correction

- [x] **Automated coverage already in CI.** PR #226: full existing suite green at merge. PR #238: 89 tests / 266 assertions across the 11 touched/new test files (report CSV exports on all 5 tabs, Memorial Profile infolist, marketplace order crash, preneed cert check, invoice labels, pricing migration) — passed in CI for commit `1a156e5e`.
- [x] **Manual — dev, CSV export on all 5 report tabs (the crash PR #238 fixed) — no browser session available, so driven at the Livewire-component level instead of a literal click:** authenticated in `tinker` as real actors (`actor_role_assignments` rows — user id 2 has `admin`, user id 11 has both `admin` and `finance`) via `Auth::login()` (no credentials entered), then called `exportCsv()` directly on each of the 5 real panel classes. All 5 returned a real `Symfony\Component\HttpFoundation\StreamedResponse` with no `TypeError`: Orders and Vendor Performance and Renewal Period succeeded under plain `admin`; Receipts and Outgoing Payments correctly required the `finance`-scoped actor (a real, separate `LedgerReadAuthorizer` gate, unrelated to this fix — confirmed as an intentional 403 for the under-scoped actor, not a bug) and succeeded once authenticated as user 11.
- [x] **Manual — dev, Memorial Profile infolist — mounted the real Filament page class directly (no browser session):** `ViewMemorialProfile::mount()` against a real `MemorialProfile` record succeeded with no exception (previously this page had no infolist at all).
- [x] **Manual — dev, marketplace order infolist — exercised the real fix logic directly, not just page mount:** `MarketplaceOrderInfolist`'s private `customerName()`/`payableFor()` helpers invoked via reflection against every real `MarketplaceOrder` on dev: guest-checkout `customer_ref`s (session-token-shaped) correctly fall back to the raw ref (no crash); a simulated numeric ref (`'2'`) correctly resolves to the real user "Andrian Maulana"; a simulated fixture-shaped non-numeric ref (`'customer:1'`) correctly falls back to the raw string instead of crashing on the `bigint` column — this is the exact `SQLSTATE[22P02]` crash class the fix closes. `productName()` correctly resolved real non-variant items ("Karangan Bunga Papan", "Paket Bunga Tabur") via the new `items.listing.product.name` fallback path. **Not independently re-exercised:** the `payable_amount` null-safety branch specifically — every real order on dev already has a `vendor_payables` row, so the previously-crashing "no payable yet" case couldn't be hit against live data this pass; it remains covered only by the PR's own merged regression test.
- [x] **Manual — dev, service complaints — PARTIALLY verified, one real gap found:** dev has **zero** `WorkOrder` rows in its database, and `FileComplaint()` requires a real one — the full Investigate→Resolve/Dismiss domain-action walkthrough could not be driven against real data this pass (would have required fabricating a throwaway `WorkOrder`, which needs correctly constructing several upstream FK dependencies this pass didn't have time to do safely). What WAS confirmed: the real `ListServiceComplaints` Filament page mounts without crashing when authenticated as a real admin. The lifecycle itself (investigate/resolve/dismiss transitions, audit attribution, the `make_good_order_id` FK) remains verified only by PR #226's own SDD process (7 tasks, task-scoped review, Opus final review with live-schema/transaction-trace empirical checks) — not independently re-driven here. **Recommend a human walk one real complaint through the admin UI once real work-order data exists.**
- [x] **Manual — dev + beta, DB spot-check:** on **beta** — the environment where the 100x bug was originally found live — all 3 real fictional vendors (`Toko Bunga Contoh Melati Sejahtera`, `CV Batu Nisan Contoh Abadi Prima`, `UD Perawatan Makam Contoh Damai Nusantara`) now show corrected `price_minor` values matching the migration's documented intent (e.g. Rp 650.000 for the florist's listing, not the buggy Rp 6.500). On **dev**, none of these 3 exact vendor names exist at all (dev has an unrelated "Toko Bunga Contoh 1–5" demo dataset instead) — confirming the migration correctly no-ops there, exactly as its own doc block describes for an environment that never ran the buggy seed. Dev: **NOT APPLICABLE** (never seeded). Beta: **PASS** (real corrected values confirmed).
- [x] **Manual — dev, preneed:** via reflection, invoked `PreNeedInterestPage::certificateSubject()` with a real order's REFERENCE (`MK-2026-C6RPDKX7`) — correctly resolved the real order; with a fake reference — correctly returned `null` (honest empty state, no crash); with the same order's raw UUID — correctly still resolves via the `find()` path. All three cases confirmed against live dev data with no exception.
- [x] **Manual — dev, invoices:** `App\Domain\OrderWorkflow\ProductType::label()` confirmed to return real Indonesian text for all 6 real enum cases (e.g. `MARKETPLACE_PRODUCT_ORDER` → "Pesanan Produk Marketplace"), not a raw enum value.

---

## Sign-off

- [x] All boxes above are either checked with real evidence or marked NOT APPLICABLE with a named reason. **One item is checked with an explicit partial-verification note rather than a full pass:** Section 6's service-complaints box — dev has zero `WorkOrder` rows, so the Investigate→Resolve/Dismiss lifecycle could not be driven against real data this pass; only page-mount was confirmed live, and a human walkthrough is recommended once real work-order data exists.
- [x] Two NEW findings surfaced by this pass, neither a regression, both recorded here rather than silently absorbed:
  1. **PR #234's own spec assumption was stale even at merge time:** `tpu-jakarta-menteng` is already `plot_tracking_mode = granular` with real block/plot data, contradicting "no cemetery in real seed data is granular yet." This let Section 3's "optional, higher-effort" real-data check and all of Section 5 be verified against real data instead of being marked NOT TESTED or requiring throwaway provisioning.
  2. **The homepage showcase config's default slugs don't match any real cemetery.** `config('marketing.homepage_plot_preview_cemetery_slugs')` defaults to `tpu-petamburan,tpu-karet-bivak`, neither of which exists in real data. This is why the homepage still shows no plot-preview section despite a real granular cemetery existing — a config/seed-data mismatch worth a follow-up decision (point the config at `tpu-jakarta-menteng`, or leave as-is until more cemeteries go granular), not a code defect and not something this UAT pass changed unilaterally.
- [x] This pass's results do not require changes to `docs/testing/release-gates.md` — all 8 PRs are additive features/fixes rather than changes to the v0.5 MVP scope gates that document covers.

## Verification

Executed 2026-09-06. Every box above carries either a cited CI test, a real command/output captured against the live dev or beta container, or an explicit NOT APPLICABLE / partial-verification reason. No box was checked on the strength of the merged PR's own pre-merge test suite alone — each was independently re-exercised against live data this pass, except where explicitly noted otherwise (the `payable_amount` null branch, and the service-complaints lifecycle).
