# Superpowers Retrofit Backlog

**Version:** v0.1
**Date:** 09 Aug 2026
**Governing methodology:** `AGENTS.md` §Development methodology
**Origin plan:** `docs/superpowers/plans/2026-08-09-retrofit-cemetery-directory-capability.md` (the pilot retrofit that established the recipe this backlog sequences)

This is the durable, single record of (1) which already-shipped modules still need
a real Superpowers SDD retrofit, in what order, and (2) the specific gaps each
completed retrofit disposed rather than built inline. `AGENTS.md` §Documentation
forbids duplicating this in a second hand-maintained document — update this file,
do not create a rival one. Per-spec acceptance-criteria status stays in each Kiro
spec's own `tasks.md`; this file tracks the retrofit program across modules, not
per-AC detail.

---

## §1. Cross-module retrofit sequencing

Order and rationale as decided in the governing plan's "Sequencing" section.
Each module's retrofit gets its own plan doc at
`docs/superpowers/plans/<date>-retrofit-<module>.md`, its own worktree/branch/PR,
and the same recipe the pilot exercised: two-tier review
(task-scoped via `dispatching-parallel-agents`, then one whole-module review),
one bounded fix wave with regression tests, one scoped re-review, explicit
disposition of every self-flagged gap, and an append-correction on the module's
`sprint-plan.md` row.

| # | Module(s) | Status | Notes |
|---|---|---|---|
| 1 | `CemeteryDirectory` + `CemeteryCapability` | ✅ **Done** — pilot, 09 Aug 2026 | See §2 below for disposed gaps. PR # and CI run recorded on `sprint-plan.md`'s S4-T6 row once merged. |
| 2 | `IdentityAccess/Mfa` + `IdentityAccess/Reauthentication` | ✅ **Done** — 09 Aug 2026 | Wired real self-service MFA + `RequireRecentAuthentication`'s first real attachment. See §2 below for the full scope decision and disposition. PR [#7](https://github.com/andrianm28/makam-app/pull/7), CI run [`31299824997`](https://github.com/andrianm28/makam-app/actions/runs/31299824997), also recorded on `sprint-plan.md`'s S3-T2/S3-T3 rows. |
| 3 | `Faq` | ✅ **Done** — 09 Aug 2026 | Two-tier review across 5 slices (this module has both a write-lifecycle and a full admin CRUD surface, one more than the pilot). 1 Critical, 13 Important, 19 Minor found; 11 closed in-wave, 4 ledgered pending human ruling. See §2 below. PR [#9](https://github.com/andrianm28/makam-app/pull/9), CI run [`31302714214`](https://github.com/andrianm28/makam-app/actions/runs/31302714214), also recorded on `sprint-plan.md`'s S4-T2 row. |
| 4 | `GraveRegistry` | ✅ **Done** — 09 Aug 2026 | Two-tier review across 4 slices (schema, domain, ui-seam, tests). 0 Critical, 7 Important, 17 Minor found; 7 Important + 2 ride-alongs closed in-wave, including a real privacy gap (draft-cemetery records reachable as full open-mode results — fixed, human-reviewed before merge). AC4 (benchmark) stays ledgered, not closed. See §2 below. PR/CI recorded on `sprint-plan.md`'s S4-T7 row once merged. |
| 5 | `Renewal` | ✅ **Done** — 09 Aug 2026 | Reprioritized ahead of `Outbox`/infra on 09 Aug 2026 (grave renewal is core `makam` business logic, not peripheral; previously bundled with `ServiceCatalog`/`Marketplace` as one backlog line, split out as its own unit). Two-tier review across 3 task-scoped slices (domain, UI seam, scope-honesty) + 1 whole-module review. 0 Critical, 8 Important (7 fix-wave / 1 ledger-only), 19 Minor (2 ride-along fixes, 1 closed for free by Task 5's drafted wording, 16 parked verbatim). The 7 fix-wave Important items closed across Tasks 3 and 5; the 1 ledger-only item (journey terminus, `grave-search.blade.php`) is hard-excluded and Sprint 13-owned. See §2 below. PR [#15](https://github.com/andrianm28/makam-app/pull/15), CI run [`31316000732`](https://github.com/andrianm28/makam-app/actions/runs/31316000732) (all 7 jobs green on PostgreSQL 18), also recorded on `sprint-plan.md`'s S4-T7 row. |
| 6 | `Outbox` | ✅ **Done** — 09 Aug 2026 | Wired two real producers (`StartBookingDraft`, `SaveBookingDraftStep`) through the module's first ever real-routed-caller path, proving AC1 end-to-end against a real domain mutation. 0 Critical, 0 Important, 5 Minor found; all 5 Minor either closed in-wave (4 doc corrections) or ledgered (A-1, a defensible payload-field asymmetry). See §2 below. PR [#14](https://github.com/andrianm28/makam-app/pull/14), CI run [`31313063102`](https://github.com/andrianm28/makam-app/actions/runs/31313063102) (all 9 jobs green on PostgreSQL 18 — first run red: a new test hardcoded a non-existent user id and hit a real FK violation; fixed, re-run green), also recorded on `sprint-plan.md`'s S3-T11 row. |
| 7 | `ServiceCatalog`, `Marketplace` | `ServiceCatalog`: ✅ **Done** — 09 Aug 2026. `Marketplace`: ✅ **Done** — 09 Aug 2026 | Mid-risk, two separate back-to-back retrofit units (not one combined unit — each gets its own plan/worktree/PR). `Renewal` split out to item 5 — see that row's note. `ServiceCatalog` (09 Aug 2026): two-tier Superpowers SDD review (3 task-scoped reviewers — domain, schema, tests — plus 1 whole-module review) found 0 Critical, 26 Important, 21 Minor; 18 Important + 4 Minor ride-alongs closed in a bounded 4-round fix wave with non-vacuous regression tests, verified by a scoped re-review. 3 Important (F4b `cascadeOnDelete()` FK, F11 missing closed-list CHECK constraints, F12 missing partial unique index on the current price version) and the `PRICE_VERSION_RECORDED` `SensitiveActions` question ledgered pending human ruling — each is a migration against tables already deployed to `dev.makam.co.id` (gated by `AGENTS.md` §Infrastructure-agent execution) or an `app/Platform/**` decision. 3 doc corrections to `.kiro/specs/package-and-service-bundles/tasks.md:7/:8/:9` (Ruling B — AC5/AC6/AC8 overclaims corrected). Full disposition: §2 below. PR [#13](https://github.com/andrianm28/makam-app/pull/13), CI run [`31312252851`](https://github.com/andrianm28/makam-app/actions/runs/31312252851) (all 7 jobs green on PostgreSQL 18 — first run red: the new revise-copy test's key-order assertion was order-dependent; fixed, re-run green), also recorded on `sprint-plan.md`'s S4-T1 row. `Marketplace` (09 Aug 2026): two-tier Superpowers SDD review (3 task-scoped reviewers — domain, ui, schema-and-deadcode — plus 1 whole-module review) found 1 Critical, 9 Important, 9 Minor; all wave findings closed in a single bounded 6-commit fix wave (W-1…W-6) with non-vacuous regression tests, verified by a scoped re-review. W-1 (Critical) fixed invented vendor names + prices rendering bare on a public page at the presentation seam using the established fabricated-data marker. The dead `MarketplaceComingSoon` stub was **ledgered, not deleted** (one of three orphaned ComingSoon stubs). Full disposition: §2 below. PR [#16](https://github.com/andrianm28/makam-app/pull/16), CI run [`31317976326`](https://github.com/andrianm28/makam-app/actions/runs/31317976326) (all 8 checks green), also recorded on `sprint-plan.md`'s S4-T8 row |
| 8 | `Audit`, `FeatureGate`, `Correlation`, `Analytics`, `IdentityAccess/Scopes` | Not started | Lowest priority — real CI evidence already exists for these; retrofit here is lighter-weight (review + linking the real run IDs into `sprint-plan.md` rather than expecting many findings). |

---

## §2. Per-module disposed gaps

Populated as each module's retrofit completes. Each entry gives every
self-flagged gap an explicit disposition — closed with evidence, or ledgered
with a named owner and reason — rather than silent carry-forward.

### `cemetery-directory-and-availability` (CemeteryDirectory + CemeteryCapability), retrofitted 09 Aug 2026

Source: `.kiro/specs/cemetery-directory-and-availability/tasks.md`'s self-reported
gaps, plus AC10 (confirmed absent by the retrofit's domain-slice reviewer,
per the whole-module review's Ruling §12), reviewed and dispositioned by
`.superpowers/sdd/retrofit-cemetery-directory-capability/task-2-whole-module-review.md`.

| Gap | Disposition | Owner / reason |
|---|---|---|
| AC6/AC7 — optional plot source adapter interface | **Ledgered, not built.** | Owner: `plot-inventory-and-reservation` (unbuilt spec). `design.md`'s table-ownership rule already assigns `blocks`/`plot_units`/`plot_status_events` there; confirmed accurate by this retrofit's schema reviewer (`grep` confirms none of those tables exist anywhere in this repo). |
| AC8 — stale-source monitoring and fallback | **Ledgered, not built.** | Owner: `plot-inventory-and-reservation`. Depends on the AC6/AC7 adapter existing first; there is no freshness timestamp, monitor, or reservation-disabling path to build against yet. |
| AC9 — operator write-scoping + audit | **Ledgered, not built.** | Owner: `admin-operations`. No write-side capability Action exists (`app/Domain/CemeteryCapability/Actions/` contains only `ResolveCemeteryCapabilityProfile`, confirmed by directory listing) — deliberately out of this batch's and this retrofit's scope. |
| AC10 — admin manages cities/content/facilities/prices/capabilities without deployment | **Ledgered, not built. Confirmed absent, not just undocumented.** | Owner: `admin-operations`. `find app/Filament -type f` returns only `Admin/Resources/FaqArticles/**`; `grep -rn "Cemetery" app/Filament` returns nothing. |
| Directory/map query benchmarking (AC2/AC3/AC11) | **Ledgered, not built.** | No owner module yet — needs a benchmark harness this repo doesn't have. Genuinely `NOT TESTED`, not a hedge: no timing/benchmark assertion exists anywhere in the module's 10 test files. |
| 3 missing UI states (§6.6 duplicate/retry-safe, §6.8 success, §6.9 gated-fallback banner) | **Ledgered, correct deferral — not a fix-wave item.** | This module's own future UI work. The retrofit's Review scope explicitly forbids the fix wave from building new UI states; defensible today on a read-only browse surface with no mutation, no gate, and no success outcome. |
| 44px touch targets + focus ring (accessibility §7) | **Ledgered, blocked on tooling.** | No owner module — this repo has no Dusk/Playwright/Cypress harness at all, confirmed by this retrofit's tests-slice reviewer. A repository-level tooling gap, not specific to this module. |
| DB-level closed-list guard (CHECK constraint / partial unique index on `cemetery_capability_profiles`) | **Ledgered — needs a human ruling, not a fix-wave item.** | Found live during this retrofit (validation lives only in Eloquent `saving()` hooks; both query-builder writes and this module's own seed migrations bypass it). Held out of the bounded fix wave because it is a new migration against a table already deployed to `dev.makam.co.id` — `AGENTS.md` §Infrastructure-agent execution requires human review before production-affecting migration changes. |
| `<x-mk.filter-chip>` migration for the directory's filter chips | **Ledgered — needs a design ruling, not a fix-wave item.** | Found live during this retrofit (`index.blade.php` still hand-rolls the chip recipe `ba662d1` migrated everywhere else). Held out because every fix touches a shared single-writer primitive file or changes this page's interaction model (button→link chips) — a design decision, not a mechanical fix. |
| `phpstan.neon` `paths: [app]` excludes all Blade files from static analysis | **Ledgered — program-level, not this module's to fix.** | This is the structural reason `ba662d1`'s `$code => $label` bug reached production invisibly, and the whole-module review found a second, still-live instance (`$cards`, fixed in this retrofit) purely by inspection. Repository-wide config change; out of scope for a single-module retrofit. |

Full evidence trail: `.superpowers/sdd/retrofit-cemetery-directory-capability/` (worktree-local, git-ignored — task briefs, reports, whole-module review, re-review) and the retrofit's own commit history on branch `retrofit-cemetery-directory-capability`.

### `IdentityAccess/Mfa` + `IdentityAccess/Reauthentication`, retrofitted 09 Aug 2026

Unlike the pilot, this was real new feature work on built-but-unreachable security modules, not a review-and-fix pass on already-shipped code — it went through its own `superpowers:brainstorming` pass with the user before any implementation, per `AGENTS.md`'s mandatory-human-review rule for security/authorization-adjacent changes. Full design: `docs/superpowers/specs/2026-08-09-mfa-reauthentication-integration-design.md`. Full plan: `docs/superpowers/plans/2026-08-09-mfa-reauthentication-integration.md`. Executed via `subagent-driven-development`: 7 implementation tasks, each with its own task-scoped review (one fix round each on Tasks 1 and 5), plus a whole-branch synthesis before merge.

| Original row's requirement | Disposition | Owner / reason |
|---|---|---|
| "Mandatory MFA for all privileged roles" (`sprint-plan.md` S3-T2) | **Ledgered, not built — scope narrowed deliberately during the retrofit's `brainstorming` pass.** | No role model exists (`ActorContext::$roles` is still hardcoded `[]`, confirmed unchanged by this retrofit). Owner: whichever future spec resolves K1/K2 role mapping — not named yet. This retrofit ships voluntary self-service MFA instead, which is real progress toward the same end state without inventing a role model that belongs to a different spec. |
| Re-authentication on "the six sensitive action classes" (`sprint-plan.md` S3-T3, `docs/security/authentication-and-mfa.md` §5) | **Ledgered, not built — scope narrowed deliberately.** | None of the six action classes (payout/refund approval, bank-detail change, gate/payment-config change, specific-plot override, certificate issue/revoke/replace, bulk export) has a real controller anywhere in this repo, confirmed unchanged by this retrofit. Owner: each action's own future spec (`admin-operations` and others), which attaches `RequireRecentAuthentication` itself once it ships a real route — exactly as that middleware's own doc block already specified. This retrofit gives `RequireRecentAuthentication` its first real attachment on the one sensitive action that already exists today (disabling one's own MFA), proving the mechanism works end to end. |
| `/operator` and `/vendor` panel MFA/reauthentication | **Ledgered, not built.** | Neither panel exists yet in this repo (`find app/Providers/Filament` returns only `AdminPanelProvider.php`). Owner: whichever spec builds those panels. |

Full evidence trail: retrofit's own commit history on branch `mfa-reauthentication-integration` (merged, branch deleted per this repo's worktree-cleanup convention) and PR #7's description/comments. The plan's own ledger (`.superpowers/sdd/2026-08-09-mfa-reauthentication-integration/progress.md`) was worktree-local and removed with the worktree after merge, per `subagent-driven-development`'s own "Finish" step — git history is the durable record.

### `Faq`, retrofitted 09 Aug 2026

The first module retrofitted with a real write-side lifecycle and a full Filament admin CRUD resource, not just a read-only public surface — reviewed across 5 slices (schema, domain write-lifecycle, public UI, admin UI, tests) instead of the pilot's 4. Produced a materially larger finding set than the pilot: 1 Critical, 13 Important, 19 Minor. Full plan: `docs/superpowers/plans/2026-08-09-retrofit-faq.md`.

| Gap / finding | Disposition | Owner / reason |
|---|---|---|
| No `FaqArticlePolicy`; 4 custom Filament row actions (`publish`/`unpublish`/`moveUp`/`moveDown`) bypass authorization entirely, even after a policy is added | **Ledgered — needs human ruling, not fixed.** *(Critical)* | Adding a policy class or `->authorize()` call is an authorization change, gated by `AGENTS.md` §Infrastructure-agent execution. Not currently exploitable — the role vocabulary a policy would check doesn't exist yet (`ActorContext::$roles` always `[]`, already ledgered to S3-T2/T3). An in-wave characterization test pins the current gap behavior so it fails loudly the moment a policy lands. |
| No `(category_id, sort_order)` uniqueness invariant | **Split disposition.** *(Important)* | Application-level race (unlocked `CreateFaqArticleDraft` read) fixed in-wave with a row lock; public-read nondeterminism fixed in-wave with a deterministic `orderBy('id')` tiebreaker. The durable fix — a DB unique constraint — is ledgered pending human ruling: a migration against a table already deployed to `dev.makam.co.id`. |
| `cascadeOnDelete()` on `faq_article_versions.faq_article_id` would silently destroy append-only history | **Ledgered — needs human ruling.** *(Important)* | Latent (no delete path exists today), but changing to `restrictOnDelete()` is a migration against a deployed table. Blocking condition: must be resolved before the first `DeleteFaqArticle` action ships. |
| `UpdateFaqArticleContent` writes no version row when editing an already-published article | **Ledgered — needs design/human ruling.** *(Important)* | Reverses a deliberately recorded design decision from the original batch (versioning drafts, not every live edit) and would invalidate existing version-count test assertions. Doc blocks corrected in-wave to stop overclaiming completeness; the underlying gap stays open pending a design owner's decision. |
| AC7 (FAQ content must reflect active payment/Urgent gates) has no code-level enforcement | **Closed — formally documented as editorial review, for the first time.** | No prior decision existed to defer to (an earlier retrofit-plan draft misattributed this to a "design.md Open decisions" section that never existed — corrected during Task 1 review). Recorded now in `design.md`'s new "Open decisions" section with a trigger (any `paymentMode()`/`urgentMode()` change), cadence, and an owner role (name requires human fill-in — not invented by this retrofit). AC7's subject is free-text prose; no resolver can validate it, so a code guard was never an available option. |
| `tasks.md` claimed 9 of 10 UI states implemented and test-covered | **Closed — corrected to the true count.** | Independently re-verified: 8 of 10, not 9 — `FaqArticleDetail.php` has no error-handling path at all (only the unrelated §6.4 404), so §6.3 is unimplemented, not merely untested. Corrected in `tasks.md` alongside an honest per-state coverage list. |
| Accessibility "not verified with a real browser" | **Split disposition — markup layer closed, browser layer ledgered.** | This repo already asserts accessibility markup (aria attributes, `lang`, focus classes) with no browser in 3 other modules (Marketplace, Booking, FeatureGate) — Faq had zero such assertions despite its views emitting real aria attributes. Fix wave added the missing markup-layer tests. The browser/axe/screen-reader layer stays ledgered — no Dusk/Playwright/Cypress harness exists anywhere in this repo, same program-level gap the pilot retrofit recorded independently. |
| Responsive verification (§4.3) | **Ledgered, blocked on tooling.** | Same browser-harness gap as above. |
| `tasks.md`'s primitives table names 3 primitives the shipped code deliberately doesn't use | **Closed — documentation corrected.** | `design-system.md` §3.6a has since ratified one deviation (`<x-mk.filter-chip>`) as canonical; the other two (search input, CS button) are reasoned in-file. `tasks.md` corrected so a future implementer doesn't "fix" working code back to a superseded primitive. |
| `FaqArticleVersion`'s append-only history was documented but unenforced | **Closed in-wave** — three method overrides mirroring `AuditEvent`, evidence: `tests/Feature/Domain/Faq/FaqArticleLifecycleTest.php`. | The identical gap exists in `MfaChallenge` and `ReauthenticationEvent` (confirmed by grep) — ledgered as one repository-level backlog item, not fixed here (out of this module's scope). |

Full evidence trail: `.superpowers/sdd/retrofit-faq/` (worktree-local, git-ignored — 5 task-scoped reports, the whole-module review, the fix wave's own commits, the scoped re-review) and the retrofit's own commit history on branch `retrofit-faq`.

### `GraveRegistry`, retrofitted 09 Aug 2026

A pure read/search domain — `app/Domain/GraveRegistry/Actions/` is empty, so this reviewed across 4 slices (schema, domain, ui-seam, tests), no write-lifecycle or admin-CRUD slice. 0 Critical, 7 Important, 17 Minor. Full plan: `docs/superpowers/plans/2026-08-09-retrofit-graveregistry.md`.

| Gap / finding | Disposition | Owner / reason |
|---|---|---|
| `GraveRegistryPublicQuery::buildQuery()` had no cemetery-publication-status filter — a draft cemetery's records returned as full `open`-mode projections to any caller holding its UUID | **Closed in-wave, human-reviewed before merge.** *(Important — real privacy gap)* | Sole caller (`GraveSearch`) already checked, so nothing incorrect reached a visitor, but the precondition was unstated at the seam. Fixed by composing `Cemetery::scopePublished()` (the canonical definition), not duplicating it. Regression test proves the draft-cemetery fixture yields zero results. |
| `GraveSearch::mount()` promoted `#[Url]`-bound params to a real search with no validation; an unguarded `deathDate` reaches `whereDate()` — SQLite renders a wrong state, PostgreSQL throws (caught, `report()`'d — possible PII-in-error-tracker exposure, **not verified**, no `vendor/` in this repo) | **Split — validation closed in-wave, `report()`-scrubbing question ledgered.** *(Important)* | `mount()` now validates without throwing (populates the error bag directly, doesn't call `$this->validate()`), plus a domain-level date-shape guard as defense-in-depth. The whole-module review proved the PII question survives the validation fix (`report()` wraps any `PDOException`, not just the validation-triggerable kind) — ledgered as a **program-level** `report()`-scrubbing decision, not GraveRegistry-specific. |
| Migration doc block falsely claimed the GIN trigram index accelerates both the `LIKE` and `similarity()` halves of the search query | **Closed in-wave — doc-only.** | `similarity()` as a bare function call isn't an indexable operator; the `OR`'d disjunction can't use the index for either branch. Comment corrected; the real index/query-shape work stays under AC4 (below). |
| The "fabricated seed data" disclosure label was computed only from `openResults`, never shown for privacy-limited-only result sets | **Closed in-wave.** | New `GraveSearchOutcome::hasExampleData()` accessor checks both result sets; reachable fixture (`tps-jakarta-kemang`) used as the regression test. |
| The only `aria-live` announcement was a bare "0 data makam cocok" for no-result — violates §6.2's ban on bare no-data messages | **Split — announced-copy closed in-wave, DOM-timing ledgered.** | Copy fix is server-rendered and `assertSee`-testable. The live-region insertion-timing half needs a real browser and stays under the same program-level browser-harness gap Faq and the pilot already recorded. |
| §6.1 loading state had zero test assertions despite `tasks.md` claiming coverage | **Closed in-wave** — one `assertSee`, markup was already correct. | |
| `test_the_query_never_returns_an_eloquent_model` had a vacuous-pass risk (bare `foreach`, no non-emptiness guard) | **Closed in-wave** — `assertNotEmpty()` added before the loop. | |
| `GraveRecord::$fillable` listed `deceased_name_normalized` (fully derived) and `heir_contact_reference` (protected-class data, no write path) | **Closed in-wave (ride-along R1).** | Both removed from `$fillable`; the seed test that proved its point via mass assignment was converted to `forceFill()` so removal didn't make it vacuous. New test asserts neither key is mass-assignable. |
| Migration doc block overclaimed `access_mode`'s column default as reached through every write path | **Closed in-wave (ride-along R2) — doc-only, no behavior change.** | An Eloquent write omitting `access_mode` actually throws (fail-safe, correct); the column default is only reached via a raw `DB::table()->insert()`. Comment corrected to describe the real behavior. |
| **AC4** — search must return in < 500 ms at 100k records | **Ledgered, unchanged.** | No fixture, no measurement exists. Same disposition as before this retrofit — not closed by it. |
| **AC13** — bulk import (unbuilt) | **Ledgered, unchanged, plus two new design constraints recorded for its future implementer.** | The closed-list guard has exactly one enforcement point (`GraveRecord::booted()`) that a bulk insert/upsert would bypass; `grave_records` has no unique constraint an idempotent import's `ON CONFLICT` could target. |
| Gate-check locus (`G-DATA-01`) lives only in `GraveSearch.php`, not the query | **Confirmed as an accepted, reasoned tradeoff — not the same defect as the privacy gap above.** | Avoids duplicating `ModeResolver`'s mode↔gate-id pairing inside the query layer. Distinguished explicitly from the cemetery-publication-status gap, which had no such justification. |

Full evidence trail: `.superpowers/sdd/retrofit-graveregistry/` (worktree-local, git-ignored) and the retrofit's own commit history on branch `retrofit-graveregistry`.

### `Renewal`, retrofitted 09 Aug 2026

A read-only public surface — `/perpanjangan` (PUB-030) and its step-2 cemetery
list — reviewed across 3 task-scoped slices (domain, UI seam, scope-honesty)
plus one whole-module review. 0 Critical, 8 Important (7 fix-wave / 1
ledger-only), 19 Minor. The 7 fix-wave Important items closed across Tasks 3
(code/prose, with regression tests) and 5 (documentation append-corrections);
the 1 ledger-only Important item is hard-excluded. Full plan:
`docs/superpowers/plans/2026-08-09-retrofit-renewal.md`.

| Gap / finding | Disposition | Owner / reason |
|---|---|---|
| `goToStep` dead affordance — step 1's completed dot rendered a live `<button wire:click="goToStep(1)">` calling a method `RenewalStart` did not define (PUB-030 half) | **Closed in-wave (I1).** `RenewalStart::goToStep()` implemented (allow-list: step 1 → `resetCity()`, silent no-op otherwise); regression test `RenewalStartTest::test_the_completed_step_one_dot_calls_a_method_this_component_implements`. The PUB-031 half (`grave-search.blade.php:59-63`, same mechanism on `GraveSearch`) is ledgered — **L2** below. | Sprint 13 / `GraveRegistry` follow-up for the step-3 half (hard exclusion D3). |
| Fabricated `design-system.md` §6.9 quotation ("a closed gate never removes a required MVP step") cited at all four Renewal-owned sites | **Closed in-wave (I2).** Removed; corrected citations to `design-system.md` §9.2 MUST NOT 9, `requirements.md` AC1, and the `PaymentMode` row. The same fabricated quotation survives at `BookingWizardStep.php:20-24` plus a now-stale cross-reference — ledgered **L3**. | Booking module's own retrofit (not yet scheduled); different module, contended area. |
| `#[Url]`-bound `$city` unguarded on client-initiated updates — unknown code degraded the §6.2 empty state to "Belum ada TPU/TPS terdaftar di ." | **Closed in-wave (I3).** `normalizeCity()` private helper called from `mount()` and first statement of `render()`. Deliberately not `#[Locked]` (repo has no precedent for `#[Locked]`+`#[Url]`), not `updatedCity()` (unverifiable on host); render()-top normalization subsumes every update path. Regression test `test_a_client_supplied_unknown_city_is_discarded_on_update_not_only_on_mount` (last `assertDontSee` mandatory). | |
| Doc block falsely claimed the screen renders `<x-mk.gate-closed-banner>`; markup is `<x-mk.alert>` | **Closed in-wave (I4), prose-only.** Corrected to name `<x-mk.alert>`; banner-versus-page rationale kept. The primitive itself (2-of-2 bypass rate, both §6.9 banners hand-compose `<x-mk.alert>`) is ledgered **L4**. | `platform-feature-gate` (retrofit-backlog item 8) for the primitive question. |
| Hand-forked "Ganti kota" `<button>` while the file's own header claims every button uses the primitive (§9.2 MUST #2) | **Closed in-wave (I5).** Swapped to `<x-mk.button variant="link" wire:click="resetCity">`. Regression test `test_the_change_city_control_returns_to_step_one`. Primitive conformance verified by reading, not by a test — no CI gate enforces §9.2 MUST #2 (M19). | |
| `tasks.md:16` overclaim: `RenewalJourneyStepTest::test_only_the_first_three_steps_are_implemented_in_sprint_4` "pins that boundary in code" | **Closed (I6), documentation append-correction.** The test body is a single `assertSame(GRAVE_SEARCH, LAST_IMPLEMENTED)` comparing a constant to its own definition — it guards the declaration against one class of edit; it does not pin the boundary and is not evidence steps 4–6 are absent. Both `tasks.md:16` and `traceability-matrix.md:164` corrected append-only. | |
| `sprint-plan.md` S4-T7's Consumes column claims `audit` dependency; `feature-gate` also listed | **Audit unsubstantiated, closed as documentation correction (I7); `feature-gate` substantiated and standing.** `grep -rni "audit" app/Domain/Renewal app/Livewire/Public/Renewal resources/views/livewire/public/renewal tests/Unit/Domain/Renewal tests/Feature/Livewire/Public/Renewal` → exit 1, nothing (shipped surface is public, unauthenticated, read-only, no write path to audit). `feature-gate` → exit 0: `ModeResolver::graveSearchMode()` called server-side on both screens (`RenewalStart.php:186`, `GraveSearch.php:228`). Second append-correction on the S4-T7 row, byte-for-byte preserving PR #11's. | Same defect class already append-corrected on the S4-T6 row. |
| **L1** Journey terminus — step 3's success path (`grave-search.blade.php:359-371`) offers no next action | **Ledgered, Important, not fix-wave.** | Hard exclusion D3. Sprint 13 renewal work (AC6–AC9); interim copy is a product decision, not an agent's. |
| **L2** `goToStep` dead affordance, step-3 half (`grave-search.blade.php:59-63`) | **Ledgered, Important.** PUB-030's half is fixed (I1); PUB-031's is not. | `GraveRegistry` follow-up / whichever unit next opens that file. Hard exclusion D3. |
| **L3** Fabricated §6.9 quotation at `BookingWizardStep.php:20-24` + stale cross-reference | **Ledgered, Important.** Both halves named. | Booking module's own retrofit (not yet scheduled). |
| **L4** `<x-mk.gate-closed-banner>` 2-of-2 bypass (`start.blade.php`, `home-page.blade.php` both hand-compose `<x-mk.alert>`); likely undeclared `icon` prop | **Ledgered, Minor.** | `platform-feature-gate` (backlog item 8). Depends on Blade attribute-forwarding behaviour unverifiable without a real build. |
| **L5** `RenewalComingSoon` dead code — three stubs (shared `coming-soon.blade.php`), `BookingWizardComingSoon` unowned | **Ledgered, Minor.** | A single future PR paired with `MarketplaceComingSoon`; retirement is a product decision (D1). Removal touches `routes/web.php` and `BookingWizard.php`, both contended. |
| **L6** AC4 (< 500 ms at 100k records) — NOT TESTED | **Ledgered, re-stated not re-adjudicated.** | No owner module; load-harness decision is human/product. Belongs to the grave-search half of the shared S4-T7 row. |
| **L7** AC11 (duplicate-period guard) — NOT TESTED | **Ledgered.** | Sprint 13, with the `renewals` table. Cannot be closed before the data model exists. |
| **L8** Accessibility (§7) / CLS < 0.1 — NOT TESTED | **Ledgered, program-level.** | No browser harness anywhere in the repo (Dusk/Playwright/Cypress/axe confirmed absent); needs a programme-level harness decision. |
| **L9** `information-architecture.md:17-20` lists `/perpanjangan/permohonan/{renewalReference}` and `/perpanjangan/konfirmasi/{renewalReference}`, neither registered | **Ledgered, completeness only.** | File is headed "Route Contract" (target state); `sprint-plan.md:628` already records one such divergence for S4-T6. Owner: whoever owns the IA route contract. |
| M1–M22 (Minor findings, parked verbatim) | **Parked verbatim, not fixed**, except RA1/RA2 (ride-along present-tense false-statement corrections) and M17 (closed for free by I7's drafted wording). | Each carries its owner/reason in `task-2-whole-module-review.md` §"Minor findings parked verbatim"; full text preserved there and in the Task 3 ledger entry. |

Full evidence trail: `.superpowers/sdd/retrofit-renewal/` (worktree-local, git-ignored — 3 task-scoped reports, the whole-module review, fix-wave commits, scoped re-review) and the retrofit's own commit history on branch `retrofit-renewal`.
### `Outbox`, retrofitted 09 Aug 2026

Architecturally sound infrastructure (real `SELECT ... FOR UPDATE SKIP LOCKED` claim-and-retry, `Outbox::record()` write API, `OutboxQueueRouter`, `PayloadClassification` denylist) whose only real producer had never fired in any deployed environment — the retrofit wired its first ever real-routed-caller producers (`StartBookingDraft`, `SaveBookingDraftStep`, both called by `BookingWizard.php`), proving AC1 end-to-end against a real domain mutation for the first time. 0 Critical, 0 Important, 5 Minor — the smallest finding set of any module retrofitted so far, consistent with the module being infrastructure with an already-tested core. Full plan: `docs/superpowers/plans/2026-08-09-retrofit-outbox.md`.

| Gap / finding | Disposition | Owner / reason |
|---|---|---|
| AC1 ("same transaction as the state mutation") was proved only against `tests/Fixtures/OutboxFixtureAggregate`, never a real domain mutation | **Closed — real-producer proof added.** | `StartBookingDraft` and `SaveBookingDraftStep` now emit via `Outbox::record()` inside their pre-existing `DB::transaction()` closures. Evidence: `BookingDraftOutboxTest` (both commit and rollback directions) and `OutboxBookingDraftPublicationTest` (a real producer's row is claimed and `PublishOutboxEventJob` pushed). The fixture-based `OutboxTransactionTest` remains as the standalone-transaction proof, re-documented as complementary rather than a substitute. |
| No catalogued event name for a booking-draft lifecycle event; the catalogue's only booking row (`booking.draft_submitted.v2`) is a Step 9 submission event and Step 9 is unbuilt | **Ledgered — finding N-17.** *(the retrofit's disclosed gap 1)* | Owner: whoever owns `docs/contracts/event-catalog.md` next — either add the two provisional rows (`booking.draft_started.v1`, `booking.draft_step_saved.v1`) or rule that draft-lifecycle events do not belong in the catalogue. Same disclosure pattern as N-12's `feature_gate.state_changed.v1`. |
| Neither new event is "critical" in AC1's sense — the genuinely critical event is `booking.draft_submitted.v2` (money path) and it is unbuildable today | **Ledgered — the retrofit's disclosed gap 2.** | Owner: whichever spec builds Step 9 submission. This retrofit proves AC1's *mechanism* against a real mutation with a real caller; proving it on an actually-critical event belongs to that spec. |
| AC5's atomic-claim proof is sequential-only — this suite's `RefreshDatabase`-per-test wrapping cannot expose a genuinely separate database session | **Ledgered, unchanged.** | Harness limitation, documented in `OutboxPublisherClaimTest`'s own doc block. True cross-session `SKIP LOCKED` contention is not provable inside this harness; not a defect in the claim loop. |
| AC8 starvation prevention under load | **Ledgered, unchanged.** | Routing correctness is proved; starvation prevention needs Sprint 6's Horizon supervisor pools. |
| AC9 (10k-import load test), AC11 (bounded replay), AC12 (observability/alerting), permanent-failure escalation | **Ledgered, unchanged — out of scope per the execution plan and finding N-8.** | Sprint 6 scope. |
| Correlation propagation's provider/notification half | **Ledgered, unchanged.** | No provider/notification classes exist in this repo (grep-verified); the request→event→queue-job halves are now exercised (the retrofit closed the queue-job half). |
| A-1 (Minor): `actor_role` present in `booking.draft_started.v1`'s payload, absent in `booking.draft_step_saved.v1`'s | **Ledgered — defensible asymmetry, no change.** | Envelope `actor` is present-null by N-11's design; payloads are references-only; a consumer reads the draft itself for actor context. The retrofit deliberately did not alter the existing envelope's field set. |
| Request-boundary correlation coverage binds the context directly rather than exercising `AssignCorrelationId` middleware | **Ledgered — pre-existing, not made stale by this retrofit.** | `OutboxCorrelationTest` sets `app(CorrelationContext::class)->set()` directly. The middleware-to-outbox seam remains untested as one unit; recorded so a future test can close it without rediscovering it. |

Full evidence trail: `.superpowers/sdd/retrofit-outbox/` (worktree-local, git-ignored — task briefs, task reports, slice reviews, fix-wave commits, scoped re-review) and the retrofit's own commit history on branch `retrofit-outbox`.

### `package-and-service-bundles` (ServiceCatalog), retrofitted 09 Aug 2026

Source: `.kiro/specs/package-and-service-bundles/tasks.md`'s self-reported gaps (AC8's unbuilt
admin editor, AC3/AC4's quote-expansion half, the uncatalogued publish/revise event, the
discontinued-item hole) plus a two-tier Superpowers SDD review (3 task-scoped reviewers — domain,
schema, tests — plus 1 whole-module review) over AC1–AC8. Full plan:
`docs/superpowers/plans/2026-08-09-retrofit-servicecatalog.md`. After dedupe: 0 Critical, 26
Important, 21 Minor, 3 dropped as false positives (all three refuted with evidence — e.g. the
suspected unvalidated `item_type`/`fulfillment_owner` write is closed by `assertKnown()` at the
Eloquent write seam). Dispositions: 18 Important + 4 Minor ride-alongs closed in a bounded 4-round
fix wave; 3 Important + 1 financial question need human ruling; 3 doc corrections; 19 ledgered.

| Gap / finding | Disposition | Owner / reason |
|---|---|---|
| **AC8** — admin editor UI for the catalogue | **Ledgered, not built. Confirmed absent, not just undocumented — deliberate deferral, not a defect.** | Owner: `admin-operations`. `find app/Filament -type f` returns only `Admin/.gitkeep`, `Admin/Pages/Mfa*`, `Admin/Resources/FaqArticles/**`, `Operator/.gitkeep`, `Vendor/.gitkeep`; `grep -rn "ServiceCatalog\|ServicePackage" app/Filament resources/` returns nothing. `tasks.md:6` already tracks it honestly as `[ ]`/partial; `design.md` §"Not covered, deliberately" assigns it away. Blocking condition on the ledger row: the editor is the surface that will introduce the F1–F5 call patterns the fix wave closed — it must not bypass them. |
| **AC3/AC4** — quote *expansion* half | **Ledgered — correct boundary, not built here.** | Owner: `booking-and-order-orchestration`. `design.md` §"Consumption boundary" assigns it. This retrofit reviewed and proved the *snapshot/versioning* half (historical price stability + append-only immutability, both newly enforced/tested) but not the expansion half. |
| Un-catalogued publish/revise/discontinue event | **Ledgered — current state is correct, not a defect.** | Owner: `booking-and-order-orchestration`. `design.md`'s "no event catalogued" claim re-verified true (`docs/contracts/event-catalog.md` has zero ServiceCatalog rows). Emitting an uncatalogued event would violate `AGENTS.md` §Queue and event reliability (N-12's lesson). Blocking condition: `event-catalog.md` must be amended before any producer is built. |
| Discontinued item still attached to a published bundle | **Split three ways.** | Deletion half: **closed** — `restrictOnDelete()` on both edges (`2026_07_26_180300:87`, `2026_07_26_180500:59`). Deactivation vs the Booking seam (basic services): **closed in-wave** (F14) — an invariant test pins `is_active = true` for every `ServiceCode::BASIC_CODES` row, so deactivating a mandatory service fails CI loudly instead of silently disappearing from Step 4 while still being billed on Step 5. Deactivation vs an already-published version: **ledgered — needs a human ruling + a new AC** — AC1–AC8 are silent and every candidate fix is a product decision needing new state the schema can't express. Owner: `package-and-service-bundles` itself. Becomes urgent when `booking-and-order-orchestration` starts reading published versions. |
| **AC2** — Eloquent-only enforcement depth | **Application layer closed in-wave; structural layer ledgered.** | The fix wave closed every ordinary-Eloquent bypass (C-1..C-5 + C-6's doc half): in-memory status downgrade before `delete()`, re-pointing items off a published version, direct `create(['status' => 'published'])`, `$package->delete()` cascading through published versions, and unguarded `SubstitutionPolicy`/`EvidenceRequirement` rows. The structural layer — F4b `cascadeOnDelete()` FK, F11 missing CHECK constraints on 4 closed-list columns, F12 missing partial unique index on the current price version — is **ledgered pending human ruling**: each is a migration against tables already deployed to `dev.makam.co.id` (26 Jul 2026), gated by `AGENTS.md` §Infrastructure-agent execution, and F4b additionally reverses a deliberately-reasoned design decision (`2026_07_26_180200:46-51`). A C-7 characterization test pins the current gap behaviour so a future DB guard fails loudly rather than silently. |
| **F15** — catalogue money contract broken at the consuming seam (`(float) $priceVersion->amount` in `BookingDraftQuery`, then float `$total`) | **RULED 09 Aug 2026 (Wave 0c) — resolved by L4/L3 plans.** **Status 11 Aug 2026: resolved on `lane/l3-payment-adapter`; the defect is STILL PRESENT on `lane/l4-financial-ledger`. Not closed on the trunk until L3 merges.** | Owner: `platform-financial-ledger` (L4) / `platform-payment-adapter` (L3). **Ruling:** money is stored and carried as **integer minor units** (IDR × 100) everywhere from the ledger outward — never a float. F15's `(float)` cast at the Booking seam is replaced by integer minor-unit handling at the quote/total seam, before L3 derives any sandbox payment amount (`AGENTS.md` §Domain and financial invariants). Verification: no money is stored in any deployed DB today, so the contract is additive and conversion-free — no destructive migration. See `docs/superpowers/plans/2026-08-09-wave0-decisions.md` Task 6. **Verification 11 Aug 2026, stated in both directions because neither a bare RESOLVED nor a bare OPEN is true.** *Fixed on L3:* `origin/lane/l3-payment-adapter` rewrote `app/Domain/Booking/BookingDraftQuery.php` to integer minor units with an overflow-guarded `multiplyMinorUnits(int $unitPrice, int $quantity)` — the ruling is implemented there. *Not fixed on L4:* at `lane/l4-financial-ledger` `713442f` the original line survives verbatim at `app/Domain/Booking/BookingDraftQuery.php:86`. L4 never touched that file, so the two branches fast-forward cleanly and the fix arrives at merge with no conflict — but until that merge happens, any branch other than L3's still carries the float. Whoever merges must confirm L3's version of that file is the one that lands, and only then close this row. |
| **S-Q3** — `PRICE_VERSION_RECORDED` not in `SensitiveActions::ACTIONS` | **RULED 09 Aug 2026 (Wave 0c) — add it; executed by L4's plan. RESOLVED 11 Aug 2026 — verified present, on this branch.** | Owner: `platform-financial-ledger` (L4). **Ruling:** ADD `PRICE_VERSION_RECORDED` to `SensitiveActions::ACTIONS` — a financial change, human-approved here; executed by L4's plan. The "correctable by publishing a new version" argument is *not* a full answer (an issued quote holds the old snapshot), so the action becomes a sensitive, audit-recorded action. See `docs/superpowers/plans/2026-08-09-wave0-decisions.md` Task 6. **Verification 11 Aug 2026:** `PRICE_VERSION_RECORDED` is present in `SensitiveActions::ACTIONS` at `app/Platform/Audit/SensitiveActions.php:39`, and the string the code actually emits, `SERVICE_DEFINITION_PRICE_VERSION_RECORDED`, is listed beside it at `:40`. Both were listed deliberately after a review finding — the pair is belt-and-braces, not a duplicate to remove. Enforcement is at the signature rather than as a runtime throw: `app/Domain/ServiceCatalog/Actions/RecordServiceDefinitionPriceVersion.php:89` makes `$reason` a required parameter, so the guard runs before the transaction opens. |
| `docs/contracts/service-package-schema.md` drift (F25) | **Ledgered — out of this unit's scope.** | The plan's Review scope lists the file read-only; Task 5's doc scope excludes it. Blocking condition: must be reconciled before `booking-and-order-orchestration` builds quote expansion against it — four fields the shipped schema cannot store, and `fulfillment_owner: cemetery` would fail `FulfillmentOwner::assertKnown()` at runtime. |
| **F13** — `180700` seed `down()` would hit an FK violation on any DB with authored packages | **Ledgered, permanently — nothing for a human to decide.** | `AGENTS.md` §Database: rollback is already non-load-bearing; every candidate fix is wrong (deleting items first destroys authored data, making `down()` a no-op lies about reversibility); unobservable in CI (`migrate:fresh` never calls `down()`). Exact reproduction condition recorded in the review so it isn't re-discovered. |
| 17 Minor findings (M1–M21, excluding the in-wave ride-alongs M6/M10/M11/M14) | **Ledgered, not fixed.** | Verbatim dispositions in the whole-module review. Highlights: M3 (`service_packages.is_active` written but never read) — module's own future work; M5 (`priceVersions()` zero callers, kept deliberately) — revisit when `booking-and-order-orchestration` decides package-level prices exist; M16 (doc↔code catalogue direction unguarded by `ci/verify-docs.sh`) — program-level; M17 (`lockForUpdate()` silent no-op on SQLite) — program-level, owner `booking-and-order-orchestration`; M19 (shallow query class, `basicServices()`/`additionalServices()` have zero production callers) — design note, not a retrofit fix. |

Full evidence trail: `.superpowers/sdd/retrofit-servicecatalog/` (worktree-local, git-ignored — 3 task-scoped reports, the whole-module review, the fix wave's own commits, the scoped re-review) and the retrofit's own commit history on branch `retrofit-servicecatalog`.

### `funeral-marketplace-and-vendor-portal` (Marketplace browse skeleton), retrofitted 09 Aug 2026

Reviewed across 3 task-scoped slices (domain, ui, schema-and-deadcode) plus one whole-module review — this module has no write lifecycle, no admin CRUD surface, and no Actions (`.gitkeep` only), so the Faq/GraveRegistry 4–5 slice shapes do not apply. Full plan: `docs/superpowers/plans/2026-08-09-retrofit-marketplace.md`. Findings after dedupe: **1 Critical, 9 Important, 9 Minor.** All wave findings closed in a single bounded 6-commit fix wave (W-1…W-6, commits `cb16846`…`8f0ed29`), each with a non-vacuous regression test, verified by a scoped re-review (Task 4). Documentation corrections D-1…D-5 applied as Task 5 edits. Minors parked verbatim in the worktree ledger (P-1…P-9; P-2 closed for free inside W-3).

| Gap | Disposition | Reason |
|---|---|---|
| AC2 — five missing columns (schedule, service area, delivery fee, stock/availability, evidence requirement) | **Ledgered, not built. Count corrected to five** in all four canonical locations: `tasks.md:8`, `screen-inventory.md:34`, `traceability-matrix.md:140`, and `sprint-plan.md:630` (append-only). | All five are per-vendor-offer attributes, not product-definition attributes (the create migration argues this at `:33-44`); nothing can read or write them — no `vendors`/`carts`/`marketplace_orders`/`vendor_orders` table exists (`design.md` names 15 tables, exactly 2 on disk); their only consumer is cart/checkout, which sits under the unresolved precedence conflict below; and a price/delivery-fee/service-area schema on a public table is a financial-adjacent, human-gated migration (`AGENTS.md` §Infrastructure-agent execution). Owner: cart/checkout spec work (Sprint 11–12). **Name-collision note:** the global table name `evidence_requirements` is already taken by the ServiceCatalog lane (`2026_07_26_180600_create_evidence_requirements_table.php`, keyed to `service_package_item_id`) while `design.md` calls Marketplace's equivalent `fulfilment_evidence` — reconcile before any Marketplace migration uses the name. |
| Category-code OPEN QUESTION | **Ledgered, not resolvable.** `/marketplace/kategori/{categorySlug}` stays deliberately **unregistered and BLOCKED**; filtering uses the internal `?kategori=` key. | `marketplace-catalog.md` defines 9 product codes and 0 category codes, and no slug was invented. Owner: product owner — a human/product call, not an engineering one. |
| `{productSlug}` IA drift | **Closed.** `information-architecture.md:13` corrected to `/marketplace/produk/{productCode}` (D-4). | This retrofit is the named owner; the route is built as `{productCode}`, no slug column exists, and minting one would duplicate canonical catalogue data (`AGENTS.md` §Documentation). Line 13 only — lines 12, 14, 15 untouched (category-code OQ and the cart/checkout contradiction remain this lane's prohibitions). |
| §6.1 loading / §6.6 duplicate/retry-safe / §6.8 success | **Split.** §6.6 duplicate/retry-safe is **covered, not unimplemented**: `MarketplaceIndexRouteTest::test_browsing_is_read_only_and_repeated_renders_never_mutate_the_catalogue` and `ProductDetailRouteTest::test_viewing_a_product_never_mutates_the_catalogue` assert a plain read has no state-mutating side effect, so repetition is naturally safe. §6.1 loading (no test) and §6.8 success (no mutation to guard) stay **ledgered, correct deferral**. `tasks.md:175` corrected (D-2). | §6.1 and §6.8 structurally cannot exist on a browse-only surface — there is no load that blocks and no mutation whose success needs a state. Deferring is correct, not a gap. |
| Accessibility geometry (touch targets, focus ring, mobile reflow) | **Ledgered, blocked on tooling.** Not closed by this retrofit. | No browser/axe/screen-reader harness exists anywhere in this repo — the program-level gap all prior retrofits recorded independently. The markup-layer colour-only half is CI-green (non-colour tick + single current chip tests). |
| `tasks.md` §NOT TESTED bullets 2–5 | **Ledgered — product decisions.** | Commercial completeness, product-owner review of the label mapping, and the two unresolved `ekspektasi-vs-specs.md` D3/D4 questions (Step-4 `FLOWERS` add-on ↔ marketplace `FLOWER_BOARD` relationship; the "has stock/availability" product data assertion). Owner: product owner / the relevant future spec. |
| `VendorProcessingStatus` forward definition | **Kept — justified, now drift-enforced.** New `VendorProcessingStatusTest` (W-6) re-parses the fenced block at `marketplace-catalog.md:76-85`: all eight statuses, in document order, `DIBAYAR` absent. | One catalogue-traceable definition that encodes AC12's `DIBAYAR`≠`SELESAI` invariant by omission, ahead of its named consumer (`vendor_orders`). Before W-6 its "cannot drift" justification was unenforced; the drift test makes it hold. |
| `MarketplaceComingSoon` dead stub (three-way) | **Ledgered, not deleted.** "`MarketplaceComingSoon` is one of three orphaned ComingSoon stubs (with `RenewalComingSoon` and `BookingWizardComingSoon`) sharing `coming-soon.blade.php` — one future PR should remove all three together, not a two-way pairing." | Grep evidence is complete (verified fact, not assumption): no route, no `<livewire:`/`@livewire(` tag, no `Livewire::component` registration, no config, and no test dependency — the only live assertion is `HomePageRouteTest`'s `assertDontSee('Layanan Pemakaman Segera Hadir')`, a negative. `routes/web.php:55-59` misuses "either" of three items and should be corrected in that same future PR. `BookingWizardComingSoon` is owned by **no lane** in this wave. **Shared-view open decision (not decided here):** `coming-soon.blade.php` has exactly three executable consumers, so a combined removal *could* also drop the view — left to the future PR because `routes/web.php:59-64` and `screen-inventory.md:49` still name the stub pattern as the standing answer for an unbuilt route. |
| `mvp-scope`/`sprint-plan` cart-checkout contradiction | **Open dependency — not resolvable by this retrofit.** Reproduced verbatim from the plan: | `docs/product/mvp-scope.md:35` reads `- Cart dan checkout.` under the unconditional **MUST IMPLEMENT** framing (`mvp-scope.md:5`); `docs/planning/sprint-plan.md:162` defers the identical scope outright ("Feature specs deferred entirely: `funeral-marketplace-and-vendor-portal` (cart, checkout, **vendor portal**)") and pushes it to Sprint 11–12 in three further places. `AGENTS.md` §Source precedence ranks `mvp-scope.md` above plans, and "Never remove a stakeholder MVP item merely because an external gate is closed. Implement the documented fallback." This lane picks no side. **Marketplace's SECOND retrofit — the one that reviews cart/checkout once they are actually built — MUST NOT START until a human resolves whether `mvp-scope.md` line 34/35 or `sprint-plan.md`'s Sprint 11–12 deferral governs.** Building cart/checkout against an unresolved precedence conflict would produce work whose acceptance criteria nobody can state. |
| W-1 ledgered data-layer half | **Ledgered, human-gated.** The presentation marker closed the user-facing harm in-wave; the data-layer half stays open. | The in-wave fix never renders `vendor_name` without a "contoh" marker or `base_price_idr` without the provenance line, mirroring `CemeteryPresenter`. Changing the seeded values to a literal `Contoh` prefix, or adding a `source`-style column like `GraveRecordSource::CONTOH`, is a data/schema change requiring a migration against a public, financial-adjacent table — `AGENTS.md` §Infrastructure-agent execution makes it a human gate. Say so rather than implying the in-wave fix is complete. |
| Cross-lane: ServiceCatalog migration slot claim | **Routed to the ServiceCatalog lane.** `2026_07_26_180000_create_service_definitions_table.php:63-70` carries the mirror-image false slot-reservation claim; the Marketplace half was corrected on this branch (D-3). | The parallel lane owns that file and its PR was open concurrently; the lane was notified directly. Fixing one side halves a verified-false statement rather than preserving symmetry in error. |
| Cross-module notes (four, one row) | **Routed to a design-system / program-level backlog.** | (a) `alert.blade.php:107` / `button.blade.php:76` silent intent fallback + two stale comments at `:11-12`, `:81-83`; (b) `design-system.md` §7.3 vs WCAG 2.5.5 inline-link exemption; (c) all three public query classes rest on convention, and `GraveRegistryPublicQuery.php:14`'s "The ONE" is the overclaim of the three; (d) `HomePageRouteTest.php:228` uses `DROP TABLE … CASCADE`, inconsistent with the `RenewalStartTest` doctrine. All repo-wide, none in this lane's scope, none bites this module today. |
| Parked Minors P-1…P-9 | **Parked, ledgered verbatim** in `.superpowers/sdd/retrofit-marketplace/task-3-minor-findings.md` (worktree-local, git-ignored). P-2 closed for free inside W-3. | Each has a named park reason in that ledger (defence-in-depth, INFERRED-only, cross-module, or hazard note); none is a live defect in this module. |

Full evidence trail: `.superpowers/sdd/retrofit-marketplace/` (worktree-local, git-ignored — 3 task briefs, 3 task reports, the whole-module review, the Minor-findings park, the Task 4 scoped re-review) and the retrofit's own commit history on branch `retrofit-marketplace`.

### `platform-financial-ledger` (L4), **new build** — not a retrofit — built 09–11 Aug 2026

Recorded here because §2 is where this repository disposes per-module gaps, and this lane has
many. It is a **Wave 1 new build** under Superpowers SDD, not a retrofit of shipped code, so
the "retrofitted" framing of every section above does not apply. Full plan and the complete
**18-item merge sign-off bundle**:
`docs/superpowers/plans/2026-08-09-platform-financial-ledger.md` §Merge sign-off bundle —
**that document is the authority; this table is a pointer and is deliberately not a copy of it.**

Review history: 3 task-scoped slices + 1 whole-branch review → 0 Critical, 7 Important; one
bounded fix wave closed all 7; a scoped re-review found one closure (M5) **overstated**; a
second round closed it for real; a final scoped re-review returned **APPROVE, 0 Critical /
0 Important, 4 of 4 closed, 4 mutations applied and 4 detected**. Nothing here opens a gate.

| Gap | Disposition | Reason |
|---|---|---|
| **AC14 — release rollback preserving journal + audit history** | **NO EVIDENCE OF ANY KIND. Not deferred quietly — needs a decision at merge.** | Task 8 grepped the suite; every hit is a *transaction* rollback, which is a different property. Build it or accept the risk explicitly; it must not ride through silently. Sign-off bundle item 6. |
| AC7 partial — refund and chargeback | **Ledgered.** Two of four operation types: refund is journal-shape-only, chargeback is a reserved value with no behaviour behind it. | Bundle item 7. |
| DB-level financial policy: one-reversal-ever UNIQUE index on `journal_batches.reverses_batch_id`; `payouts.payable_id` UNIQUE | **Built, human sign-off required before merge.** | These make "a batch may be reversed at most once, permanently" a *database* policy rather than a revisable code choice. Reasoning reviewed and judged sound, but the class of decision requires a human (`AGENTS.md` §Infrastructure-agent execution). Bundle items 1–2. |
| `vendor_payables` DELETE trigger rejects even a consistent delete (T4-M1) | **Known-open, deliberately not fixed.** | `assert_vendor_payable_payout_pair(OLD.id)` raises `NOT FOUND` on any DELETE with a misleading FK-style message. Latent (nothing deletes `vendor_payables`). Closing it is a policy choice. Bundle item 3. |
| Deferred-constraint trigger's positive path never exercised (T4-M2/M3); `2026_08_10_120300` `down()` is a no-op (T4-M5) | **Ledgered.** | `RefreshDatabase` rolls back before COMMIT, so the positive path is unreachable in this harness; needs a non-transactional PostgreSQL harness. A `REVOKE` cannot close a coverage gap. Bundle items 4–5. |
| Re-authentication is login-recency, not step-up (I5) | **Ledgered, owned by `fix/reauthentication-satisfy-wiring`, not this lane.** | `last_authenticated_at` is written only at login; `MfaChallenge::submit()` never refreshes it. Both directions fail closed, so this is a usability gap, not a security one — but it is the inverse of the intuitive expectation. **Do not describe the bulk export as "step-up protected" without this qualification.** Bundle item 8. |
| `EnforceMfaChallenge` attached to the export route but **NOT TESTED** in this lane | **Ledgered.** | No test binds `mfaState`, so every route test passes *through* that middleware without exercising it; it also currently routes MFA-enrolled admins into that middleware's known self-redirect loop. Bundle item 9. |
| `LedgerReport::summary($period, null)` unscoped-read shape (N3) | **Ledgered deliberately so it survives.** | Reachable by no current caller, but it is the residual hazard class of the Critical authorization finding (C1). Bundle item 10. |
| Feature has never run against the real identity adapter | **Ledgered — this is why sign-off is doing real work.** | `ActorContext::$roles` is always `[]`, so every authorized-path assertion in the suite goes through a bound fake. Correct fail-closed state, but not equivalent to proof. Bundle item 11. |
| `sql/revoke-journal-mutations.sql` **has not been run and must not be** | **Blocked, intentionally.** | Finding N-1: the host provisions one PostgreSQL role per environment, so there is no separate app role whose UPDATE/DELETE can be revoked. `ci/verify-docs.sh` GATE 13 keeps the file honest; two bypass tests assert the model-layer gap still exists so they fail loudly at the database once the role split lands. Bundle item 14. |
| Cross-lane: 5 files conflict with `lane/l3-payment-adapter`, plus 2 planning docs | **Routed to merge-time reconciliation, with per-file resolutions recorded.** | `Money.php`, `MoneyTest.php`, `SensitiveActions.php`, `SensitiveActionsTest.php`, `routes/web.php`, and now `sprint-plan.md` + `retrofit-backlog.md` (this section). **Merge order L2 → L3 → L4 is load-bearing** — `BookingDraftQuery.php:86` still carries `(float) $priceVersion->amount` here and is fixed on L3, so merging L4 first would put a float in the money path. Re-run the `git merge-tree` simulation at merge time rather than trusting the list. Bundle items 15–16. |
| `LedgerSeamStub` divergence, both halves (E1 + blank `entityRef`) | **Ledgered as ONE item with two halves, deliberately.** | Fixing the exception model and believing the item discharged would leave the `entityRef` divergence in place — the exact failure this wording prevents. Latent, not live: no L3 production code consumes the seam yet. Bundle item 17. |
| `manual_verify:` business-key format documented but now refused (B6) | **NEEDS A HUMAN DECISION — recorded, not resolved.** | `Contracts\Journal.php:60` and the plan's own §Global Constraints document `manual_verify:{verification_id}`, but corrections now require the prefix to equal `manual_verification`. Nothing is broken today (no caller or test uses it), but the lane declined to pick a resolution unilaterally because it conflicts with the plan's own text and edits a cross-lane file. Bundle item 18. |
| Sequencing deviations: `JOURNAL_REVERSAL` (Task 4, not 6), `RECONCILIATION_EXCEPTION_RESOLVED` (Task 5) | **Named explicitly rather than carried silently.** | Both are `SensitiveActions` additions beyond what the plan's Global Constraints anticipated for this lane; the second was a user-approved Wave 1c ruling. Bundle items 12–13. |
| 14 Minor findings across the review rounds | **8 fixed, 6 deferred with reasons, 0 open.** | Deferral reasons are recorded per-finding in the worktree ledger; the three most recent (orphaned doc block, incomplete `@throws`, deferrals recorded only in git-ignored files) were folded in before the PR. |

Full evidence trail: `.superpowers/sdd/2026-08-09-platform-financial-ledger/` (worktree-local,
**git-ignored** — 8 task briefs/reports, 3 task-scoped review verdicts, the whole-branch review,
and 2 scoped re-review verdicts) and the lane's commit history on `lane/l4-financial-ledger`.
Because that directory does not survive the merge, the sign-off bundle was deliberately moved
into the **git-tracked** plan document.

### `platform-payment-adapter` (new-build, not a retrofit), lane `lane/l3-payment-adapter`, completed 11 Aug 2026

Genuinely new financial-domain work, not a review-and-fix pass on shipped code — included here because `AGENTS.md` §Development methodology holds new work to the same review bar as a retrofit, and this lane produced the same shape of self-flagged, ruling-approved gaps a retrofit does. Executed via `subagent-driven-development` (Tasks 1-10, each with its own task-scoped review, several with a bounded fix round + scoped re-review) plus three whole-branch review slices (Task 9: Money/financial-invariants, Webhook/security, Guard/fallback/UX — 0 Critical/Important across all three). The lane's guard is deny-only throughout: `GuardPaymentSession`'s six conditions all deny except the server-resolved-mode check, because the upstream records they depend on (confirmation, quote, opening authorization, merchant binding — owned by the unbuilt `booking-and-order-orchestration`, Sprint 7) don't exist yet. Every task built a self-contained slice decoupled from the blocked path rather than fabricating a fixture. Full plan: `docs/superpowers/plans/2026-08-09-platform-payment-adapter.md`.

| Gap / finding | Disposition | Owner / reason |
|---|---|---|
| No working paid path anywhere (`payment_sessions` cannot be created; `Journal::post()`/`postReversal()` have zero real call sites; no `Contracts/PaymentProvider` exists) | **Ledgered, not built — deliberate, ruling-approved (1b-L3-01, 1b-L3-04, Wave 1c/1d).** | Owner: `booking-and-order-orchestration` (Sprint 7) for the five missing guard-condition records; no owner yet for `Contracts/PaymentProvider` (new-contract authorship was explicitly withheld from this lane, same discipline Task 7 was held to for not authoring a rival `Journal`). This is the single root cause behind every other NOT TESTED item below. |
| `Money.php`/`MoneyTest.php` not actually byte-identical between this lane and L4 (`financial-ledger`) despite prior task reports asserting the seam was stable | **Ledgered — needs merge-time reconciliation, corrected guidance recorded.** | `format()`'s statement ordering differs (semantically inert either way; L4's guard-before-compute ordering is the agreed convergence target). `MoneyTest.php`: both lanes independently added a PHP_INT_MIN test under different names with byte-identical bodies (dedup to one, not a blind union); L4's copy alone has `test_from_decimal_rejects_non_zero_excess_precision`, keep it. Whoever merges second reconciles. |
| `LedgerSeamStub` (Task 7's contract-test double for `Contracts\Journal`) throws `InvalidArgumentException` for 2 of the 4 required properties; the real implementation throws `QueryException` (database is the balance/uniqueness authority per the contract's own terms) | **Ledgered — needs correction before/at merge.** | Not a live bug (no L3 code calls `Journal::post()` yet), but a future consumer catching `InvalidArgumentException` for retry/redelivery handling would silently miss the real failure mode. |
| Financial human sign-off bundle: no `PaymentIntentDecision::Allowed` case; PostgreSQL-only CHECK constraints untestable on SQLite; raw-SQL/query-builder inserts into `payment_sessions` technically possible at the DB level | **Ledgered — named for PR-time human financial review, not fixed.** | All three deliberate/inherent to a deny-only guard on a single-Postgres-role setup; independently PostgreSQL-verified in CI and via throwaway containers throughout this lane (never left as a bare local-only claim). |
| Task 4's headline ACs (paid transition, journal write, outbox emission, AC7/AC10/AC11 positive halves) largely untested pending `booking-and-order-orchestration` | **Ledgered, ruling-approved reduced scope (1b-L3-04).** | Same root cause as row 1. What IS reachable (provider-event claim machinery, apply-time settlement guard, browser-return view-only proof) was built for real and independently PostgreSQL-verified. |
| Task 4 minors: dead code in `GuardCondition::position()`, a test hardcoding 3 FQCNs instead of asserting on namespace, a factory-detection test missing a nullable/union-type edge case, `GuardCondition::ORDER` hand-duplicating enum order (mitigated by a divergence test), stale "(AC1, AC2, AC3)" plan heading | **Deferred, ledgered.** | Per-item severity too low to extend a bounded fix loop (this lane's own SDD convention); none is a live defect. |
| Task 4's `lockForUpdate()` concurrency claim was reviewed reasoning only when originally shipped, not independently proven against real concurrent connections | **Ledgered — weaker evidence than the same pattern got later in this lane.** | Tasks 5/6's own row-lock/unique-constraint concurrency claims WERE independently proven with real two-connection PostgreSQL tests later in this lane (see `progress.md`); Task 4's `ProcessWebhookEvent` claim was not re-verified to that same bar before this PR. Flagged for whoever next touches that file, not re-opened here. |
| Task 5 minors: `NotFound` outcome in `ProcessWebhookEvent` silently swallowed with no log/audit/metric; a strict `===` identity re-match against a DB-normalised UUID (latent, not exploitable with the single current caller); AC4 structural blacklist test covers 2 files but not call patterns or Blade views (the 4 behavioural tests independently prove AC4 holds regardless) | **Deferred, ledgered.** | Same "too low severity to extend the fix loop" reasoning as Task 4's minors. |
| `payment_verifications`/`payment_reversals`' `reference` columns are free-text, not foreign keys, to any order/session/verification record | **Deliberate design call, flagged not settled.** | Both migrations' own doc blocks name this explicitly: once an order aggregate exists, a future task will very likely add a real FK and a backfill migration. Not a gap to close now — a decoupling choice made because the FK target doesn't exist. |
| Task 6's plan text names a general "Reversal" `PaymentReversalType` category but never names a concrete `SensitiveActions` audit action for it | **Ledgered — open design question, not resolved unilaterally.** | Built exactly the two cases (`Refund`/`Chargeback`) the plan text does name audit actions for; a third case needs a human/product decision on its audit-action name, not an agent guess. |
| Task 8 Step 1 (live SumoPod sandbox smoke test) | **Explicitly NOT TESTED.** | Requires live outbound network calls to a real third-party sandbox with real credentials from a human/ops-provisioned dev host — the agent session's own permission system correctly refused even a presence-check read of `.env`. A webhook-receiver-only fallback was considered and rejected: it would not honestly satisfy "simulate the webhook flow with the sandbox's test event" and risked misrepresenting coverage. |
| Screen ID `PUB-019` (Task 4's browser-return/cancel landing pages) minted without product/doc-owner ratification | **Ledgered — needs product/doc-owner sign-off.** | `AGENTS.md` requires the screen inventory to follow behaviour; the screen genuinely exists and needed an ID, but the ID itself needs ratification, not re-invention. |
| `ReauthenticationService`'s class doc block still claims no `satisfy()` caller exists | **Not this lane's to fix — pre-existing trunk debt, now stale.** | Found live during Task 9's Guard/fallback/UX slice review: three real callers landed across this lane (`DisableMfaController` pre-existing, `VerifyManualPaymentController` Task 5, `RecordPaymentReversalController` Task 6), making the doc block's claim false. The doc lives outside this lane's owned files. |
| `HomePageRouteTest.php:228`'s `DROP TABLE ... CASCADE` SQLite incompatibility | **Confirmed cross-referenced, not owned by this lane.** | Same item the Marketplace retrofit already routed to a program-level backlog (row above, "Cross-module notes (d)"). This lane independently hit and confirmed the identical 2 pre-existing SQLite errors (this one plus `EloquentGateRegistrySourceTest`) in every full-suite run this session; both vanish entirely on real PostgreSQL (verified repeatedly), confirming they are SQLite-only artifacts. |

Full evidence trail: `.superpowers/sdd/2026-08-09-platform-payment-adapter/` (worktree-local, git-ignored — 6 task briefs with Wave-1b/1c/1d append-corrections, task reports, review diffs, and the full `progress.md` ledger) and the lane's own commit history on branch `lane/l3-payment-adapter`.
