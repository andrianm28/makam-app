# Traceability Matrix — v0.6

## Status legend

- `Specified` — spec and screen exist; **no test evidence yet**.
- `Specified (gated fallback)` — `Specified`, and the user-facing step is required even when an external gate is closed (see section C).
- `Covered` — an automated test exists, is named in this row's **Test evidence** cell, asserts what this row claims, and passes in CI. `AGENTS.md`: *"Every traceability item marked `Covered` needs test evidence."*

`ci/verify-docs.sh` GATE 7 enforces the mechanical half of `Covered`: every section B row marked `Covered` must name at least one test path in its **Test evidence** cell, and every path it names must exist on disk. What a gate cannot check is whether that test actually asserts what the row claims — section D records that reading per row, with the specific test methods, so a reviewer can re-check it instead of trusting a filename.

**Browser testing is a smoke harness only, not the mandated suite.** As of 08 Aug 2026 there was no Dusk, Playwright, or Cypress harness at all; on 13 Aug 2026 a Playwright + `@axe-core/playwright` harness landed (PR #31, `opencode/playwright-a11y-harness`): `tests/browser/smoke.spec.ts` drives the homepage and asserts axe-clean accessibility, and CI runs it as the `browser-test` job. That is one page, one journey — the `E2E-*` values in the **Test family** column still name families that are not implemented as journey suites, and the evidence behind the `Covered` rows below remains server-side HTTP/Livewire feature tests. `AGENTS.md` §Testing separately requires *"Browser tests cover all four homepage routes, nine booking steps, marketplace flow, renewal flow, FAQ, admin, and vendor"* — **that requirement is still not satisfied by any row in this file.**

### Revision history

v0.4 — 25 July 2026, finding H-3: statuses in section B previously read `Covered` for items that were specified but untested; all 31 were corrected to `Specified`. The repository currently contains no test files.

**Correction — finding T-A, 08 August 2026.** The v0.4 note above is kept verbatim rather than rewritten, per this repository's convention of recording superseded reasoning (see `docs/planning/sprint-plan.md` findings N-10/N-11). Two parts of it have since gone stale:

1. *"The repository currently contains no test files"* was true when written and is now false. Verified 08 Aug 2026 by direct count: **90 test files** under `tests/`, **503 `test_*` methods**, zero of them the Laravel skeleton's `ExampleTest.php` stubs (both were deleted). The whole suite passes in CI — GitHub Actions run `30196713664`, job `PHP (validate, lint, analyse, test)` (id `89779419790`), step *Tests* → `success`, on commit `3f5d14f`, which is the current `HEAD` of `docs/design-system-and-planning`.
2. The v0.4 status legend said `Covered` was *"unusable until tests land"*. Tests have landed, so that sentence is removed from the legend above and preserved here.

H-3's blanket downgrade of all 31 rows was correct in July and became wrong in the opposite direction as real tests landed: rows whose tests genuinely exist and pass kept reporting `Specified`, understating coverage as badly as the pre-H-3 file overstated it. **10 rows are raised to `Covered` in v0.5** (HOME-01…HOME-04, FAQ-01…FAQ-06) — each one read against its test file first, not raised on the strength of a filename. The other 21 stay `Specified`; section D says why for the ones where a test file exists but does not cover the row's claim.

**v0.7 — 09 August 2026, S4-T4/S4-T5 (booking wizard Steps 1–5).** The booking wizard was built 08–09 Aug 2026 and reviewed; **four rows move from `Specified` to `Specified (tests written, CI pending)`** — BOOK-01, BOOK-03, BOOK-04, BOOK-05 — each with real test paths now named in its evidence cell, each read against the test file first. **None is raised to `Covered`**: the branch is unmerged and has had no CI run, and this file's own `Covered` legend requires a CI pass. Whoever merges the branch and sees a green run should raise all four and delete the way-station status if nothing else uses it. BOOK-02's scope note is corrected — it said the wizard embed was unbuilt because S4-T4 was paused. Section D carries the method-level trail. The v0.6 note below is kept verbatim.

**v0.8 — 13 August 2026, order-orchestration domain (`booking-and-order-orchestration`).** The order aggregate, forward-only commercial state machine, immutable versioned quotes, payment-guard conditions 2–5, paid effects, Step 9 read model, and order-scoped notification dispatch were built on branch `lane/l7-order-orchestration`. This is the **domain** half of the wizard's Steps 6–9 (the presentation half is L6's). It adds real, named test evidence for BOOK-06…BOOK-09's *domain* claims and keeps the rows at their current statuses — **no row here is raised to `Covered`**: the branch is unmerged with no CI run, and the way-station rule from v0.7 applies. The payments gate (`FIN-DEC-01`) remains closed, so BOOK-08 stays `Specified (gated fallback)` and Step 8's manual-coordination fallback is required, never removed. Section D carries the method-level trail.

**v0.9 — 14 August 2026, post-merge reconciliation (booking steps 6–9, renewal steps 4–6, vendor portal).** L6 (`lane/l6-booking-completion`), L7 (`lane/l7-order-orchestration`), L8 (`lane/l8-renewal-completion`), and L10 (`lane/l10-vendor-portal`) all merged into `docs/design-system-and-planning` on 13 Aug 2026 (PRs #27, #28, #29, #30). The merged trunk ran green — GitHub Actions run [`31680266045`](https://github.com/andrianm28/makam-app/actions/runs/31680266045), commit `60aa130` — and has stayed green since (run `31763118027` on `7438b4b`, 14 Aug). This is the moment the v0.7 note above appointed: *"Whoever merges the branch and sees a green run should raise all four and delete the way-station status if nothing else uses it."* BOOK-01, BOOK-03, BOOK-04, BOOK-05 (L6) and BOOK-06, BOOK-07, BOOK-09 (L6 presentation + L7 domain) are raised to `Covered`; BOOK-08 stays `Specified (gated fallback)` — the online-payment gate (`FIN-DEC-01`) is still closed, so Step 8's manual-coordination fallback is still the required state and its evidence cells are unchanged. REN-04 and REN-06 (L8) are raised to `Covered` on their screens' own tests; REN-05 stays `Specified (gated fallback)` with evidence named. VENDOR-01 (L10) is raised to `Covered` on the vendor panel's own tests. The way-station status is retired from the legend — no row uses it after these raises. The v0.8 note above is kept verbatim: its "unmerged, no CI run" clause was true when written earlier on 13 Aug, before PR #28 landed, and is superseded here rather than rewritten.

**v0.10 — 15 August 2026, P1 admin order management (PRs #60–#64).** The three admin order-management resources (`BookingOrderResource`, `MarketplaceOrderResource`, `RenewalOrderResource`) and their supporting infrastructure (order transition authorizer, re-authentication guard, operator and money-adjacent domain Actions) merged into `docs/design-system-and-planning` on 15 Aug 2026. **ADMIN-01 is raised to `Covered`** on the named P1 tests, each read against the row's claim first — see section D. The row's claim is scoped there: it now covers the shipped admin order-management module set (booking/marketplace/renewal order resources, role-gated transitions, money-adjacent re-auth, audit-every-transition, append-only timeline); reports (ADM-090) and the audit-review surface (ADM-100) remain unbuilt and are not claimed. The v0.9 note above is kept verbatim.

**v0.11 — 16 August 2026, P2 admin data management (PRs #69–#72).** The P2 module set — site settings (`SiteSettingsResource`), feature-gate operations (`FeatureGateAdmin`), launch cities (`LaunchCityResource` + the `LaunchCity` table seam), service packages with versions/items (`ServicePackageResource`), product variants (`VariantsRelationManager` under `ProductResource`), and vendor administration (`VendorResource` with members/listings/availability relation managers) — merged into `docs/design-system-and-planning` on 16 Aug 2026. Merge order in history was #71 (packages/variants), #72 (vendors), #69 (settings/gates), #70 (launch cities) — not the plan's A→B→C→D; the lanes were file-disjoint so the order did not matter, and the ledger records it. **ADMIN-01's claim is widened** from order management to the admin data-management module set and stays `Covered` on the P2 tests added to its evidence cell, each read against the claim first — see the P2 block in section D. Reports (ADM-090) and the audit-review surface (ADM-100) remain unbuilt and are not claimed; the master-data/FAQ module set stays covered by the already-named master-data/FAQ tests. The v0.10 note above is kept verbatim.

**v0.12 — 16 August 2026, P3 plot inventory + reservation (PRs #77/#78/#79).** The P3 module set — `CemeteryBlock` + `GravePlot` with bulk plot generation (`CreateCemeteryBlock`), the admin surfaces (`BlocksRelationManager` under `CemeteryResource`, `GravePlotsResource` with state overrides), the append-only `PlotReservation` module (atomic reserve via plot-row lock + `plot_state` aggregate — the partial-unique-index backstop was dropped, see §4.2 of the P3 design — plus confirm/release/expire), and the order-view reservation integration — merged into `docs/design-system-and-planning` on 16 Aug 2026. **Three new rows are added as `Covered`** — PLOT-01 (inventory), PLOT-02 (reservation), PLOT-03 (the availability decision on the order view) — each read against its test file first, per the "read against the claim, not the filename" rule; section D carries the method-level trail. These rows implement the authoritative half of RKS K24 (Availability), whose default remains manual confirmation. The v0.11 note above is kept verbatim.

**v0.13 — 16 August 2026, P4 memorial + QR + visitation (PRs #81–#84).** The P4 module set merged into `docs/design-system-and-planning` on 16 Aug 2026: visitation domain (PR #81), visitation public page + operator bookings resource (PR #83), memorial domain (PR #82), memorial public resolve + family surface + admin memorial/moderation resources (PR #84). **Ten new rows are added as `Covered`** — VISIT-01…VISIT-04 (visitation) and MEM-01…MEM-06 (memorial) — each read against its test file first, per the "read against the claim, not the filename" rule; section D carries the method-level trail. These rows implement kiro `visitation-booking` AC1–AC7 and `memorial-and-qr` AC1–AC8's shipped half (AC8's retention/deletion policy stays out of scope — no policy document exists yet, exactly as the memorial spec's "explicitly not covered" records). Both modules are gated: visitation mode is the per-cemetery `visitation_mode` capability column, and the memorial public surface is fail-closed while `G-MEM-01` is closed — the rows claim the built surfaces (uniform fail-closed included), not hypothetical gate-open behavior beyond what the shipped allowlist projection tests prove. The v0.12 note above is kept verbatim.

**v0.14 — 16 August 2026, P5a certificates + agreements + pre-need (PRs #86/#88/#89/#90).** The P5a module set merged into `docs/design-system-and-planning` on 16 Aug 2026: agreements+certificates domain (PR #86), admin certificate and agreement resources + the customer certificate status page (PR #88), the pre-need paid-flow domain + case resource (PR #89 — the domain and admin lanes merged as one PR in history), and the public pre-need surface (PR #90). **Seven new rows are added as `Covered`** — CER-01…CER-03 (agreements & certificates) and PREN-01…PREN-04 (pre-need) — each read against its test file first, per the "read against the claim, not the filename" rule; section D carries the method-level trail. These rows implement kiro `certificates-and-agreements` AC1–AC8 and `pre-need-contracting` AC1–AC8's shipped half. Both modules are honest about their gates: certificate issuance is issuer-role-gated with eligibility by rule (never payment status directly), the paid pre-need flow is fully built but fail-closed while `G-LEGAL-01` is closed (uniform `PreNeedGateClosedException`, no state change, `PRENEED_GATE_DENIED` audit), interest + consultation are never gated, and the per-installment payment-link action is fail-closed under the payment guard's amount==quote-total invariant (FIN-DEC-09) — the rows claim the built fail-closed behavior, not hypothetical gate-open behavior beyond what the tests exercise with the gate test-opened. The v0.13 note above is kept verbatim.

**v0.6 — finding T-J, 08 August 2026.** Sprint 4 batches S4-T6 (cemetery directory), S4-T7 (renewal skeleton), and S4-T8 (marketplace browse) landed and this file understated all three, exactly the direction T-B warns about. **7 rows are raised to `Covered`** — BOOK-02, MKT-01…MKT-03, REN-01…REN-03 — each read against its test file first, not raised on the strength of a filename; section D carries the method-level trail and the scope limits. **BOOK-01 was deliberately not raised despite real new evidence**, and section D says why rather than leaving the omission silent. Two gaps are recorded as `NOT TESTED` rather than glossed: the renewal spec's AC4 (< 500 ms at 100k records) and the cemetery spec's directory/map benchmark. The v0.5 revision note above is kept verbatim. **Correction, 22 Aug 2026:** the AC4 half of that pair is closed at reduced scale — see section D's REN-03 entry for the real benchmark now cited; the directory/map benchmark gap is unchanged.

**Addition — finding T-H, 08 August 2026.** Screen PUB-060 (`/bantuan`) shipped the same day as a fix for a real defect — see [`.kiro/specs/help-centre-missing-route/`](../../.kiro/specs/help-centre-missing-route/), this repository's first Bugfix Spec. It does not fit section B's table, which is scoped to RKS-derived Stakeholder Workflow expectations (section A); PUB-060 answers `product-brief.md` §5.10 and `information-architecture.md` §2 instead, a cross-cutting UX principle with no RKS item behind it. Rather than force an ill-fitting RKS mapping, it is recorded in new **section E** below.

**v0.15 — 20 August 2026, `/akun` customer account area (PRs #112/#113/#114).** The `/akun` account
area — login, registration, logout, password reset (PR #112, `lane/akun-auth-foundation`), the
account shell + header wiring + draft-resume list (PR #113, `lane/akun-shell-and-drafts`), and the
order list (PR #114, `lane/akun-pesanan`) — merged into `docs/design-system-and-planning` on
20 Aug 2026, all three CI-green on first push after PR #112's own follow-up round found (and
fixed) four real bugs no local check or code review had caught. **Eight new rows are added as
`Covered`, in section E rather than section B** — AKUN-01…AKUN-08 — each read against its test
file first, per the "read against the claim, not the filename" rule; section D carries the
method-level trail. Section E, not B, because — same reasoning `SUPPORT-01` above already
establishes — this work has **no owning RKS item or kiro spec**: it began from a plan-mode
session, not a spec batch, and section A's RKS K23–K35 table has no capability these rows would
attach to. This closes a gap this file's own §1 already names the general shape of ("browser
tests cover... admin, and vendor" — not satisfied by any row in this file; the same is now true
one level further for `/akun`, and stays true — no `E2E-*` browser suite covers these screens
either, only server-side HTTP/Livewire feature tests, exactly like every other row in this file).
**Two decisions worth recording explicitly, not silently folded into the table:** first,
`BookingWizard`'s draft-resume mechanism (AKUN-05) is a **deliberate, narrow reversal** of a prior
security ruling in `BookingDraftBinding`'s own doc block (session-only resume, written before
customer accounts existed) — the reversal was raised to and decided by the user directly during
PR #113's whole-branch review, not decided silently in code, and AKUN-05's evidence trail below
says so. Second, `PUB-050` (the still-unbuilt per-order detail/timeline screen) remains untouched
and unclaimed by any row here — `/akun/pesanan` (AKUN-06) is a *list*, and its own test file
explicitly asserts no "Lihat detail" link exists, rather than silently implying PUB-050 is
superseded. The v0.14 note above is kept verbatim.

**v0.16 — 20 August 2026, three admin capabilities made honest instead of silently folded.**
ADMIN-01's own evidence trail (v0.10, v0.11) already said in prose that reports (ADM-090) and
the audit-review surface (ADM-100) remain unbuilt and are not claimed by that row's `Covered`
status — but neither ever got its own row, so a reader scanning section B's table alone would
not see the gap. A third capability, ADM-070 (payment/transaction/manual verification —
`admin-operations` AC5, also required by `docs/product/mvp-scope.md` §6), had no disclaimer
anywhere: it was silently folded into ADMIN-01 with no exclusion note at all, which is worse
than the other two. **Three rows are added, all `Specified` with no test evidence** —
ADM-070, ADM-090, ADM-100 — matching what `docs/product/screen-inventory.md` already records
these screens as (unshipped) and what `.kiro/specs/admin-operations/tasks.md` already scopes
them as (Tasks 5–7 of that spec's plan). None is raised to `Covered`; none has a test file.
The v0.15 note above is kept verbatim.

**v0.17 — 20 Aug 2026, traceability audit (MKT-04/MKT-05, CARE-SUB-01/CARE-SUB-02).** Two research
passes found this file understating one capability and misdating another, both corrected here
after independent verification against the code and test suite, not taken on faith from either
brief. **MKT-04** was carried `Specified`, evidence `—`, since 08 Aug 2026 — true then, stale since:
cart (`CartScreenTest`, `CartTest`), checkout/order-placement (`CheckoutScreenTest`,
`PlaceMarketplaceOrderTest`), vendor order processing (`VendorOrderStatusTransitionActionsTest`),
and order tracking (`OrderTrackingScreenTest`) are all real, routed, and tested. **MKT-04 is raised
to `Covered`** on that evidence, re-scoped to name exactly that journey; a **new row MKT-05** is
added `Specified (gated fallback)` for marketplace online payment specifically, which
`OpenPaymentSession` still refuses for `OrderType::Marketplace` (verified by reading the action
directly) — the manual-transfer path is the live payment path today, exactly the BOOK-08/REN-05
pattern. See the section D entry below; the three prose references to MKT-04's old status (in the
MKT-01…03 scope note and twice in the VENDOR-01 scope note) are corrected in place rather than
rewritten, per this file's own convention. **CARE-SUB-01** was carried `Specified`, evidence `—`;
`CreateSubscription` is real and tested (`CreateSubscriptionTest`) and is **raised to `Covered`**,
narrowly scoped to the creation transition only — `PauseSubscription`/`CancelSubscription` exist in
code with zero test coverage and are not claimed, and no test exercises the Filament header-action
wiring itself, only the domain Action it calls. **CARE-SUB-02**'s prose wrongly attributed the
`care.cycle_created.v1` outbox event to cycle *generation*; verified by reading
`GenerateCycle` (audit only, no outbox call) and `MarkCyclePaid` (`Outbox::record()`, proved by
`MarkCyclePaidTest::test_mark_paid_emits_outbox_event`) that the event fires on cycle *payment*.
The clause is removed from CARE-SUB-02's expectation text; **the row's `Specified` status is
unchanged** — only the factual claim about timing was wrong. The v0.16 note above is kept verbatim.

**v0.18 — 24 Aug 2026, ADM-090/ADM-100 corrected from stale `Specified` to `Covered`.** The
v0.10/v0.11/v0.16 notes above all said reports (ADM-090) and the audit-review surface (ADM-100)
"remain unbuilt and are not claimed" — that was true when written and has since gone stale: both
shipped and are browser-tested by `tests/browser/e2e-admin-vendor.spec.ts` (PR #142, merged 22 Aug
2026), verified directly by reading the file rather than trusting
`docs/testing/release-gates.md`'s own citation of it. **ADM-090 is raised to `Covered`** on
`'the three master-data-gated report pages are reachable and titled correctly'` and `'the three
finance-gated report pages are reachable and titled correctly for the admin holding ledger-read
access'`, which together exercise all 6 report pages the `admin-operations` spec names. **ADM-100
is raised to `Covered`** on `'audit trail review shows the required columns and is read-only'` and
`'audit trail records this suite's own seeded grants'`, which assert the read-only audit-trail
review's real column set and a real audited write actually appearing in it. See the section D
entry below for the method-level trail. This correction is scoped narrowly: it does **not** touch,
raise, or otherwise affect CARE-SUB-02 through CARE-SUB-07, which stay exactly as v0.16/v0.17 left
them — `docs/testing/release-gates.md`'s §A-35 box separately names those as substantive, open
evidence gaps, and this note changes nothing about them. The v0.17 note above is kept verbatim.

**v0.19 — 24 Aug 2026, maintenance pass on ADM-070/CARE-SUB-03/04/05/07 (all five carried evidence
`—`).** Each of the five rows the v0.18 note above named as open was searched for real,
already-existing test evidence that simply was never cited, not written fresh — per this file's own
"read against the claim, not the filename" rule. **Four rows are raised to `Covered` — CARE-SUB-03,
CARE-SUB-04, CARE-SUB-05, and (narrowed) CARE-SUB-07; ADM-070 stays a confirmed, genuine gap** — the
only route that exists (`admin.payments.manual-verifications.verify`) is a plain controller route,
not a Filament panel page, and there is no GET/view surface for payment/transaction references at
all. `PR #130` (merge commit `1a93938`, CI run
[`32506685010`](https://github.com/andrianm28/makam-app/actions/runs/32506685010), success) is the
real, already-merged commit behind the four raised rows' evidence; nothing new was written for this
pass. See the section D entry below for the method-level trail. The v0.18 note above is kept
verbatim.

## A. RKS authority

| RKS | Capability | Spec | Gate/control |
|---|---|---|---|
| K23 | TPU/TPS directory and map | `cemetery-directory-and-availability` | Admin master data |
| K24 | Availability | `cemetery-directory-and-availability` | Manual confirmation by default |
| K25 | New, overlapping, Urgent, Pre-Need | `public-booking-wizard`, `at-need-booking`, `pre-need-contracting` | Ops/legal gates |
| K26 | Basic/add-on services | `package-and-service-bundles`, service catalog | Versioned price/availability |
| K27 | Booking and status | `public-booking-wizard`, `booking-and-order-orchestration` | No early payment |
| K28 | Deceased data/documents | `public-booking-wizard`, booking orchestration | Private, short URL, audit |
| K29 | Funeral marketplace | `funeral-marketplace-and-vendor-portal` | Single-vendor MVP |
| K30 | Vendor portal | `funeral-marketplace-and-vendor-portal` | Query scope; payout gate |
| K31 | Renewal | `renewal-and-grave-registry` | Tariff source; external marking |
| K32 | Registry/search/reminder | `renewal-and-grave-registry` | Data/privacy gate |
| K33 | Recurring care | `recurring-care-subscriptions`, `grave-care-fulfillment` | Billing != fulfillment |
| K34 | Operator dashboard | `cemetery-operator-dashboard` | Non-blocking fallback |
| K35 | Admin dashboard | `admin-operations` | Audited changes |

## B. Stakeholder Workflow MVP

The **Test evidence** column holds repo-relative paths to the tests backing a row, and `—` for a row with none. A `Specified (tests written, CI pending)` row also names paths — that status exists precisely because evidence is present but not yet CI-verified. GATE 7 reads this column; see section D for the method-level trail.

| ID | Expectation | Canonical doc/spec | Screen | Test family | Test evidence | Status |
|---|---|---|---|---|---|---|
| HOME-01 | Pemesanan Makam menu | `public-home-and-navigation` | PUB-001 | E2E-HOME | `tests/Feature/Livewire/Public/HomePageRouteTest.php` | Covered |
| HOME-02 | Layanan Pemakaman menu | `public-home-and-navigation` | PUB-001 | E2E-HOME | `tests/Feature/Livewire/Public/HomePageRouteTest.php` | Covered |
| HOME-03 | Perpanjangan menu | `public-home-and-navigation` | PUB-001 | E2E-HOME | `tests/Feature/Livewire/Public/HomePageRouteTest.php` | Covered |
| HOME-04 | FAQ menu | `public-home-and-navigation` | PUB-001 | E2E-HOME | `tests/Feature/Livewire/Public/HomePageRouteTest.php` | Covered |
| BOOK-01 | Lima kota awal | directory + wizard | PUB-010 | E2E-BOOK-01 | `tests/Feature/Livewire/Public/Booking/BookingWizardRouteTest.php` | Covered |
| BOOK-02 | TPU/TPS detail | directory | PUB-011 | E2E-BOOK-02 | `tests/Feature/Livewire/Public/Directory/CemeteryDetailRouteTest.php`<br>`tests/Feature/Livewire/Public/Directory/CemeteryDirectoryIndexRouteTest.php`<br>`tests/Feature/Livewire/Public/Directory/PublicCapabilityProjectionTest.php` | Covered |
| BOOK-03 | Empat jenis layanan | wizard | PUB-012 | E2E-BOOK-03 | `tests/Feature/Livewire/Public/Booking/BookingWizardEndToEndTest.php`<br>`tests/Unit/Domain/Booking/BookingServiceTypeTest.php` | Covered |
| BOOK-04 | Basic/add-on services | service catalog + wizard | PUB-013 | E2E-BOOK-04 | `tests/Feature/Livewire/Public/Booking/BookingWizardStepsFourAndFiveTest.php`<br>`tests/Feature/Domain/Booking/Actions/SaveBookingDraftStepServicesTest.php` | Covered |
| BOOK-05 | Ringkasan | wizard/quote | PUB-014 | E2E-BOOK-05 | `tests/Feature/Livewire/Public/Booking/BookingWizardStepsFourAndFiveTest.php`<br>`tests/Feature/Domain/Booking/BookingDraftQueryTest.php` | Covered |
| BOOK-06 | Data pemesan | wizard | PUB-015 | E2E-BOOK-06 | `tests/Feature/OrderWorkflow/SubmitBookingDraftTest.php` | Covered |
| BOOK-07 | Almarhum + documents | wizard | PUB-016 | E2E-BOOK-07 | `tests/Feature/OrderWorkflow/OrderDocumentTest.php` | Covered |
| BOOK-08 | Payment | wizard/payment contract | PUB-017 | E2E-BOOK-08 | `tests/Feature/Payment/GuardPaymentSessionUpstreamTest.php`<br>`tests/Feature/Payment/GuardPaymentSessionTest.php`<br>`tests/Feature/OrderWorkflow/ApplyPaidEffectsTest.php` | Specified (gated fallback) |
| BOOK-09 | Confirmation/invoice/notification | wizard + notification matrix | PUB-018 | E2E-BOOK-09 | `tests/Feature/OrderWorkflow/OrderReadModelTest.php`<br>`tests/Feature/OrderWorkflow/OrderNotificationTest.php` | Covered |
| MKT-01 | Flower categories | marketplace catalog | PUB-020/021 | E2E-MKT | `tests/Feature/Livewire/Public/Marketplace/MarketplaceIndexRouteTest.php`<br>`tests/Feature/Livewire/Public/Marketplace/ProductDetailRouteTest.php`<br>`tests/Feature/Domain/Marketplace/ProductCatalogueSeedTest.php` | Covered |
| MKT-02 | Gravestone categories | marketplace catalog | PUB-020/021 | E2E-MKT | `tests/Feature/Livewire/Public/Marketplace/MarketplaceIndexRouteTest.php`<br>`tests/Feature/Livewire/Public/Marketplace/ProductDetailRouteTest.php`<br>`tests/Feature/Domain/Marketplace/ProductCatalogueSeedTest.php` | Covered |
| MKT-03 | Care intervals | marketplace catalog | PUB-020/021 | E2E-MKT | `tests/Feature/Livewire/Public/Marketplace/MarketplaceIndexRouteTest.php`<br>`tests/Feature/Livewire/Public/Marketplace/ProductDetailRouteTest.php`<br>`tests/Feature/Domain/Marketplace/ProductCatalogueSeedTest.php` | Covered |
| MKT-04 | Cart/checkout/order-placement/vendor-processing/order-tracking | marketplace spec | PUB-022–024 | E2E-MKT | `tests/Feature/Livewire/Public/Marketplace/CartScreenTest.php`<br>`tests/Feature/Livewire/Public/Marketplace/CheckoutScreenTest.php`<br>`tests/Feature/Livewire/Public/Marketplace/OrderTrackingScreenTest.php`<br>`tests/Feature/Domain/Marketplace/CartTest.php`<br>`tests/Feature/Domain/Marketplace/PlaceMarketplaceOrderTest.php`<br>`tests/Feature/Filament/Vendor/VendorOrderStatusTransitionActionsTest.php` | Covered |
| MKT-05 | Online payment (marketplace) | marketplace spec; `platform-payment-adapter` Task 6 | PUB-023 | E2E-MKT | `tests/Feature/Livewire/Public/Marketplace/CheckoutOnlinePaymentTest.php` | Specified (gated fallback) |
| REN-01 | City | renewal spec | PUB-030 | E2E-REN | `tests/Feature/Livewire/Public/Renewal/RenewalStartTest.php` | Covered |
| REN-02 | TPU/TPS | renewal spec | PUB-030 | E2E-REN | `tests/Feature/Livewire/Public/Renewal/RenewalStartTest.php` | Covered |
| REN-03 | Grave search | renewal spec | PUB-031 | E2E-REN | `tests/Feature/Livewire/Public/Renewal/GraveSearchStatesTest.php`<br>`tests/Feature/Domain/GraveRegistry/GraveRegistryPublicQueryTest.php`<br>`tests/Feature/Domain/GraveRegistry/GraveRecordSeedTest.php` | Covered |
| REN-04 | Fee | renewal spec | PUB-032 | E2E-REN | `tests/Feature/Livewire/Public/Renewal/RenewalFeeTest.php` | Covered |
| REN-05 | Payment | renewal spec | PUB-033 | E2E-REN | `tests/Feature/Livewire/Public/Renewal/RenewalPaymentTest.php`<br>`tests/Feature/Domain/Renewal/GuardRenewalPaymentOpeningTest.php` | Specified (gated fallback) |
| REN-06 | Confirmation/invoice | renewal spec | PUB-034 | E2E-REN | `tests/Feature/Livewire/Public/Renewal/RenewalConfirmationTest.php` | Covered |
| FAQ-01 | Cara memesan | FAQ catalog/spec | PUB-040/041 | E2E-FAQ | `tests/Feature/Livewire/Public/Faq/FaqIndexRouteTest.php`<br>`tests/Feature/Domain/Faq/FaqCategorySeedTest.php` | Covered |
| FAQ-02 | Dokumen | FAQ catalog/spec | PUB-040/041 | E2E-FAQ | `tests/Feature/Livewire/Public/Faq/FaqIndexRouteTest.php`<br>`tests/Feature/Domain/Faq/FaqCategorySeedTest.php` | Covered |
| FAQ-03 | Pembayaran | FAQ catalog/spec | PUB-040/041 | E2E-FAQ | `tests/Feature/Livewire/Public/Faq/FaqIndexRouteTest.php`<br>`tests/Feature/Domain/Faq/FaqCategorySeedTest.php` | Covered |
| FAQ-04 | Perpanjangan | FAQ catalog/spec | PUB-040/041 | E2E-FAQ | `tests/Feature/Livewire/Public/Faq/FaqIndexRouteTest.php`<br>`tests/Feature/Domain/Faq/FaqCategorySeedTest.php` | Covered |
| FAQ-05 | Pembayaran gagal | FAQ catalog/spec | PUB-040/041 | E2E-FAQ | `tests/Feature/Livewire/Public/Faq/FaqIndexRouteTest.php`<br>`tests/Feature/Domain/Faq/FaqCategorySeedTest.php` | Covered |
| FAQ-06 | Customer service | FAQ catalog/spec | PUB-040/041 | E2E-FAQ | `tests/Feature/Livewire/Public/Faq/FaqIndexRouteTest.php`<br>`tests/Feature/Domain/Faq/FaqCategorySeedTest.php` | Covered |
| ADMIN-01 | Admin dashboard modules | `admin-operations` | ADM-* | E2E-ADMIN | `tests/Unit/Domain/OrderWorkflow/OrderTransitionAuthorizerTest.php`<br>`tests/Unit/Platform/ReauthenticationGuardTest.php`<br>`tests/Feature/OrderWorkflow/AdminOperatorActionsTest.php`<br>`tests/Feature/OrderWorkflow/AdminMoneyActionsTest.php`<br>`tests/Feature/Filament/BookingOrderResourceAccessTest.php`<br>`tests/Feature/Filament/BookingOrderTransitionActionTest.php`<br>`tests/Feature/Filament/MarketplaceOrderResourceTest.php`<br>`tests/Feature/Renewal/AdminRenewalActionsTest.php`<br>`tests/Feature/Filament/RenewalOrderResourceTest.php`<br>`tests/Feature/SiteSettings/SettingsServiceTest.php`<br>`tests/Feature/Filament/SiteSettingsResourceTest.php`<br>`tests/Feature/Filament/FeatureGateAdminTest.php`<br>`tests/Feature/Domain/CemeteryDirectory/LaunchCityTest.php`<br>`tests/Feature/Domain/Booking/Actions/SaveBookingDraftStepTest.php`<br>`tests/Feature/Filament/LaunchCityResourceTest.php`<br>`tests/Feature/Filament/ServicePackageResourceTest.php`<br>`tests/Feature/Filament/ProductVariantRelationManagerTest.php`<br>`tests/Feature/Filament/VendorResourceTest.php` | Covered |
| ADM-070 | Payment/transaction/manual verification | `admin-operations` | ADM-070 | E2E-ADMIN | `tests/Feature/Filament/Admin/PaymentVerifications/PaymentVerificationsResourceAccessTest.php`<br>`tests/Feature/Filament/Admin/PaymentVerifications/PaymentVerificationsTableTest.php` | Covered |
| ADM-090 | Reports | `admin-operations` | ADM-090 | E2E-ADMIN | `tests/browser/e2e-admin-vendor.spec.ts` | Covered |
| ADM-100 | Audit and sensitive-action review | `admin-operations` | ADM-100 | E2E-ADMIN | `tests/browser/e2e-admin-vendor.spec.ts` | Covered |
| VENDOR-01 | Vendor dashboard modules | marketplace/vendor spec | VND-* | E2E-VENDOR | `tests/Feature/Filament/Vendor/VendorPanelAccessTest.php`<br>`tests/Feature/Filament/Vendor/VendorPanelScopingTest.php`<br>`tests/Feature/Filament/Vendor/VendorOrderStatusTransitionActionsTest.php` | Covered |
| PLOT-01 | Authoritative plot inventory — cemetery blocks + bulk-generated plots with per-plot state and audited state overrides | `plot-inventory-and-reservation`; P3 design §4.1 | ADM-170, ADM-180 | E2E-ADMIN | `tests/Feature/Domain/PlotInventory/CreateCemeteryBlockTest.php`<br>`tests/Unit/Domain/PlotInventory/CemeteryBlockModelTest.php`<br>`tests/Feature/Filament/PlotInventoryAdminTest.php` | Covered |
| PLOT-02 | Atomic plot reservation — one active hold per plot (plot-row lock + `plot_state` aggregate), order idempotency, append-only lifecycle | `plot-inventory-and-reservation`; P3 design §4.2 | ADM-190 | E2E-ADMIN | `tests/Feature/Domain/PlotReservation/ReservePlotTest.php`<br>`tests/Feature/Domain/PlotReservation/ReservePlotTwoConnectionTest.php`<br>`tests/Feature/Domain/PlotReservation/PlotReservationLifecycleTest.php`<br>`tests/Feature/Domain/PlotReservation/PlotReservationQueryTest.php` | Covered |
| PLOT-03 | Availability decision on the order view — operator reserve/lifecycle actions backed by the authoritative reservation, RKS K24's authoritative half | `plot-inventory-and-reservation`; P3 design §4.3 | ADM-190 | E2E-ADMIN | `tests/Feature/Filament/BookingOrderReservationTest.php` | Covered |
| VISIT-01 | Visitation policy + blackout — per-cemetery weekday operating hours, daily capacity, blackout dates with visitor-visible reason, one policy per cemetery | `visitation-booking` AC1/AC2 | ADM-200 | E2E-VISIT | `tests/Feature/Domain/Visitation/VisitationPolicyTest.php`<br>`tests/Feature/Filament/VisitationAdminTest.php` | Covered |
| VISIT-02 | Atomic capacity-safe booking request — date-ledger row lock, no oversell, blackout/closed-day refusal with reason, idempotent duplicate | `visitation-booking` AC2/AC3/AC7 | PUB-090 | E2E-VISIT | `tests/Feature/Domain/Visitation/RequestVisitationTest.php`<br>`tests/Feature/Domain/Visitation/RequestVisitationTwoConnectionTest.php` | Covered |
| VISIT-03 | Mode-aware public surface — info-only banner vs bookable form (mode from the server-side capability column), blackout dates disabled with reason, confirmation card with reference + fallback contact, one row per submission | `visitation-booking` AC1/AC3/AC5/AC7 | PUB-090 | E2E-VISIT | `tests/Feature/Livewire/Public/Visitation/VisitationPageTest.php` | Covered |
| VISIT-04 | Operator bookings queue — per-cemetery scoped (AC6), confirm/cancel/no-show with audit + `visit.booking_confirmed.v1` outbox on confirm | `visitation-booking` AC6 | ADM-210 | E2E-VISIT | `tests/Feature/Filament/VisitationAdminTest.php` | Covered |
| MEM-01 | Privacy-first memorial profile — private default, consent-evidence-gated editors, lifecycle separate from the grave record (AC7) | `memorial-and-qr` AC1/AC2/AC7 | ADM-220 | E2E-MEM | `tests/Feature/Domain/Memorial/MemorialProfileTest.php` | Covered |
| MEM-02 | Opaque revocable QR tokens — never derived from any identifier (AC4), rotation revokes the old token, one active token per profile | `memorial-and-qr` AC4/AC5 | ADM-220, PUB-092 | E2E-MEM | `tests/Feature/Domain/Memorial/MemorialQrTest.php` | Covered |
| MEM-03 | Public QR resolve — uniform fail-closed state for gate-closed/unknown/revoked/private (no enumeration), allowlist projection for a public profile (or `family_only` for an active editor) | `memorial-and-qr` AC3/AC5 | PUB-091 | E2E-MEM | `tests/Feature/Domain/Memorial/MemorialQrTest.php`<br>`tests/Feature/Livewire/Public/Memorial/MemorialPublicPageTest.php` | Covered |
| MEM-04 | Family management — consent-gated content submit (pending), media upload (quarantine → scan → pending), QR display + rotation, privacy change | `memorial-and-qr` AC1/AC2/AC5 | PUB-092 | E2E-MEM | `tests/Feature/Livewire/Public/Memorial/MemorialPublicPageTest.php` | Covered |
| MEM-05 | Moderation — content lifecycle (pending/approved/rejected/hidden), report intake into open cases, open-first admin queue, resolve/dismiss with required reason | `memorial-and-qr` AC6 | ADM-220, ADM-230 | E2E-MEM | `tests/Feature/Domain/Memorial/MemorialModerationTest.php`<br>`tests/Feature/Filament/MemorialAdminTest.php` | Covered |
| MEM-06 | QR image — deterministic SVG encoding the token URL (endroid/qr-code) | `memorial-and-qr` AC4 | ADM-220, PUB-092 | E2E-MEM | `tests/Unit/Domain/Memorial/MemorialQrImageTest.php` | Covered |
| CER-01 | Agreements & certificates as versioned domain records — issuer-gated issuance/revocation/replacement with audit (AC4), eligibility by rule kept separate from payment status (AC3), history preserved on replace/supersede (AC5), reference unique per issuer+type (AC7), agreement acceptance bound to actor + exact quote/agreement versions (AC2), manual external references recorded without claiming platform issuance (AC8) | `certificates-and-agreements` AC1–AC8 | ADM-240, ADM-241 | E2E-CERT | `tests/Feature/Domain/AgreementCertificate/AgreementTest.php`<br>`tests/Feature/Domain/AgreementCertificate/CertificateTest.php`<br>`tests/Feature/Domain/AgreementCertificate/CertificateEligibilityTest.php` | Covered |
| CER-02 | Admin certificates/agreements surfaces — issuer-gated issuance/revocation/replacement with real vault PDF uploads, role visibility (operator/finance view only), the honest inline eligibility refusal, history-preserving supersede | `certificates-and-agreements` AC4/AC5 | ADM-240, ADM-241 | E2E-CERT | `tests/Feature/Filament/CertificateAdminTest.php` | Covered |
| CER-03 | Customer certificate status view — state only (type/status/version/effective date/issuer role), never the vault reference or document number (AC6), unknown type/id indistinguishable 404 | `certificates-and-agreements` AC6 | PUB-093 | E2E-CERT | `tests/Feature/Livewire/Public/Certificates/CertificateStatusPageTest.php` | Covered |
| PREN-01 | G-LEGAL-01 fail-closed paid flow — every paid action throws the uniform `PreNeedGateClosedException`, no state change, the denial audited `PRENEED_GATE_DENIED`; interest + consultation unaffected while the gate is closed (AC1) | `pre-need-contracting` AC1; negative criteria | ADM-242 | E2E-PREN | `tests/Feature/Domain/PreNeed/PreNeedGateClosedTest.php`<br>`tests/Feature/Domain/PreNeed/RequestPreNeedConsultationTest.php` | Covered |
| PREN-02 | Paid flow with the gate test-opened — proposal → optional reservation → quote → agreement accept (AC5 exact binding) → schedule (AC6 explicit + idempotent) → settlement → eligibility-driven certificate (AC7) → activation linking a NEW At-Need FuneralCase with the original contract history intact (AC8) | `pre-need-contracting` AC3/AC5/AC6/AC7/AC8 | ADM-242 | E2E-PREN | `tests/Feature/Domain/PreNeed/PreNeedPaidFlowTest.php` | Covered |
| PREN-03 | Admin pre-need case resource — the seven paid-flow actions role-gated, the honest gate-closed denial with no state change, schedule creation, per-installment payment links fail-closed under the amount==quote-total guard (FIN-DEC-09), settlement via the manual-verification path | `pre-need-contracting` AC3/AC6 | ADM-242 | E2E-PREN | `tests/Feature/Filament/PreNeedCaseResourceTest.php` | Covered |
| PREN-04 | Public pre-need surface — interest registration + consultation requests persist and audit with the gate closed (AC1), the non-dismissible InterestOnly banner while the gate is closed (absent when open, interest still registers), the certificate-status section state-only | `pre-need-contracting` AC1 | PUB-094 | E2E-PREN | `tests/Feature/Livewire/Public/PreNeed/PreNeedInterestPageTest.php`<br>`tests/Feature/Domain/PreNeed/RequestPreNeedConsultationTest.php` | Covered |
| CARE-SUB-01 | Subscription creation (admin-managed) — subscription lifecycle with care plan reference, frequency, price, status transitions | `recurring-care-subscriptions` AC1/AC2 | ADM-250 | E2E-CARE | `tests/Feature/Domain/CareSubscription/CreateSubscriptionTest.php` | Covered |
| CARE-SUB-02 | Cycle generation (scheduler) — `care:generate-cycles` command finds due ACTIVE subscriptions, generates cycles with idempotent dedup via unique constraint | `recurring-care-subscriptions` AC2/AC5 | — (CLI) | E2E-CARE | `tests/Feature/Console/CareGenerateCyclesCommandTest.php` | Specified |
| CARE-SUB-03 | Payment webhook validation — webhook-driven payment state on subscription cycles, durable signed merchant-scoped replay-protected, never browser-return | `recurring-care-subscriptions` AC4 | — (webhook) | E2E-CARE | `tests/Feature/Payment/CareSubscriptionWebhookSettlementTest.php` | Covered |
| CARE-SUB-04 | Work order creation from paid cycle — `CreateWorkOrderFromCycle` expands checklist template, emits `care.work_order_created.v1` | `grave-care-fulfillment` AC1/AC3 | — (domain) | E2E-CARE | `tests/Feature/Domain/VendorFulfillment/CreateWorkOrderTest.php` | Covered |
| CARE-SUB-05 | Evidence upload (vault-backed) — before/after images through document-vault quarantine, never previewable before scan acceptance | `grave-care-fulfillment` AC4 | VND-050 | E2E-CARE | `tests/Feature/Filament/Vendor/WorkOrderEvidenceUploadTest.php` | Covered |
| CARE-SUB-06 | Customer acceptance/complaint/make-good — acceptance confirms, complaint files with audit, make-good creates replacement linked to original cycle | `grave-care-fulfillment` AC5 | PUB-096 | E2E-CARE | `tests/Feature/Livewire/Public/CareSubscription/CareHistoryPageTest.php` | Specified |
| CARE-SUB-07 | Vendor replacement audit — replace with reason captured and audited | `grave-care-fulfillment` AC7 | VND-050 | E2E-CARE | `tests/Feature/Filament/Admin/WorkOrderVendorReplacementTest.php` | Covered |

## C. Gate interpretation

`Specified (gated fallback)` means the user-facing step and outcome are required even when an external capability is inactive. A closed gate cannot be used to remove Step 8, hide the feature silently, or report a false success.

## E. Cross-cutting screens — no RKS-derived expectation ID

Screens required by a cross-cutting UX principle rather than a stakeholder-workflow item in section A. Same `Covered` bar as section B: a named test must exist, be read against the claim, and pass in CI.

| ID | Expectation | Canonical doc | Screen | Test evidence | Status |
|---|---|---|---|---|---|
| SUPPORT-01 | Customer-service escape hatch reachable from every page | `product-brief.md` §5.10; `information-architecture.md` §2; `design-system.md` §6.10 | PUB-060 — `/bantuan` | `tests/Feature/Livewire/Public/Support/HelpCentreRouteTest.php`<br>`tests/Feature/Livewire/Public/Legal/FooterLegalLinksRouteTest.php` | Covered |

**Evidence, read against the claim, not the filename.** `HelpCentreRouteTest` asserts: the route resolves at the expected name/URI; `GET /bantuan` returns 200; the page states the `ContactInfo` channels, the operating hours, and the emergency disclaimer *above* the channel list (a safety-ordering assertion); it does not claim 24/7 availability or an SLA; it admits the channels are placeholders, not a live line; it renders without any `wire:*` binding or `<form>`, satisfying §6.10's "must work with JS disabled"; and it links onward to `/faq` and `/`. `FooterLegalLinksRouteTest`'s companion method confirms `/privasi` still links `/bantuan` and that following the link now reaches this page rather than a 404. Both files ran and passed together in CI: GitHub Actions run `31237318086`, job **PHP (validate, lint, analyse, test)** (id `93052168835`), commit `97dfbbf`.

**Ownership, not coverage, is what remains open.** No feature spec's `requirements.md` claims PUB-060 — see `.kiro/specs/help-centre-missing-route/design.md`'s closing note. That is a spec-authoring decision for a human, not a defect this row can resolve by itself.

| AKUN-01 | Login (`/masuk`) — no-enumeration error, rate limit + `Lockout` event, remember-me cookie, `ActorContextResolver::forget()` after guard mutation | plan-mode session (no owning spec); PR #112 | PUB-097 | `tests/Feature/Livewire/Public/Auth/LoginPageTest.php` | Covered |
| AKUN-02 | Registration (`/daftar`) — auto-authenticates, rate limit, zero `ActorRole` grants on the new account (no panel access) | plan-mode session; PR #112 | PUB-098 | `tests/Feature/Livewire/Public/Auth/RegisterPageTest.php` | Covered |
| AKUN-03 | Password reset (`/lupa-password`, `/reset-password/{token}`) — identical confirmation for known/unknown email, no auto-login after reset, `remember_token` rotated on reset | plan-mode session; PR #112 | PUB-099, PUB-100 | `tests/Feature/Livewire/Public/Auth/PasswordResetTest.php` | Covered |
| AKUN-04 | Account shell (`/akun`) — guest redirected with intended URL preserved, four tiles with per-user-scoped counts | plan-mode session; PR #113, PR #114 | PUB-101 | `tests/Feature/Livewire/Public/Akun/AkunIndexRouteTest.php`<br>`tests/Feature/Livewire/Public/Akun/AkunIndexTest.php` | Covered |
| AKUN-05 | Draft resume (`/akun/draft`) — own open drafts only, submitted drafts excluded, most-recent-first; resume by session binding **or** proven authenticated ownership | plan-mode session; PR #113 | PUB-102 | `tests/Feature/Livewire/Public/Akun/DraftListTest.php`<br>`tests/Feature/Livewire/Public/Booking/BookingWizardDraftBindingTest.php` | Covered |
| AKUN-06 | Order list (`/akun/pesanan`) — own orders only via `order_parties.user_id`, most-recent-first, status badge resolved through `StatusIntent` (not a hardcoded label) | plan-mode session; PR #114 | PUB-103 | `tests/Feature/Livewire/Public/Akun/OrderListTest.php`<br>`tests/Feature/Domain/OrderWorkflow/OrderForUserScopeTest.php` | Covered |
| AKUN-07 | Renewal (akun) gate-closed page — honest "not yet available" over `<x-mk.gate-closed-page>`, never a raw 403/404 | plan-mode session; PR #113 | PUB-104 | `tests/Feature/Livewire/Public/Akun/DeferredSubPagesTest.php` | Covered |
| AKUN-08 | Document (akun) gate-closed page — same honest pattern as AKUN-07 | plan-mode session; PR #113 | PUB-105 | `tests/Feature/Livewire/Public/Akun/DeferredSubPagesTest.php` | Covered |

**Evidence, read against the claim, not the filename — all eight verified by reading each named
test file's method list directly on 20 Aug 2026, not trusted from a prior summary.**
`LoginPageTest`'s `test_unknown_email_shows_the_exact_same_generic_error_as_a_wrong_password` is
the no-enumeration proof for AKUN-01; `test_a_stale_cached_actor_context_is_forgotten_after_a_successful_login`
is the regression test for the exact `ActorContextResolver::forget()` gotcha that class's own doc
block had flagged as future work. `PasswordResetTest`'s
`test_unknown_email_shows_the_identical_generic_confirmation_and_sends_nothing` does a
byte-for-byte HTML comparison, not a message-text check, and
`test_a_successful_reset_rotates_the_remember_token` proves a stolen remember-me cookie stops
working the moment an account holder resets their password — the actual security property, not
just "the form submitted." `RegisterPageTest`'s `test_newly_registered_user_has_no_panel_access`
confirms zero `ActorRole` grants on a fresh account, not merely that a row was inserted.
`AkunIndexTest`'s `test_the_open_draft_count_is_scoped_to_the_viewing_user_only` and
`test_the_order_tile_count_is_scoped_to_the_viewing_user_only` are the discriminating tests —
each creates a *second* user's data and asserts it is excluded, not just that the viewing user's
own data appears. `BookingWizardDraftBindingTest`'s
`test_an_authenticated_owner_resumes_their_own_draft_via_mount_after_losing_the_session_secret`
and its `..._via_save_step_one_...` sibling exercise the two distinct call sites
(`BookingWizard::mount()` vs `currentOrNewDraft()`) separately, and
`test_an_authenticated_non_owner_cannot_resume_someone_elses_draft_via_mount` (+ its
`..._via_save_step_one` sibling) prove the ownership rescue fails closed for a real, different
authenticated user, not just a guest. `OrderForUserScopeTest`'s
`test_an_order_belonging_to_a_different_user_is_not_returned` is the equivalent discriminating
test for the order scope, and `OrderListTest`'s
`test_a_real_booking_submission_by_an_authenticated_customer_is_visible_on_the_order_list` is a
genuine end-to-end proof through the real `StartBookingDraft` → `SubmitBookingDraft` pipeline, not
a hand-built fixture row — the only test of that shape in this file so far.
`DeferredSubPagesTest`'s four methods confirm both gate-closed pages return 200 with a working
fallback link for an authenticated user and redirect guests to login, covering AKUN-07/08
together (one test file, two screens, same pattern).

**What these rows do NOT claim.** None of the eight cite an `E2E-*` browser suite as anything
more than an aspirational label — the same caveat this file's §1 states for every other row: all
evidence here is server-side HTTP/Livewire feature tests, never a real browser walking the actual
rendered page. AKUN-06 does not claim a "Lihat detail" per-order link exists — its own test file
and component doc block name `PUB-050` explicitly and say why the link is deliberately absent
(the detail route doesn't exist; `/marketplace/pesanan/{orderNumber}` is a different order
concept entirely). AKUN-05's ownership-rescue behavior is a genuine, recorded reversal of a prior
security ruling — see `BookingDraftBinding`'s own doc block, updated in the same PR that added
this capability, and the v0.15 revision note above — not a silent widening of access.

## D. Evidence trail for the `Covered` rows

Written 08 Aug 2026 (finding T-A) by reading each test file, not its filename. Test-method names below are the real ones; if a rename breaks this trail, the trail is what is wrong, not the row.

### HOME-01…HOME-04 — the four primary homepage menus

All four rows rest on `tests/Feature/Livewire/Public/HomePageRouteTest.php`, which drives real HTTP requests against `/` with real seeded data.

- **Shared, covers all four rows:** `test_all_four_menus_appear_in_ac1s_exact_order` asserts that "Pemesanan Makam", "Layanan Pemakaman", "Perpanjangan Makam", and "FAQ" all appear, and appear in that order, scoped from `<body>` onward so the page `<title>` cannot satisfy the match. `test_viewing_the_homepage_records_menu_impressions_without_sensitive_data` asserts exactly four impression rows with `menu_key` `['pemesanan', 'layanan', 'perpanjangan', 'faq']` — a per-menu assertion, not an aggregate one. `test_homepage_returns_ok` asserts PUB-001 renders at all.
- **HOME-01** additionally: `test_pemesanan_makam_is_the_primary_call_to_action` (`Pesan Makam` CTA plus `href="/pemesanan-makam"`) and `test_pemesanan_makam_stub_route_returns_ok_not_404`.
- **HOME-02** additionally: `test_marketplace_stub_route_returns_ok_not_404` — `/marketplace`, the `layananHref` target in `resources/views/components/mk/header.blade.php`, returns 200.
- **HOME-03** additionally: `test_perpanjangan_stub_route_returns_ok_not_404` — `/perpanjangan` returns 200.
- **HOME-04** additionally: `test_faq_highlights_link_into_the_real_faq_routes` asserts the homepage links to the real `faq.index` and `faq.show` routes, and `test_faq_highlights_degrade_gracefully_when_the_faq_query_fails` asserts the homepage still renders the four menus when the FAQ table is dropped underneath it.

**Scope of the claim.** These rows claim the *menu* exists on PUB-001, in the mandated order, and leads to a route that answers 200. They do **not** claim the destination is built: `/pemesanan-makam`, `/marketplace`, and `/perpanjangan` are explanatory "Segera Hadir" stubs, which is why every BOOK-*, MKT-*, and REN-* row below stays `Specified`.

### FAQ-01…FAQ-06 — the six mandated FAQ categories

Each row is one of the six categories `AGENTS.md` §FAQ requires be seeded and preserved.

- **Every row, category identity:** `tests/Feature/Domain/Faq/FaqCategorySeedTest.php::test_exactly_six_categories_are_seeded` and `::test_categories_have_the_exact_catalogue_codes_and_labels_in_order` assert exactly six categories with the exact codes, display labels, and sort order from `docs/product/faq-catalog.md` — `CARA_MEMESAN` (FAQ-01), `DOKUMEN` (FAQ-02), `PEMBAYARAN` (FAQ-03), `PERPANJANGAN` (FAQ-04), `PEMBAYARAN_GAGAL` (FAQ-05), `CUSTOMER_SERVICE` (FAQ-06). `::test_find_by_code_resolves_every_known_code` covers each code individually.
- **Every row, public reachability (PUB-040):** `tests/Feature/Livewire/Public/Faq/FaqIndexRouteTest.php::test_category_chip_links_never_include_the_real_seeded_draft_articles_category_as_leaking_its_content` loops all six category slugs, asserting each returns 200 and none leaks the seeded draft. `::test_faq_index_returns_ok_and_lists_seeded_published_articles` covers `/faq` itself.
- **Every row, content presence:** `tests/Feature/Domain/Faq/FaqArticleSeedTest.php::test_each_category_has_its_exact_catalogue_question_count` is data-provided per category and asserts the exact `faq-catalog.md` article count for each of the six (4/3/4/4/4/4), not merely "more than zero".
- **Per-row extras:** FAQ-01 — `FaqIndexRouteTest::test_faq_index_returns_ok_and_lists_seeded_published_articles` and `tests/Feature/Livewire/Public/Faq/FaqArticleDetailRouteTest.php::test_a_real_seeded_published_article_renders_title_body_updated_date_and_cs_cta` (PUB-041, article `bagaimana-cara-memesan-makam`). FAQ-02 — `FaqIndexRouteTest::test_faq_category_route_narrows_the_list_to_that_category_only` and `::test_empty_category_state_renders_when_a_category_has_zero_published_articles`, both against `dokumen`. FAQ-03 — `::test_search_finds_matching_published_articles_across_categories` surfaces the Pembayaran article `Kapan invoice diterbitkan?`. FAQ-06 — `::test_search_with_no_results_shows_related_categories_and_support_path_not_a_bare_empty_state` asserts the Customer Service category and `Hubungi Customer Service` / `/bantuan` path.
- **`AGENTS.md` §FAQ "draft/unpublished articles must never be public"**, which every FAQ row inherits: `tests/Feature/Domain/Faq/FaqArticleDraftExclusionTest.php` plus `FaqIndexRouteTest::test_search_never_returns_the_real_seeded_draft_article`, `::test_a_freshly_created_draft_and_unpublished_article_never_appear_on_the_index`, and `FaqArticleDetailRouteTest::test_the_real_seeded_draft_article_slug_404s_not_a_gated_or_hidden_page`.

**Scope of the claim.** FAQ-04 and FAQ-05 rest on the shared per-category evidence only (identity, exact article count, route 200, no draft leakage) — there is no test asserting the *wording* of any individual Perpanjangan or Pembayaran Gagal answer. That is coverage of what the row states (the category is present and publicly reachable with its catalogue content seeded), not of answer correctness, which no automated test in this repository checks for any category.

### BOOK-02, MKT-01…MKT-03, REN-01…REN-03 — raised to `Covered` 08 Aug 2026 (finding T-J)

Sprint 4 batches S4-T6 (cemetery directory), S4-T7 (renewal skeleton), and S4-T8 (marketplace browse) landed on 08 Aug 2026. All three ran together and passed in **GitHub Actions run [`31248602859`](https://github.com/andrianm28/makam-app/actions/runs/31248602859)**, job **PHP (validate, lint, analyse, test)** (id `93081219154`), on commit `a150a3b455230f504f2a7006e27a5a9e77363ae7`, the tip of `docs/design-system-and-planning`.

**On citing one cumulative run rather than three.** The per-batch commits `5f1c2f8`, `604dd1f`, and `c50122a` each went **red** in CI; the greens are `31240015162` (after the directory test fix, commit `4773dfb`) and `31248602859` (after a cross-batch FK-ordering fix, commit `a150a3b`). Only `31248602859` has all three batches in the tree simultaneously and green, so it is the run cited throughout. Reporting "each batch was CI-green" would be false.

- **BOOK-02 (TPU/TPS detail).** PUB-011 shipped standalone at `/pemakaman` and `/pemakaman/{cemeterySlug}` — see the reconciliation note in `docs/product/screen-inventory.md`. `CemeteryDetailRouteTest::test_the_detail_page_shows_every_ac3_field` and `CemeteryDirectoryIndexRouteTest::test_a_card_shows_every_ac3_field` both enumerate AC3's full field list (type, name, photo, address, maps URL, facilities, attributed price range, availability) rather than spot-checking one field, which is what makes them evidence for "TPU/TPS detail" rather than merely "the page returns 200". Availability honesty is asserted as an *absence*: `test_no_success_intent_badge_renders_while_every_cemetery_is_indicative` and `test_an_available_package_does_not_render_as_success_while_the_cemetery_is_indicative`. Not-found handling is asserted as indistinguishability, not just a 404 — `test_the_draft_and_unknown_slug_responses_are_indistinguishable`. `PublicCapabilityProjectionTest` backs the negative criterion "no public exposure of restricted plot data" structurally (a `ReflectionClass` walk confirming the projection has no property for a restricted mode) and the route tests confirm it in rendered output.
  **Scope of the claim:** this row claims the cemetery directory list and detail exist and show AC3's fields. It does **not** claim booking Step 2 shows them. *Corrected 09 Aug 2026:* the wizard embed is no longer unbuilt — S4-T4 resumed and Step 2 now lists cemeteries and offers a package/class picker — but it renders name, type, and packages only, not AC3's photo/address/Maps URL/facilities/price-range set. The embed therefore still contributes nothing to this row.
- **MKT-01, MKT-02, MKT-03 (flower categories, gravestone categories, care intervals).** All three rest on the same per-category evidence, which is why they rise together. `MarketplaceIndexRouteTest::test_every_known_category_key_filters_to_exactly_its_own_products` iterates every category key and asserts each filters to exactly its own products — *exactly*, so a category leaking a foreign product fails. `ProductDetailRouteTest::test_every_seeded_product_code_has_a_reachable_detail_page` iterates all nine codes; neither test hardcodes a name list, both read the enum. `ProductCatalogueSeedTest` re-parses the live `docs/product/marketplace-catalog.md` at test time and asserts agreement, so catalogue drift fails the build. MKT-02 additionally: `::test_a_gravestone_product_shows_its_seeded_variants` and `::test_the_calligraphy_product_shows_its_calligraphy_style_and_inscription_example`.
  **Scope of the claim:** these rows claim the three categories and their products are publicly browsable from the canonical catalogue. They do **not** claim AC2 completeness — schedule, service area, delivery fee, stock/availability, and evidence requirement have no column on `products` or `product_variants` (verified 08 Aug 2026) — nor any cart, checkout, or vendor behaviour, which was MKT-04 and stayed `Specified`. **Corrected 20 Aug 2026 (v0.17):** that clause is stale — MKT-04 (cart/checkout/order-placement/vendor-processing/order-tracking) is raised to `Covered` and MKT-05 (online payment specifically) is added as `Specified (gated fallback)`; see the section D entry below.
- **REN-01 (City), REN-02 (TPU/TPS).** Both on PUB-030, `/perpanjangan`. `RenewalStartTest::test_all_five_launch_cities_are_offered_in_the_canonical_order` asserts all five *and* their order. The load-bearing assertion for this journey is the negative one — `::test_a_city_with_no_published_cemetery_is_still_offered`, paired with `::test_a_city_with_no_published_cemetery_renders_a_three_part_empty_state` — because silently omitting an MVP city is the spec's named defect. REN-02: `::test_selecting_a_city_lists_its_published_cemeteries` and `::test_a_draft_cemetery_is_never_offered`. Robustness: `::test_an_unknown_city_code_is_discarded_rather_than_404ing`, `::test_select_city_refuses_a_code_outside_the_launch_list`, `::test_a_failed_cemetery_read_degrades_honestly_instead_of_500ing`.
- **REN-03 (Grave search).** PUB-031, `/perpanjangan/cari`. The claim is that grave search exists and behaves honestly, and the evidence is shaped as denials, which is what makes it real coverage: `GraveSearchStatesTest::test_the_privacy_limited_state_never_says_the_record_was_not_found`, `::test_the_privacy_limited_state_discloses_no_withheld_name`, `::test_the_no_result_state_is_not_confused_with_the_other_two`, `::test_the_gate_closed_state_never_implies_the_record_does_not_exist`, `::test_a_search_backend_failure_is_never_reported_as_not_found`, and `::test_a_blank_submission_is_a_validation_error_not_a_no_result`. Privacy and scoping at the query layer: `GraveRegistryPublicQueryTest::test_a_closed_record_is_still_counted_and_never_reported_as_not_found`, `::test_a_search_never_reaches_a_record_in_another_cemetery`, `::test_a_search_with_no_terms_returns_nothing_and_never_dumps_the_registry`, `::test_no_access_mode_can_project_heir_contact_because_the_projection_has_no_such_property`, `::test_the_query_never_returns_an_eloquent_model`. Fuzzy matching: `::test_a_name_term_matches_regardless_of_case_and_punctuation` and `GraveRecordTrigramSearchTest::test_a_misspelled_name_still_finds_the_record`.
  **Scope of the claim, and one explicit gap.** This row claims grave search exists and returns honest results. It does **not** claim the renewal spec's **AC4 (< 500 ms at 100,000 records)**, which is **NOT TESTED and not passing** — nothing in the repository measures search latency and no 100k-row fixture exists. `GraveRecordTrigramSearchTest`'s only `assertLessThan` bounds a *similarity score*, not a duration; it must not be read as a benchmark. **Correction, 22 Aug 2026:** AC4 now has a real benchmark — `php artisan bench:grave-search` (`BenchGraveSearchCommand`) measures real p50/p95/p99 in CI against the largest generated cemetery's ~1,000 records (`docs/testing/release-gates.md` §H); this is not yet a flat 100,000-record certification, which stays deferred to Phase 3.

### BOOK-06…BOOK-09 — order-orchestration domain evidence (13 Aug 2026; raised to `Covered` in v0.9)

**Scope of this entry — the domain half only.** These rows are stakeholder-workflow
expectations for the full wizard step. The evidence below covers the **domain**
half (the server-side behavior `booking-and-order-orchestration` owns); the
**presentation** half — PUB-015…PUB-018 screens, the stepper, field components —
is `public-booking-wizard`'s (L6) and is not asserted here. A row's way-station
status therefore claimed the *domain* behavior had tests that pass locally; it
did not claim the step was rendered. **No row was raised to `Covered` at the time
of writing** (unmerged branch, no CI run); *v0.9 (14 Aug 2026) raises BOOK-06,
BOOK-07, BOOK-09 on merge + green CI against exactly the evidence below —
BOOK-08 stays `Specified (gated fallback)`.*

- **BOOK-06 (Data pemesan), domain.** `SubmitBookingDraftTest` walks real order
  parties through submission and asserts the party is persisted under the order.
- **BOOK-07 (Almarhum + documents), domain.** `OrderDocumentTest::` proves the
  purpose-scoped vault composition: a quarantined document is never
  previewable/downloadable/thumbnailed, signed URLs expire within 300 s, every
  access writes an audit row, and a document bound to order A is unreachable
  from order B. It does **not** claim the wizard's Step-7 upload UI.
- **BOOK-08 (Payment), domain.** `GuardPaymentSessionUpstreamTest` /
  `GuardPaymentSessionTest` prove conditions 2–5 evaluate against real order,
  quote, and authorization records (`DomainDenied`, not `UnavailableUpstream`),
  and that the guard still denies on condition 6 (`FIN-DEC-01`). `ApplyPaidEffectsTest`
  proves the paid effects path — webhook and manual verification each apply
  exactly once, the cross-path case (manual then webhook) yields one status
  event and one outbox row via `order_status_events_paid_once`, and the amount/
  currency preconditions throw before any write. **The row stays `Specified
  (gated fallback)`**: Step 8's manual-coordination fallback is required and the
  online gate stays closed while `FIN-DEC-01` is `TBD`.
- **BOOK-09 (Confirmation/invoice/notification), domain.** `OrderReadModelTest`
  proves Step 9 content server-side — order reference, `StatusIntent`-resolved
  status, invoice state as a read-model state only (`FIN-DEC-02` `TBD`, no
  invoice produced), channel-delivery state, next action, support reference —
  and the AC12 fallback-reachability assertion. `OrderNotificationTest` proves
  dispatch per the matrix (Order processing, Order completed) with no channel
  state overclaim.

**PostgreSQL 18 note.** The exactly-once mechanism BOOK-08's evidence rests on
(`order_status_events_paid_once`) and the status CHECK constraints were verified
on a disposable real PG18 container in the same batch (Task 10), along with the
two-connection sequential re-read that the hermetic SQLite suite cannot execute.

### REN-04, REN-05, REN-06 — raised to `Covered` 14 Aug 2026 (L8)

Renewal steps 4–6 shipped with L8 on 12 Aug 2026 and merged 13 Aug 2026 (PR #29). The rows rest on their screens' own tests, each read against the claim below.

- **REN-04 (Fee), PUB-032.** `tests/Feature/Livewire/Public/Renewal/RenewalFeeTest.php` — `test_the_fee_screen_always_shows_the_tariff_source_and_last_update` and `test_the_fee_screen_shows_the_renewal_amount` assert the row's "source, last updated" states; `test_no_late_fine_figure_is_rendered_when_there_is_no_written_basis` asserts the G-RATE-01 honesty; `test_rendering_the_fee_screen_writes_nothing` pins the read-only contract; `test_accepting_the_quote_creates_exactly_one_renewal_and_redirects` covers the journey's only write; `test_a_closed_record_shows_the_privacy_limited_state_and_no_grave_fields` / `::test_a_limited_record_shows_the_privacy_limited_state_and_no_identity` / `test_a_restricted_record_cannot_be_renewed_by_calling_the_action_directly` assert the AC14 projection on this surface.
- **REN-05 (Payment), PUB-033.** *Stays `Specified (gated fallback)`* — the online-payment gate is closed, so the manual-coordination path is the required state, exactly what `RenewalPaymentTest::test_the_payment_screen_shows_manual_coordination_when_gate_is_closed` and `::test_the_payment_step_is_never_removed_when_gate_is_closed` pin. `GuardRenewalPaymentOpeningTest` asserts the five guard conditions — expired quote, amount mismatch, unaccepted quote, closed gate → manual required, closed grave access mode — and `RenewalPaymentTest::test_a_denial_never_prints_the_guards_specific_reason` covers the not-leaking side.
- **REN-06 (Confirmation/invoice), PUB-034.** `RenewalConfirmationTest::test_the_confirmation_shows_reference_status_and_invoice_state` asserts the row's "invoice and due-date result"; `test_reloading_the_confirmation_never_creates_a_second_renewal` asserts the exactly-one-renewal invariant; `::test_an_unsettled_renewal_does_not_claim_a_new_due_date` and `::test_a_settled_renewal_shows_the_new_due_date` hold the due-date honesty; `test_support_escape_hatch_is_present` covers the support state.

### VENDOR-01 — raised to `Covered` 14 Aug 2026 (L10)

The vendor panel shipped 13 Aug 2026 (PR #30, `lane/l10-vendor-portal`) as a Filament panel at `/vendor` (`VendorPanelProvider`), with resources for listings, service areas, availabilities, and orders plus pages for evidence, transaction history, and payout status. The tests below assert the portal's hard properties; they do not assert the pixel content of every page.

- **Access control.** `tests/Feature/Filament/Vendor/VendorPanelAccessTest.php` — `test_a_vendor_with_an_active_grant_reaches_the_panel`, `::test_a_vendor_role_without_a_grant_is_refused`, `::test_a_grant_without_the_vendor_role_is_refused`, `::test_a_user_with_neither_is_refused`, `::test_every_vendor_route_is_behind_the_same_check`, `::test_a_granted_vendor_can_open_every_surface`.
- **Query-level scope.** `VendorPanelScopingTest::test_surface_returns_only_the_granted_vendors_rows` (data-provided over every surface), `::test_surface_is_closed_for_an_actor_with_no_vendor_grant`, `::test_surface_is_closed_for_a_guest`, `::test_revoking_the_grant_closes_the_surfaces_again`, plus the structural `::test_every_resource_in_the_panel_applies_the_vendor_scope` and `::test_every_table_page_in_the_panel_applies_the_vendor_scope` walks.
- **Order workflow.** `VendorOrderStatusTransitionActionsTest::test_the_action_transitions_status_audits_and_notifies` — the transition actions are what implement `AGENTS.md`'s "Paid does not mean completed" — and `::test_each_action_is_hidden_once_its_target_status_is_already_set`; `VendorOrderEditFormTest::test_submitting_the_edit_form_audits_the_status_change_via_the_domain_action` and `::test_submitting_an_out_of_list_status_fails_validation_without_writing`.
- **Stamping.** `VendorCreateStampingTest` / `VendorEditStampingTest` prove a forged vendor id cannot attach a record to another vendor.

**Scope of the claim.** The row claims the vendor dashboard module set exists and is vendor-scoped. It does **not** claim evidence for the *content* of the `TransactionHistory` and `PayoutStatus` pages beyond their wiring and scope. *Corrected 15 Aug 2026 (v0.10):* the clause "the admin side of marketplace orders stays unbuilt (MKT-04 remains `Specified`)" is withdrawn — the admin side shipped with P1 on 15 Aug 2026 (`MarketplaceOrderResource`). MKT-04 itself remains `Specified`, because its claim is the **public** checkout/payment/vendor-processing journey (PUB-022–024), which is still unbuilt; the admin surface is claimed by ADMIN-01 instead. *Corrected 20 Aug 2026 (v0.17):* the preceding sentence is also stale — the public checkout/payment/vendor-processing journey shipped and is tested; MKT-04 is raised to `Covered` (cart/checkout/order-placement/vendor-processing/order-tracking) and MKT-05 is added `Specified (gated fallback)` for online payment specifically, which remains refused by `OpenPaymentSession` for `OrderType::Marketplace`. See the section D entry below.

### BOOK-01, BOOK-03, BOOK-04, BOOK-05 — booking wizard evidence (09 Aug 2026; raised to `Covered` in v0.9)

The booking wizard's Steps 1–5 were built 08–09 Aug 2026 on branch `booking-wizard-steps-1-5` (a worktree off `docs/design-system-and-planning`) and reviewed whole-branch. **There is no CI run for this work**, unlike the 08 Aug rows above which cite run [`31248602859`](https://github.com/andrianm28/makam-app/actions/runs/31248602859). The booking test surface is green locally (109 tests, 317 assertions, `vendor/bin/phpunit tests/Unit/Domain/Booking/ tests/Feature/Domain/Booking/ tests/Feature/Livewire/Public/Booking/`, 09 Aug 2026); a local green is explicitly **not** this repository's oracle (`sprint-plan.md` §Sprint 4, `CLAUDE.md`), which is why these four rows stop short of `Covered`.

- **BOOK-01 (Lima kota awal), PUB-010.** `BookingWizardRouteTest::test_step_1_offers_all_five_launch_cities_in_order` asserts all five on the wizard's own Step 1 *and* their relative order in the rendered markup, which is what the July/August notes said was missing — the row's Screen cell (PUB-010) no longer has to borrow PUB-011's or PUB-030's evidence. `::test_no_draft_is_created_merely_by_viewing_the_page` and `::test_selecting_a_city_creates_a_draft_and_redirects_to_its_resume_url` back the "first meaningful input" contract around it.
- **BOOK-03 (Empat jenis layanan), PUB-012.** `BookingWizardEndToEndTest::test_step_3_shows_product_labels_not_raw_enum_codes` iterates all four `BookingServiceType` values and asserts each renders under its `mvp-scope.md` label (Makam Baru, Makam Tumpang, Urgent, Pre-Need) as the button's own visible copy, while the machine code survives only in the `wire:click` payload. `BookingServiceTypeTest::test_labels_match_the_canonical_mvp_scope_wording` pins that wording against the canonical document so a rename fails the build.
  **Scope of the claim:** four types are offered and persisted. It does **not** claim PUB-012's `conditional` or `gated` states — no Makam Tumpang precondition and no Urgent/Pre-Need gate wiring exists (deliberately out of scope; `BookingServiceType`'s own doc block records why).
- **BOOK-04 (Basic/add-on services), PUB-013.** `BookingWizardStepsFourAndFiveTest::test_step_4_offers_every_basic_and_additional_service` iterates `ServiceCode::KNOWN_CODES` — the enum, not a hardcoded list — and asserts each is the submitted value of a real checkbox; `::test_step_4_labels_each_service_with_its_real_catalogue_name` asserts the visible label is the catalogue's own `name`. The mandatory half is enforced as a denial in the domain layer: `SaveBookingDraftStepServicesTest::test_step_4_rejects_a_selection_missing_document_processing` and `::test_step_4_rejects_a_selection_missing_grave_digging`, plus `::test_step_4_rejects_an_unknown_service_code` and `::test_step_4_rejects_the_same_service_code_twice`.
  **Scope of the claim:** the 12 catalogue services are selectable and validated. It does **not** claim PUB-013's `unavailable item` state, nor `booking-wizard-fields.md` §Step 4's per-line description/provider/price/availability/quantity — Step 4 renders name and code only, and quantity is fixed at 1.
- **BOOK-05 (Ringkasan), PUB-014.** `BookingWizardStepsFourAndFiveTest::test_step_5_shows_the_real_computed_total_from_seeded_prices` asserts real seeded prices and their sum render, and asserts the *absence* of the degraded message so a fabricated fallback cannot pass. The honest-degradation half is `BookingDraftQueryTest::test_summary_marks_a_missing_price_honestly_instead_of_fabricating_a_total` (which supersedes a seeded price first, so the state is real, not mocked) and `::test_summary_renders_a_row_with_an_unusable_quantity_as_unpriced_rather_than_guessing_a_total`.
  **Scope of the claim:** a summary of the selected services with a computed total, or an honest "harga belum tersedia". It does **not** claim PUB-014's `changed price` or `expired quote` states, nor `booking-wizard-fields.md` §Step 5's city/cemetery/service-type/package/schedule/fee/exclusions lines — there is no persisted Quote row (AC8 is out of scope for this batch), so there is nothing to version, expire, or reconfirm.

**Rows adjacent to this work that did NOT move.** BOOK-02 stays where it was (see its scope correction above — the wizard embed contributes nothing to it). *Corrected 14 Aug 2026 (v0.9):* the second half of this paragraph is withdrawn — Steps 6–9 shipped with L6/L7 (PRs #27/#28), BOOK-06/07/09 moved to `Covered`, and the tests cited here were rewritten with the steps: `BookingWizardStepTest`'s boundary test is now `test_all_nine_steps_are_the_last_implemented_boundary` (asserting `LAST_IMPLEMENTED = CONFIRMATION = 9`), and `SaveBookingDraftStepTest`'s batch-boundary rejection (`test_a_step_beyond_this_batchs_boundary_is_rejected`) was superseded when the boundary dissolved — `SaveBookingDraftStepSteps678Test.php` now covers the step 6–8 writes directly.

### ADMIN-01 — raised to `Covered` 15 Aug 2026 (P1, PRs #60–#64), widened 16 Aug 2026 (P2, PRs #69–#72)

The admin order-management module set shipped 15 Aug 2026: the order transition authorizer + re-authentication guard (PR #60), the operator and money-adjacent domain Actions (PR #61), and the three order resources — `BookingOrderResource` (PR #62), `MarketplaceOrderResource` (PR #63), `RenewalOrderResource` (PR #64). The evidence below is read against the row's claim (the `admin-operations` dashboard module set) and against the P1 design's own section titles — role split, operator actions, resources, money-adjacent re-auth, audit-every-transition, append-only — per the "read against the claim, not the filename" rule.

- **Role split.** `OrderTransitionAuthorizerTest` — `test_operator_can_run_non_money_transition`, `test_finance_cannot_run_plain_operator_transition`, `test_finance_can_run_money_transition`, `test_operator_cannot_run_money_transition`, `test_admin_can_run_everything`, `test_restricted_admin_cannot_issue_quote`, `test_restricted_admin_can_verify_order`, `test_guest_is_denied`.
- **Money-adjacent re-auth.** `ReauthenticationGuardTest` — `test_null_last_authentication_fails_closed`, `test_recent_authentication_passes`, `test_stale_authentication_fails`, `test_boundary_of_freshness_window_passes`.
- **Operator actions (transition + audit + outbox).** `AdminOperatorActionsTest` — verify → DIVERIFIKASI, request availability → MENUNGGU_KETERSEDIAAN, issue quote composed from the draft (refuses an order without one), buyer approval → DISETUJUI_PEMESAN, process/complete, and `test_illegal_edge_from_matrix_is_rejected` (the matrix guard denies the illegal edge); each transition asserts the `order_status_events` row, the `audit_events` row, and the `outbox_events` row.
- **Money-adjacent actions.** `AdminMoneyActionsTest` — reject requires a reason and transitions with one, cancel/expire, `test_grant_payment_opening_creates_order_scope_grant_and_transitions` (ORDER-scope `ScopeAssignment` + MENUNGGU_PEMBAYARAN + `ORDER_PAYMENT_OPENING_AUTHORIZED` audit), `test_manual_payment_verification_records_note` (note carried as event/audit reason + `ORDER_MANUAL_PAYMENT_VERIFICATION_STARTED` audit), `test_mark_order_paid_reaches_dibayar_and_stamps_source` (paid_via `manual_verification`, source `manual:user:1`, outbox event), `test_mark_order_paid_without_quote_throws`.
- **Booking resource.** `BookingOrderResourceAccessTest` — guest/bare user denied, the four back-office roles admitted, vendor denied. `BookingOrderTransitionActionTest` — `test_operator_can_invoke_verify_transition`, `test_operator_cannot_invoke_money_transition`, `test_finance_money_transition_is_authorized`.
- **Marketplace resource.** `MarketplaceOrderResourceTest` — `test_operator_can_access_marketplace_resource`, `test_operator_cannot_run_mark_paid_action`, `test_finance_can_run_mark_paid_action`.
- **Renewal resource.** `AdminRenewalActionsTest` — `test_mark_paid_externally_records_evidence` (the `RenewalExternalMarking` evidence row), `test_mark_paid_refuses_settled_renewal`, `test_expire_transitions_to_kedaluwarsa`. `RenewalOrderResourceTest` — access matrix, `test_operator_cannot_run_record_external_payment_action` / `test_finance_can_run_record_external_payment_action` (finance + re-auth), `test_operator_can_run_expire_action` + `::test_operator_calling_expire_action_transitions_the_renewal` (operator expiry).

**P2 block — admin data management (16 Aug 2026, PRs #69–#72).** The P2 module set shipped with four file-disjoint lanes merged in history order #71 (packages/variants), #72 (vendors), #69 (settings/gates), #70 (launch cities). The evidence below is read against the P2 design's own section titles (§4.4 settings, §4.5 gates, §4.6 launch cities, §4.1 packages, §4.2 variants, §4.3 vendors) and the row's widened claim: the six surfaces exist, are role-gated (four back-office roles admitted, vendor denied), and every write is audited.

- **Site settings.** `SettingsServiceTest` — `test_config_overrides_env`, `test_env_is_consulted_before_db`, `test_db_value_used_when_no_env`, `test_default_when_nothing_configured`: one test per link of the config → env → DB → default fallback chain (§4.4). `SiteSettingsResourceTest` — `test_back_office_roles_can_access` / `test_vendor_role_cannot_access`: the four back-office roles admitted, vendor denied.
- **Feature-gate operations.** `FeatureGateAdminTest` — `test_admin_can_see_gates` (list reads the rows), `test_admin_with_fresh_reauthentication_can_transition_a_gate` (evidence + recorder + audit), `test_missing_evidence_surfaces_an_error_and_does_not_change_state` (fail without state change), `test_a_stale_actor_is_redirected_to_the_challenge_instead_of_transitioning` (re-auth lapse → MFA challenge, fail closed), `test_a_non_admin_back_office_role_cannot_transition_a_gate` (view for the four roles, transitions admin-only), plus the access pair `test_back_office_roles_can_access` / `test_vendor_role_cannot_access` (§4.5).
- **Launch cities.** `LaunchCityTest` — `test_the_canonical_five_are_seeded_with_the_table` (AGENTS.md baseline as seed), `test_active_cities_ordered_by_sort_order`, `test_is_known_reads_table_then_constants`, `test_launch_cities_fall_back_to_the_canonical_constants_when_the_table_is_empty`, `test_active_cities_degrade_to_the_canonical_fallback_when_the_read_fails` (the public seam §4.6). `SaveBookingDraftStepTest` (the action-level city tests) — `test_step_1_accepts_a_known_launch_city`, `test_step_1_accepts_an_admin_added_launch_city` (the product-approved extension beyond the canonical five passes the wizard seam), `test_step_1_rejects_an_unknown_city_code`, `test_step_1_rejects_a_missing_city_code_with_a_field_keyed_error`. `LaunchCityResourceTest` — `test_the_four_back_office_roles_can_access` / `test_vendor_role_cannot_access` / `test_guests_and_bare_users_are_denied`, `test_an_authorized_admin_can_create_a_launch_city_with_an_audit_trail` (+ update/delete twins), `test_a_duplicate_code_fails_create_validation`, `test_a_lowercase_code_fails_create_validation`, `test_move_down_swaps_sort_orders_with_the_next_city_and_audits_the_swap` + `::test_move_up_swaps_sort_orders_with_the_previous_city` (reorder), `test_deleting_a_launch_city_referenced_by_a_booking_draft_is_honest` (delete blocked while referenced), `test_deactivating_a_launch_city_removes_it_from_the_active_city_query`.
- **Service packages.** `ServicePackageResourceTest` — access matrix (`test_the_four_back_office_roles_can_access_the_service_package_resource`, guest/bare denied), `test_an_authorized_admin_can_create_a_package_with_one_item_through_the_list_page_action` + `test_define_action_lands_a_draft_version_one_with_items_and_an_audit_row` (create routes through `DefineServicePackage`), `test_publishing_a_draft_marks_it_published_and_writes_an_audit_row` + `test_publishing_a_version_with_zero_items_is_rejected` (publication only via the domain action, ≥1 item), `test_revise_creates_version_two_as_a_draft_copy_and_writes_an_audit_row`, `test_published_versions_items_are_immutable_and_direct_writes_throw` + `test_the_versions_relation_manager_hides_publish_for_published_rows` (published rows immutable), `test_an_authorized_admin_can_add_an_item_to_the_draft_version_from_the_items_relation_manager` + `::test_an_authorized_admin_can_edit_an_item_on_the_draft_version_with_an_audit_row` (draft-only item writes), `test_deleting_a_package_that_still_has_a_published_version_is_refused_honestly` + `test_a_package_with_only_draft_versions_can_be_deleted_with_an_audit_row`.
- **Product variants.** `ProductVariantRelationManagerTest` — `test_variant_create_on_a_gravestone_product_succeeds_with_an_audit_row`, `test_variant_edit_records_an_audit_row`, `test_variant_create_on_a_non_variant_product_throws_the_models_invariant` (owning product must have variant axes), `test_a_customer_cannot_interact_with_the_variants_relation_manager`.
- **Vendor administration.** `VendorResourceTest` — `test_the_four_back_office_roles_can_access_the_vendor_resource` (+ guest / bare-customer denied), `test_an_authorized_admin_can_create_a_vendor_with_an_audit_trail` + update twin, `test_members_relation_manager_adds_a_member_with_an_audit_row` + `test_members_relation_manager_revokes_a_member_without_deleting_the_row` (add/revoke, no hard delete), `test_listings_relation_manager_creates_a_listing_with_real_closed_list_values` (AvailabilityMode/EvidenceRequirement from the closed lists), `test_availability_relation_manager_creates_a_schedule_day_with_an_audit_row`, `test_deleting_a_vendor_with_members_is_honest` + `test_deleting_a_vendor_without_members_or_listings_succeeds` (delete blocked while members/listings exist).

**Scope of the claim.** *Corrected 16 Aug 2026 (v0.11):* the row now claims the admin dashboard's order-management **and data-management** module sets — the P1 order resources (list/view, transitions through domain Actions, money-adjacent re-auth, audit + append-only timeline) plus the P2 surfaces (site settings, feature-gate operations with evidence + re-auth, launch cities with seed/reorder/reference-blocked delete, service packages with versioned publish/revise, product variants, vendor administration). It does **not** claim reports (ADM-090) or the audit-review surface (ADM-100), which remain unbuilt; nor does it claim the master-data/FAQ module set beyond what the already-named master-data/FAQ tests evidence. Two-phase-journey scope qualifier: the row does not claim that a generic buyer passes the P0 payment guard — condition 4 (`AuthorizeOrderPaymentOpening`) requires the ADMIN role plus an ORDER-scope grant under the acting identity, so the re-click that passes it is the granted admin's own; a buyer/customer role cannot pass until a product decision admits one (deferred).

### ADM-090, ADM-100 — raised to `Covered` 24 Aug 2026 (v0.18)

Both rows were added `Specified`, no evidence, in v0.16 (20 Aug 2026) as an honesty correction — ADMIN-01's own `Covered` status never claimed them. PR #142 (merged 22 Aug 2026) closed the gap with real browser coverage in `tests/browser/e2e-admin-vendor.spec.ts`, confirmed by reading the file directly rather than trusting `docs/testing/release-gates.md`'s own citation of it.

- **ADM-090 (Reports).** `'the three master-data-gated report pages are reachable and titled correctly'` asserts real 200 responses and headings for `/admin/laporan-pesanan`, `/admin/laporan-periode-perpanjangan`, `/admin/laporan-kinerja-vendor`. `'the three finance-gated report pages are reachable and titled correctly for the admin holding ledger-read access'` does the same for `/admin/laporan-keuangan`, `/admin/laporan-kwitansi`, `/admin/laporan-pembayaran-keluar`. Together these are all 6 report pages the `admin-operations` spec names.
- **ADM-100 (Audit and sensitive-action review).** `'audit trail review shows the required columns and is read-only'` asserts the real column set (`Waktu`, `Aksi`, `Hasil`, `Aktor`, `Peran aktor`, `Sumber`, `Subjek`, `Alasan`) and that no create/new action exists on the page. `'audit trail records this suite's own seeded grants'` proves the trail is real, not a stub, by asserting the suite's own seeded `ROLE_GRANT` audit rows actually appear in it.

**Scope of the claim.** These two rows claim the reports and audit-review surfaces exist, are role-gated, and render their real data at the browser level. They do **not** claim Feature/Unit-level coverage of either surface — no such tests exist for these two surfaces specifically — and they do **not** claim anything about CARE-SUB-02 through CARE-SUB-07, which stay exactly as they were; `docs/testing/release-gates.md`'s §A-35 box separately names those as open, substantive evidence gaps, unaffected by this correction.

### PLOT-01, PLOT-02, PLOT-03 — raised to `Covered` 16 Aug 2026 (P3, PRs #77/#78/#79)

The P3 module set shipped 16 Aug 2026: lane 1 (PR #77) the inventory half — `CemeteryBlock` + `GravePlot` models, `CreateCemeteryBlock` bulk generation, `BlocksRelationManager` under `CemeteryResource`, `GravePlotsResource` with the three state-override row actions; lane 2 (PR #78) the reservation core — append-only `PlotReservation`, `ReservePlot` (plot-row lock + `plot_state` aggregate), the lifecycle actions, the two-connection concurrency test; lane 3 (PR #79) the order-view integration — `ReservePlotAction` + `PlotReservationLifecycleActions` + the 'Reservasi' infolist section on `ViewBookingOrder`. The evidence below is read against the P3 design's own section titles (§4.1 inventory, §4.2 reservation, §4.3 booking integration) per the "read against the claim, not the filename" rule.

- **PLOT-01 (inventory).** `CreateCemeteryBlockTest` — `test_creates_block_and_generates_capacity_plots` (block + `capacity` plots with slots `001..N`, every plot `available`, both audit rows `CEMETERY_BLOCK_CREATED` + `GRAVE_PLOTS_GENERATED`), `test_code_is_normalized_to_uppercase` (the code guard), `test_capacity_must_be_positive` (capacity ≥ 1), `test_class_link_is_applied_to_generated_plots` (the optional package link lands on each generated plot). `CemeteryBlockModelTest` — `test_plot_delete_blocked_when_not_available` (the model's honest delete refusal) and `test_plot_state_must_be_known` (the `PlotState::KNOWN_STATES` booted guard). `PlotInventoryAdminTest` — the access matrices (`test_grave_plots_resource_access_matrix`, `test_blocks_relation_manager_access_matrix`, `test_a_customer_cannot_interact_with_the_blocks_relation_manager`), the create path (`test_block_create_via_the_relation_manager_generates_plots_and_audit_rows`), the state overrides with their per-state visibility (`test_mark_occupied_flips_the_state_and_audits`, `test_maintenance_then_available_override_round_trips_with_audits`, `test_mark_available_is_only_offered_for_maintenance_or_occupied_plots`, `test_mark_available_is_not_offered_for_available_or_reserved_plots`), the plot-override re-auth invariant (`test_state_override_requires_recent_reauthentication`), and the no-delete surface (`test_plot_delete_is_blocked_and_no_delete_action_is_offered`).
- **PLOT-02 (reservation).** `ReservePlotTest` — `test_reserves_an_available_plot` (plot → reserved, HELD row, audit + outbox `plot_reservation.state_changed.v1`), `test_order_idempotency_returns_incumbent` (idempotent per order via `activeForOrder()`), `test_occupied_plot_is_refused` (the state-assert refusal), `test_reserved_plot_without_reservation_row_is_refused` (the state-assert path — a plot manually flipped to `reserved` with no row is refused, which is the invariant the partial-unique-index backstop was originally written for), `test_plot_can_be_reserved_again_after_expire` (the lifecycle regression the index made impossible — held → expire → re-hold the same plot, chain preserved append-only). `ReservePlotTwoConnectionTest` — `test_a_second_reservation_is_refused_after_the_first_commits` (driver-guarded for PostgreSQL: the plot-row `lockForUpdate()` serialization re-read sees `reserved` and refuses — the concurrency backstop). `PlotReservationLifecycleTest` — `test_confirm_keeps_plot_reserved` (held → confirmed, plot stays reserved), `test_release_restores_availability`, `test_expire_restores_availability` (release/expire flip the plot back), `test_terminal_reservation_refuses_further_transitions` + `test_second_transition_on_same_chain_is_refused` (terminal/late-state honest refusals). `PlotReservationQueryTest` — `test_released_head_returns_null`, `test_expired_head_returns_null` (a released/expired order has no active reservation — the filter-before-order bug the head-row semantics exist to prevent), `test_confirmed_head_is_returned_by_identity`, `test_reopened_chain_returns_the_new_held_head_by_identity` + `test_same_plot_can_be_reheld_after_expiry_and_is_the_new_head` (latest-row semantics on the append-only chain), `test_active_for_plot_mirrors_the_head_semantics`.
- **PLOT-03 (availability decision).** `BookingOrderReservationTest` — `test_reserve_action_is_visible_at_menunggu_ketersediaan` + `test_reserve_action_is_hidden_outside_the_reservable_statuses` (order-state render gate: DIVERIFIKASI/MENUNGGU_KETERSEDIAAN only), `test_reserve_action_is_hidden_when_the_draft_cemetery_does_not_resolve` (the visibility falls shut when the draft's cemetery cannot be resolved), `test_operator_reserves_an_available_plot_from_the_order_view` (action → `ReservePlot` → reservation row + plot state), `test_reserve_action_options_are_filtered_by_the_draft_package_class` (the §4.3 package/class filter), `test_finance_role_is_denied_the_reserve_action` + `test_operator_restricted_admin_and_admin_roles_are_allowed_the_reserve_action` + `test_finance_role_is_denied_the_lifecycle_actions` (operator/restricted_admin/admin admitted, finance denied — the role split), `test_lifecycle_actions_follow_the_active_reservation_state` (Konfirmasi/Lepaskan/Kedaluwarsakan per-edge visibility from the active reservation), `test_expire_restores_plot_availability_and_reopens_reservation` (expiry → plot available → reserve action offered again).

**Scope of the claim.** These rows claim the authoritative half of RKS K24 (Availability): an authoritative per-plot inventory with audited state overrides (PLOT-01), atomic reservation with one active hold per plot and an append-only lifecycle (PLOT-02), and the operator-facing availability decision on the order view backed by that reservation (PLOT-03). They do **not** claim buyer-side specific-plot selection in the wizard, auto-expiry on a scheduler (expiry is operator-on-demand), occupancy beyond the plot state, or the public projection of plot data — all explicitly out of scope in the P3 design (§3); the public side of K24 remains the package/class default with manual confirmation, unchanged.

### VISIT-01…VISIT-04, MEM-01…MEM-06 — raised to `Covered` 16 Aug 2026 (P4, PRs #81–#84)

The P4 module set shipped 16 Aug 2026: lane 1 (PR #81) the visitation domain — `CemeteryVisitationPolicy` (weekday hours template + daily capacity), `VisitationBlackoutDate` (required reason), `VisitationDateCapacity` (per-date booked ledger), `RequestVisitation` (date-ledger row lock, no oversell, idempotent, audit + outbox); lane 2 (PR #83) the surfaces — `VisitationPage` (`/kunjungan`, mode-aware per `PublicCapabilityProjection::forCemetery()->visitationMode`) and `CemeteryVisitationPolicyResource` + `VisitationBookingsResource`; lane 3 (PR #82) the memorial domain — `MemorialProfile` (private default), `MemorialEditor` (consent evidence), `MemorialContent`/`MemorialMedia` (moderated), `MemorialQrToken` (opaque, partial-unique active token), `ModerationCase`/`AbuseReport`, `ResolveMemorialQr` (gate-checked), publish/unpublish/rotate actions with outbox; lane 4 (PR #84) the surfaces — `MemorialPublicPage` (`/m/{token}`), `MemorialFamilyPage` (`/kenangan/{profileId}`), `MemorialProfileResource` + `ModerationCaseResource`, `MemorialQrImage` SVG. The evidence below is read against each row's claim, per the "read against the claim, not the filename" rule.

- **VISIT-01 (policy + blackout).** `VisitationPolicyTest` — `test_rejects_an_unknown_weekday_key` / `::test_rejects_a_malformed_open_time` / `::test_rejects_a_malformed_close_time` / `::test_rejects_a_non_null_weekday_entry_without_open_and_close` (the allowlist key + time-format guards), `test_rejects_daily_capacity_below_one` (capacity ≥ 1), `test_accepts_a_closed_weekday_key_as_null`, `test_is_visiting_day_and_time_helpers_follow_the_weekday_template` (the hour-template semantics the page renders from), `test_blackout_semantics_are_policy_scoped_and_carry_the_reason` (scoped to the owning policy, reason carried), `test_blackout_reason_is_required_non_blank` (required visitor-visible reason), `test_bookable_dates_excludes_blackouts_and_reports_capacity_left_from_the_ledger` + `::test_bookable_dates_is_empty_for_a_cemetery_without_a_policy` (the public slot-source contract). Admin half: `VisitationAdminTest::test_creating_a_policy_for_a_cemetery_writes_the_policy_and_audits`, `::test_a_cemetery_cannot_get_a_second_policy` (one policy per cemetery), `::test_editing_a_policy_updates_the_hours_and_audits`, `::test_blackout_create_requires_a_reason`, `::test_blackout_create_with_a_reason_writes_the_row_and_audits`, `::test_blackout_delete_writes_the_row_removal_and_audits` (ADM-200's write surface).
- **VISIT-02 (capacity-safe request).** `RequestVisitationTest` — `test_requests_a_visit_and_audits_the_request` (happy path: booking row + audit + `visit.booking_requested.v1` outbox), `test_refuses_when_capacity_is_exhausted_and_rolls_back_everything` (no oversell — refusal rolls back booking/count/audit/outbox together), `test_refuses_a_blackout_date_with_the_surfaced_reason` + `test_refuses_a_date_on_a_closed_weekday` (specific surfaced reasons, never a bare "tidak tersedia"), `test_a_repeated_idempotency_key_returns_the_incumbent_without_a_second_row` (AC7 — one row, incumbent returned), `test_two_bookings_for_the_same_date_share_one_ledger_row` (the ledger is per-date, not per-booking), `test_refuses_a_cemetery_without_a_policy`. The two-connection half is PG-guarded: `RequestVisitationTwoConnectionTest::test_a_second_booking_after_the_first_commits_is_refused_without_oversell` (the date-ledger `lockForUpdate` serialization — the no-oversell concurrency backstop, driver-guarded for PostgreSQL).
- **VISIT-03 (mode-aware public surface).** `VisitationPageTest` — `test_the_index_route_renders_a_cemetery_picker` (PUB-090's slug-less picker), `test_an_information_only_cemetery_renders_the_hours_banner_and_no_form` + `::test_an_information_only_cemetery_without_a_policy_renders_the_generic_hours_line` + `test_a_none_mode_cemetery_renders_no_bookable_controls` (the hard mode distinction — information-only/NONE render zero bookable controls), `test_a_bookable_cemetery_renders_the_form_with_blackout_dates_disabled_and_their_reason` (blackouts visible, disabled, reason-carrying), `test_a_bookable_cemetery_with_no_policy_renders_an_explained_empty_state` (empty ≠ 500), `test_submitting_the_form_creates_a_booking_and_shows_the_confirmation_card` (AC5: reference + instructions + change/cancel note + fallback contact), `test_a_repeated_submission_returns_the_incumbent_confirmation_and_never_a_second_row` (AC7 at the page layer), `test_an_unknown_slug_returns_404` + `::test_an_unpublished_cemetery_returns_the_same_404_as_an_unknown_slug` (404 indistinguishability).
- **VISIT-04 (operator bookings queue).** `VisitationAdminTest` — `test_the_policy_resource_access_matrix` / `test_the_bookings_resource_access_matrix` / `test_the_blackout_relation_manager_access_matrix` (the four back-office roles admitted, others denied), `test_a_cemetery_granted_operator_sees_only_their_cemetery_bookings` + `test_an_admin_sees_bookings_from_all_cemeteries` (AC6 per-cemetery scoping via cemetery grants; admins/ungranted see all), `test_confirm_transitions_to_confirmed_and_emits_the_outbox_event` (confirm → `visit.booking_confirmed.v1` + `VISITATION_STATUS_CHANGED` audit), `test_cancel_is_allowed_from_requested_and_confirmed`, `test_no_show_is_allowed_only_from_requested` (the per-edge action map), `test_a_late_transition_is_refused_honestly_without_a_write` (terminal/late-state honest refusal).
- **MEM-01 (privacy-first profile).** `MemorialProfileTest` — `test_create_defaults_to_private_and_records_audit_and_outbox` (AC1's private default + `memorial.profile_created.v1`), `test_explicit_privacy_mode_is_honoured` + `test_unknown_privacy_mode_is_rejected` (AC2's closed mode list), `test_second_create_for_the_same_grave_returns_the_incumbent` (one profile per grave), `test_profile_with_an_editor_cannot_be_deleted` / `::test_profile_with_content_cannot_be_deleted` / `::test_profile_with_media_cannot_be_deleted` / `::test_profile_with_a_qr_token_cannot_be_deleted` / `test_empty_profile_can_be_deleted` (the honest delete refusal set), `test_editor_grant_without_consent_evidence_is_refused` + `test_editor_grant_with_consent_evidence_succeeds_and_is_audited` (AC1's consent evidence), `test_revoked_editor_can_be_granted_again` + `test_grant_for_an_already_active_editor_returns_the_incumbent` (revoke is `revoked_at`, never hard-delete), `test_publish_records_published_at_audit_and_outbox` + `::test_publish_when_already_published_is_an_idempotent_noop`, `test_unpublish_is_immediate_and_emits_the_pre_catalogued_event` + `::test_unpublish_when_not_published_is_an_idempotent_noop` (AC5's immediate unpublish emitting `memorial.unpublished.v1`). AC7's boundary is structural: the profile holds `grave_record_id` as its only GraveRegistry link and copies no grave-record field — asserted by `MemorialQrTest::test_projection_never_carries_private_fields`'s allowlist walk and the domain-model §9 rule.
- **MEM-02 (QR tokens).** `MemorialQrTest` — `test_tokens_are_random_and_never_derived_from_any_identifier` (AC4: random opaque; a never-derived test over a generation run, plus the doc-blocked equivalence reasoning), `test_rotate_revokes_the_old_token_and_issues_a_new_active_one` (AC5 rotation: revoke + mint, old token dead), `test_second_active_token_for_the_same_profile_is_rejected_by_the_partial_unique_index` (one active token per profile).
- **MEM-03 (public resolve).** `MemorialQrTest` — `test_closed_gate_throws_the_uniform_not_visible_exception_before_any_lookup` (the gate-closed branch performs **no token lookup** — the kiro design's no-enumeration rule), `test_unknown_token_throws_the_same_uniform_exception_as_gate_closed`, `test_private_profile_resolves_with_the_same_uniform_exception`, `test_closed_unknown_and_privacy_denials_are_the_same_exception_class` (the AC5 negative criterion is carried as a class-identity assertion, not just three separate cases), `test_privacy_matrix_for_token_holders` (family_only/unlisted/public × holder), `test_resolve_returns_the_allowlisted_projection_for_a_public_profile` + `test_projection_never_carries_private_fields` (AC3 allowlist). Page layer: `MemorialPublicPageTest` — `test_gate_closed_renders_the_uniform_state_even_for_a_valid_token` / `::test_unknown_token_renders_the_same_uniform_state` / `::test_revoked_token_renders_the_same_uniform_state` / `::test_private_profile_without_an_editor_renders_the_same_uniform_state` (four render-level identities of the one uniform state), `test_the_because_privacy_message_never_appears_in_the_html` (the review watch: privacy detail is debug-log-only), `test_public_profile_renders_only_the_allowlist`, `test_family_only_profile_renders_for_an_active_editor`, `test_the_gate_is_re_checked_on_render` (a mid-session gate close falls back immediately).
- **MEM-04 (family management).** `MemorialPublicPageTest` (the family half of the same file) — `test_family_page_denies_a_non_editor_with_the_uniform_state` + `::test_family_page_denies_an_unknown_profile_with_the_uniform_state` (consent gate, no existence leak), `test_family_editor_submits_content_into_pending` (submit → pending), `test_family_editor_uploads_media_into_quarantine` (vault quarantine-first; the model's creating guard only admits accepted documents), `test_an_accepted_upload_attaches_as_pending_media_after_scan` (attach-on-render sync), `test_family_editor_sees_the_qr_and_rotates_the_token` (display + rotate), `test_family_privacy_change_is_audited_and_takes_effect`.
- **MEM-05 (moderation + reports).** `MemorialModerationTest` — `test_submitted_content_starts_pending`, `test_moderation_approves_and_emits_audit_and_outbox` (approve → `memorial.content_moderated.v1`), `test_moderation_can_reject_or_hide_content`, `test_moderating_back_to_pending_is_refused` + `test_approved_content_can_be_moderated_again` (the per-state map and its honest refusals), `test_reporting_content_creates_an_open_case_with_an_abuse_report` + `test_report_without_a_reason_is_refused` (AC6 report intake), `test_media_requires_a_document_that_exists_and_is_accepted` (fail-closed media attach). Admin half: `MemorialAdminTest` — `test_both_memorial_resources_fail_closed_outside_the_back_office_roles` (access matrix), `test_publish_is_role_gated_so_finance_cannot_publish` + `test_an_operator_publishes_with_audit` (moderator-backed publish), `test_editor_grant_without_consent_evidence_is_refused` + `test_grant_action_refuses_a_blank_evidence_reference` (the grant backstop), `test_content_moderation_flow_approves_and_records_audit_and_outbox`, `test_moderation_case_resolves_with_reason_and_audit` + `test_moderation_case_dismissal_requires_a_reason` (resolve/dismiss via the domain action with reason), `test_cemetery_scoping_limits_an_operator_to_their_grants` (per-cemetery scope), `test_the_queue_orders_open_cases_first_in_the_table_html` (open-first ordering asserted against rendered HTML), `test_the_unique_media_attach_backstop_refuses_a_duplicate` (the (profile, storage_ref) index classifier).
- **MEM-06 (QR image).** `MemorialQrImageTest` — `test_svg_returns_a_well_formed_svg_document`, `test_svg_is_deterministic_for_the_same_payload`, `test_distinct_payloads_render_distinct_svgs`, `test_the_payload_length_carries_into_the_output_size`. The endroid/qr-code SVG writer embeds the payload only as encoded matrix data (no text/title attribute), so the unit asserts well-formedness/determinism/distinctness instead of a literal string — documented in the test and the class doc block.

**Scope of the claim.** These rows claim the visitation module (policy/blackout, capacity-safe idempotent requests, the mode-aware public page, the per-cemetery operator queue) and the memorial module (privacy-first profile + consent-gated editors, opaque revocable QR tokens, the uniform-fail-closed public resolve, the consent-gated family surface, moderation + reports + the open-first admin queue, the SVG QR image) as built and tested with the gates as shipped: visitation mode from the per-cemetery capability column (no code reads `G-VISIT-01`; the flag stays registry-level), memorial public resolve fail-closed while `G-MEM-01` is closed (activation via `GateActivationRecorder` is the documented path, not a claim here). They do **not** claim AC8's deletion/retention policy (no approved policy document exists — the memorial spec's own "explicitly not covered"), scheduled/auto no-show detection (operator-marked), notification delivery for confirmed visits beyond the outbox emission (no channel dispatch is wired to the visitation events), or any memorial surface behavior after `G-MEM-01` opens beyond the allowlist projection the tests exercise directly.

### CER-01…CER-03, PREN-01…PREN-04 — raised to `Covered` 16 Aug 2026 (P5a, PRs #86/#88/#89/#90)

The P5a module set shipped 16 Aug 2026: lane 1 (PR #86) the agreements+certificates domain — `Agreement`/`Certificate`/`ExternalCertificateReference` models, `CertificateEligibilityPolicy` rule objects, the `CreateAgreement`/`AcceptAgreement`/`SupersedeAgreement`/`IssueCertificate`/`RevokeCertificate`/`ReplaceCertificate` actions, `CertificateStatusView`; lane 1b (PR #88) the admin surfaces — `CertificatesResource`, `AgreementsResource`, and the customer `CertificateStatusPage` (`/sertifikat/{subjectType}/{subjectId}`); lane 2/2b (PR #89) the pre-need paid flow — `PreNeedCase` + the seven paid-flow actions (each G-LEGAL-01-gated via `ModeResolver::preNeedMode()`) + `PreNeedCasesResource`; lane 3 (PR #90) the public `/preneed` surface. The evidence below is read against each row's claim per the "read against the claim, not the filename" rule.

- **CER-01 (versioned domain records).** `AgreementTest` — `test_create_records_a_draft_row_with_audit` (stable reference + type + version + audit), `test_accept_binds_actor_quote_and_exact_agreement_version` (AC2 binding), `test_accept_refuses_when_the_version_is_not_the_exact_row` (the honest refusal — a caller cannot bind a different version), `test_supersede_preserves_the_old_row_and_creates_the_next_version` (AC5), `test_delete_is_blocked_while_a_certificate_references_the_agreement` + `::test_delete_is_blocked_for_a_non_draft_agreement`. `CertificateTest` — `test_issue_records_reference_status_document_audit_and_outbox` (AC1 row shape + `CERTIFICATE_ISSUED` + `certificate.issued.v1`), `test_issue_allows_the_restricted_admin_role` / `::test_issue_refuses_an_operator_role_and_writes_nothing` (AC4 issuer role gate), `test_issue_refuses_when_eligibility_is_not_met_and_writes_nothing` (AC3 gate before write), `test_issue_refuses_a_document_that_is_not_accepted` (vault Accepted-only), `test_revoke_records_status_and_audit_with_a_reason` + `::test_revoke_requires_a_non_blank_reason`, `test_replace_preserves_history_and_issues_the_next_version` (AC5), `test_a_reference_collision_for_issuer_and_type_is_classified` (AC7 unique per issuer+type), `test_an_external_reference_is_recorded_and_flagged_external` (AC8), `test_status_view_exposes_state_only_never_the_document_id_or_reference` (the AC6 projection pin). `CertificateEligibilityTest` — `test_order_settlement_rule_allows_a_paid_order` / `::test_order_settlement_rule_refuses_an_unpaid_order` (eligibility via `Order::status()`, not a payment-status column — AC3), `test_an_unknown_certificate_type_has_no_rule_and_is_refused`, `test_a_subject_that_is_not_an_order_is_refused`, `test_pre_need_settlement_rule_allows_a_settled_pre_need_case` / `::test_pre_need_settlement_rule_refuses_an_unsatisfied_case` / `::test_pre_need_settlement_rule_refuses_a_non_case_subject`.
- **CER-02 (admin surfaces).** `CertificateAdminTest` — `test_both_resources_fail_closed_outside_the_back_office_roles` + `::test_back_office_roles_access_both_resources` (the four back-office roles admitted, others denied), `test_an_issuer_issues_a_certificate_through_the_resource_with_a_real_vault_upload` (issue through the resource with a genuine vault document), `test_operator_and_finance_never_see_the_issuer_actions` + `test_an_operator_wire_call_on_the_issue_action_is_refused` (role visibility AND the wire-call backstop), `test_the_issue_action_is_refused_after_the_issuer_role_is_revoked_mid_session` (re-check on render), `test_issuance_refuses_an_ineligible_subject_with_an_honest_inline_error` (eligibility surfaced as an inline error, not a 500), `test_an_issuer_revokes_through_the_resource_with_a_reason` + `::test_revoke_requires_a_reason`, `test_an_issuer_replaces_through_the_resource_preserving_history` (AC5 at the surface), `test_an_agreement_can_be_accepted_through_the_resource_binding_the_exact_version` + `::test_an_agreement_can_be_superseded_through_the_resource_preserving_history` (AC2/AC5 at the surface).
- **CER-03 (customer status view).** `CertificateStatusPageTest` — `test_the_status_page_renders_state_only_and_never_the_vault_reference_or_document_number` (the AC6 render-level denial — the template reads only the projected rows), `test_the_status_page_renders_the_empty_state_for_a_subject_without_certificates`, `test_an_unknown_subject_id_404s` + `::test_an_unknown_subject_type_404s` (indistinguishable 404s, no enumeration).
- **PREN-01 (fail-closed paid flow).** `PreNeedGateClosedTest` — `test_every_paid_action_throws_the_uniform_exception_audits_the_denial_and_changes_no_state` (the seven actions against the same `PreNeedGateClosedException`, one `PRENEED_GATE_DENIED` audit per attempt, zero writes), `test_interest_registration_is_unaffected_while_the_gate_is_closed` (AC1's interest flow survives). `RequestPreNeedConsultationTest` — `test_a_consultation_request_persists_and_audits`, `test_a_consultation_request_can_be_linked_to_a_registered_interest`, `test_the_consultation_is_gate_independent_in_both_modes` (the consultation flow never gated).
- **PREN-02 (paid flow, gate test-opened).** `PreNeedPaidFlowTest` — `test_the_full_paid_flow_from_interest_to_activation` (proposal → reservation → quote → accept with AC5 exact binding → schedule → settle → activate: a NEW At-Need `FuneralCase` exists and the case keeps its agreement/quote/reservation history — AC8), `test_the_reservation_optional_branch_quotes_a_proposal_without_a_reservation` (the plan-gap-fill: proposal→quoted without the optional P3 reservation), `test_scheduling_requires_a_bound_quote_to_denominate_the_installments` (schedule refuses without a bound quote), `test_an_illegal_transition_is_refused_without_state_change` (per-edge honest refusal).
- **PREN-03 (admin case resource).** `PreNeedCaseResourceTest` — `test_guests_and_bare_users_are_denied` / `::test_the_four_back_office_roles_can_access` / `::test_vendor_role_cannot_access` (access matrix), `test_operator_cannot_authorize_a_contract_step` / `::test_admin_can_authorize_a_contract_step` (contract-step role split), `test_operator_cannot_authorize_a_money_step` / `::test_finance_can_authorize_money_steps` (money-step role split + re-auth), `test_gate_open_propose_via_the_page_transitions_the_case_to_proposal`, `test_gate_closed_propose_surfaces_the_honest_notification_and_changes_no_state` (the fail-closed denial at the surface — notification, zero writes), `test_accept_agreement_binds_the_cases_own_quote_and_the_same_lane_1_agreement` (the Task 4 review's quote-identity fix), `test_schedule_action_creates_the_installments`, `test_settlement_via_the_verified_payment_path` (order walked to DIBAYAR via the manual-verification path, `SettlePreNeed` records `settled_paid_source_ref`), `test_a_per_installment_payment_link_never_opens_a_session_for_an_installment_amount` (the fail-closed per-installment link — no `payment_sessions` row, the installment untouched; the FIN-DEC-09 capture).
- **PREN-04 (public pre-need surface).** `PreNeedInterestPageTest` — `test_interest_registers_and_confirms_with_the_gate_closed` (AC1), `test_the_interest_only_info_banner_shows_while_the_gate_is_closed` (the §6.9 non-dismissible banner), `test_the_banner_is_absent_and_interest_still_registers_when_the_gate_is_open` (gate-resolved render), `test_a_consultation_request_persists_from_the_form` + `::test_a_consultation_filed_after_interest_registration_is_linked_to_it` (the form's consultation half), `test_the_certificate_status_section_renders_state_only` (the in-page AC6 section).

**Scope of the claim.** These rows claim the agreements+certificates module (versioned, eligibility-driven, issuer-gated, history-preserving records with the AC6 state-only customer view) and the pre-need module (the paid flow fully built but fail-closed while `G-LEGAL-01` is closed, with interest + consultation never gated) as built and tested with the gates as shipped: issuance is issuer-role-gated and eligibility comes from `CertificateEligibilityPolicy` rules (never payment status directly); the paid-flow actions throw the uniform `PreNeedGateClosedException` while the gate is closed; the per-installment payment-link action is fail-closed under the payment guard's amount==quote-total invariant (no partial payment — FIN-DEC-09 awaits a human decision); MVP settlement is the manual-verification path. They do **not** claim the public paid-flow UI (deferred — the domain is ready), auto-generated certificate PDFs (vault-uploaded documents only), cancellation/pause/grace or dunning policy (a P6/FIN-DEC item), or any behavior after `G-LEGAL-01` opens beyond what `PreNeedPaidFlowTest` and the resource's gate-open tests exercise.

### MKT-04, MKT-05 — corrected 20 Aug 2026 (v0.17, traceability audit)

MKT-04 was carried as `Specified`, evidence `—`, since 08 Aug 2026 — accurate then, stale by the time cart, checkout, order-placement, and vendor order processing actually shipped. Verified 20 Aug 2026 by reading each file below against the row's claim, not its filename.

- **Cart.** `tests/Feature/Livewire/Public/Marketplace/CartScreenTest.php` — `test_an_empty_cart_shows_the_empty_state_not_a_broken_table`; the single-vendor conflict path (`test_adding_a_second_vendor_opens_the_conflict_modal_and_keeps_both_options`, `test_the_conflict_never_loses_the_existing_item`, `test_replacing_is_explicit_and_only_then_swaps_the_vendor` — AC4's constraint enforced, not just disclosed); `test_a_changed_price_demands_reconfirmation_before_checkout` (stale-price honesty); `test_update_quantity_cannot_touch_an_item_from_another_cart` and `test_remove_item_cannot_delete_an_item_from_another_cart` (cross-session isolation, the discriminating tests — a second session's cart is created and asserted untouched, not merely that the owner's cart works). `tests/Feature/Domain/Marketplace/CartTest.php` covers the same invariants at the domain layer — `test_adding_a_first_item_locks_the_cart_to_that_vendor`, `test_a_second_vendor_returns_a_conflict_and_changes_nothing`, `test_a_price_change_after_adding_is_detected`, `test_an_emptied_cart_releases_its_vendor_lock`.
- **Checkout / order-placement.** `tests/Feature/Livewire/Public/Marketplace/CheckoutScreenTest.php` — `test_placing_an_order_creates_it_unpaid_and_awaiting_the_vendor` (order created `BELUM_DIBAYAR`, one `MarketplaceOrder` with a `vendorOrders` row carrying the real recipient email — not a stub write); `test_a_double_submit_creates_only_one_order` (idempotency at the screen); `test_a_missing_service_area_shows_an_inline_error_and_keeps_entered_data` (§6.3 — never clears what the customer typed); `test_an_unconfigured_badan_usaha_degrades_without_leaking_internals` (fails closed, no config value in the response); `test_submitting_manual_proof_creates_a_payment_verification_for_the_order` (the manual-transfer path — proof submission — is the live payment path today). `tests/Feature/Domain/Marketplace/PlaceMarketplaceOrderTest.php` — `test_placing_an_order_totals_correctly_and_starts_unpaid`, `test_the_order_allocates_one_vendor_order_per_listing_line_awaiting_the_vendor`, `test_the_payable_references_the_correct_badan_usaha_and_vendor_and_starts_held`, `test_resubmitting_the_same_idempotency_key_returns_the_same_order`, `test_a_stale_price_blocks_checkout_rather_than_silently_recharging`.
- **Vendor order processing.** `tests/Feature/Filament/Vendor/VendorOrderStatusTransitionActionsTest.php::test_the_action_transitions_status_audits_and_notifies` (data-provided over all six one-click transitions — accept/reject/process/send-or-schedule/complete/complain — each asserted against the real `UpdateVendorOrderStatus` Domain Action's side effects: the status column and an `audit_events` row with `previous_state`/`new_state`, not a bare Filament `$record->update()`) and `::test_each_action_is_hidden_once_its_target_status_is_already_set`. This is the UI-level proof, not just the domain Action's own contract (`UpdateVendorOrderStatusTest` proves that separately).
- **Order tracking.** `tests/Feature/Livewire/Public/Marketplace/OrderTrackingScreenTest.php` — `test_the_customer_sees_the_current_vendor_processing_status`; `test_a_paid_order_is_never_shown_as_fulfilment_complete` (AC12 — payment state and vendor-processing state are two separate indicators, "Selesai" never shown just because payment cleared); `test_every_vendor_status_resolves_through_status_intent` (loops every `VendorProcessingStatus::KNOWN_STATUSES`); `test_another_customers_order_is_indistinguishable_from_one_that_never_existed` (no enumeration — a wrong `customerRef` and a nonexistent order number produce the identical "Pesanan tidak ditemukan" response).
- **MKT-05 (Online payment, marketplace) stays `Specified (gated fallback)`.** `App\Platform\Payment\Actions\OpenPaymentSession::__invoke` still throws `PaymentSessionOrderTypeNotSupportedException` for any command whose `orderType !== OrderType::Booking` (verified by reading the action directly, 20 Aug 2026) — `OrderType::Marketplace`'s own doc block still calls it "a closed case but has no production path yet." `tests/Feature/Livewire/Public/Marketplace/CheckoutOnlinePaymentTest.php` proves the checkout surfaces this honestly rather than removing the option or faking success: `test_the_online_option_renders_when_the_gate_is_open` (the card is shown, not hidden, once `G-PAY-01` is open); `test_online_submit_surfaces_the_marketplace_deferral_honestly` (clicking pay-online sets an honest `onlinePaymentUnavailable` state and the copy "belum tersedia untuk pesanan marketplace" — and asserts **zero** `PaymentSession` rows and `Http::assertNothingSent()`, i.e. no fabricated session, no provider call); `test_the_online_option_is_absent_when_the_gate_is_closed` (closed gate hides the option entirely, `CheckoutScreenTest::test_the_online_payment_option_is_shown_as_gate_closed_not_hidden` covers the gate-closed disclosure-with-alternative state — "transfer manual" — the manual-proof path `CheckoutScreenTest::test_submitting_manual_proof_creates_a_payment_verification_for_the_order` already proves works end to end).

**Scope of the claim.** MKT-04 claims the public cart/checkout/order-placement journey, vendor order processing, and customer order tracking, all screen- and domain-level tested. It does **not** claim marketplace online payment, which is MKT-05 and stays gated — the live payment path for marketplace orders today is manual bank transfer with proof submission and vendor-side manual verification, exactly as BOOK-08 and REN-05 already record for booking and renewal orders under the same closed `FIN-DEC-01`/`G-PAY-01` gate.

### CARE-SUB-01 raised, CARE-SUB-02 prose corrected — 20 Aug 2026 (v0.17, traceability audit)

**CARE-SUB-01.** Was carried `Specified`, evidence `—`. Verified 20 Aug 2026: `App\Domain\CareSubscription\Actions\CreateSubscription` is real and is called with an explicit `admin` actor role. `tests/Feature/Domain/CareSubscription/CreateSubscriptionTest.php::test_create_subscription_creates_draft_with_first_cycle` proves the row's claim for creation — a `DRAFT` subscription is created carrying the care plan reference, the plan's price copied onto the subscription, one `SCHEDULED` first cycle generated alongside it, and a `SUBSCRIPTION_CREATED` audit row with `actor_role => 'admin'`. `::test_create_subscription_generates_correct_cycle_dates_for_quarterly` and `::test_create_subscription_generates_correct_cycle_dates_for_annual` prove the frequency-driven cycle-date math for the other two frequencies (monthly is the first test's fixture default). The row is raised to `Covered` on this evidence.

**Scope of the claim — narrower than the row's full expectation text.** The expectation cell also names "status transitions" (plural). Only the creation transition (into `DRAFT`) is tested here; `App\Filament\Admin\Resources\Subscriptions\Actions\PauseSubscriptionAction` and `CancelSubscriptionAction` (and their domain Actions `PauseSubscription`/`CancelSubscription`) exist in the codebase — verified 20 Aug 2026 by reading `app/Filament/Admin/Resources/Subscriptions/Actions/` and `app/Domain/CareSubscription/Actions/` — but no test file in the repository references either action (`grep -rl "CancelSubscription\|PauseSubscription" tests/` returns nothing). Those transitions are real, wired code with zero test evidence, not claimed by this row's `Covered` status. Separately: `App\Filament\Admin\Resources\Subscriptions\Actions\CreateSubscriptionAction` (the header-action form that calls `CreateSubscription`) is also real and wired — verified by reading the file, which resolves grave and care plan, invokes the domain Action with the resolved actor, and redirects to the created record — but no Filament-level test exercises that header action the way `VendorOrderStatusTransitionActionsTest` exercises the vendor panel's transition actions. This row's `Covered` status rests on the domain Action's own test, not on any test of the admin UI wiring around it.

**CARE-SUB-02 — factual correction, status unchanged.** The row previously read "...generates cycles with idempotent dedup via unique constraint, emits `care.cycle_created.v1`," attributing the outbox event to cycle *generation*. Verified false by reading the two actions directly: `App\Domain\CareSubscription\Actions\GenerateCycle` writes the cycle and its invoice and records a `CYCLE_GENERATED` **audit** event — it calls `Outbox::record()` nowhere. The `care.cycle_created.v1` **outbox** event is emitted by `App\Domain\CareSubscription\Actions\MarkCyclePaid`, on cycle *payment*, not generation — proved by `tests/Feature/Domain/CareSubscription/MarkCyclePaidTest.php::test_mark_paid_emits_outbox_event`, which asserts the `outbox_events` row only after `MarkCyclePaid` runs. The clause is removed from CARE-SUB-02's expectation text above; the event's true timing (payment) sits in CARE-SUB-03's territory (payment webhook validation), not CARE-SUB-02's. CARE-SUB-02 stays `Specified` — this correction touches only the prose, not the status, per the review brief that requested it.

### CARE-SUB-03/04/05/07 raised, ADM-070 confirmed open — 24 Aug 2026 (v0.19)

Each of the five rows the v0.18 note above left open (CARE-SUB-03, CARE-SUB-04, CARE-SUB-05, CARE-SUB-07, ADM-070; all carried evidence `—`) was searched for real, already-existing test evidence that simply was never cited, not written fresh — per this file's own "read against the claim, not the filename" rule. `PR #130` (merge commit `1a93938`, CI run [`32506685010`](https://github.com/andrianm28/makam-app/actions/runs/32506685010), success) is the real, already-merged commit behind the four raised rows' evidence — all four newly-cited test files were introduced or last touched there; nothing new was written for this pass.

- **CARE-SUB-03 (payment webhook validation), raised.** `tests/Feature/Payment/CareSubscriptionWebhookSettlementTest.php` proves the care-subscription leg of `Actions\ApplyPaymentSettlement`'s webhook resolution end to end against a real, subscription-cycle-scoped invoice reference — a correctly-signed `payment.completed` delivery pays the cycle, advances the subscription, and auto-creates its work order; a wrong-amount delivery is refused by `MarkCyclePaid`'s amount assert and changes nothing; an unresolvable invoice reference fails closed. The delivery itself runs through the same merchant-scoped (`/api/payments/webhook/{merchant}`), signed (real HMAC verification via the `svix-signature` header), durable (a `ProviderEvent` row persisted before validation), and replay-protected receiver pipeline `tests/Feature/Payment/WebhookReceiverTest.php` already proves generically (forged-signature, stale-delivery-replay, and unserved-merchant rejection) — this row's own evidence is the subscription-cycle-specific proof that pipeline is genuinely wired to `SubscriptionCycle`/`Subscription`, not a claim that this file re-proves the generic properties. Never browser-return: the endpoint is the JSON webhook API, not `payments.return`.
- **CARE-SUB-04 (work order creation from paid cycle), raised.** `tests/Feature/Domain/VendorFulfillment/CreateWorkOrderTest.php` (note the real location — `CreateWorkOrderFromCycle` lives under `App\Domain\VendorFulfillment\Actions`, not `App\Domain\CareSubscription`), whose five tests prove checklist-template expansion into `work_order_tasks` (including the empty-template and multi-item cases), one-work-order-per-cycle idempotency, and the `care.work_order_created.v1` outbox emission (asserted directly against `outbox_events`, not inferred).
- **CARE-SUB-05 (evidence upload, vault-backed), raised.** `tests/Feature/Filament/Vendor/WorkOrderEvidenceUploadTest.php::test_a_vendor_uploads_evidence_through_the_resource_with_a_real_vault_upload` drives the vendor panel's real `unggahBukti` action with a real uploaded file through the actual quarantine → scan → promote pipeline (run synchronously, per the test's own doc block, since this host has no always-on media worker) and asserts the resulting `Document` is `DocumentState::Accepted` with `DocumentKind::VendorEvidence` before `WorkEvidence` is written; the domain-level `tests/Feature/Domain/VendorFulfillment/EvidenceUploadTest.php::test_upload_rejects_non_accepted_document` proves the companion refusal (evidence cannot be attached from a non-accepted document). The row's "never previewable before scan acceptance" half is not re-proved per-kind here — it is the document-vault's own generic, kind-agnostic guarantee, already covered by `tests/Feature/DocumentVault/DocumentStateViewTest.php::test_unaccepted_states_only_render_safe_file_metadata_and_never_preview_markup` (no `<img>`/preview/thumbnail markup for any unaccepted state) and `tests/Feature/DocumentVault/DownloadDocumentTest.php`'s quarantined-object denial tests, which this evidence flow inherits by using the same `Document` model and state machine as every other vault consumer.
- **CARE-SUB-07 (vendor replacement audit), raised and narrowed.** Real evidence exists for vendor replacement with an audited, mandatory reason: `tests/Feature/Domain/VendorFulfillment/ReplaceVendorTest.php` (the domain Action — old/new vendor IDs recorded in `VENDOR_REPLACED` audit metadata) and `tests/Feature/Filament/Admin/WorkOrderVendorReplacementTest.php` (the admin `WorkOrdersResource`'s `gantiVendor` header action — role-gated access matrix, reason required, a real write through the resource). But the row's own prior text claimed two things the search found no evidence for and the code does not build: a **reschedule** capability (grepped across `app/Domain/VendorFulfillment` and `tests/` — no reschedule action, model, or test exists anywhere; `grave-care-fulfillment` AC7's text covers reschedule too, but only the replace half shipped), and a **"one replacement per original" constraint** (`App\Domain\VendorFulfillment\Actions\ReplaceVendor` has no such guard — it is called and re-called freely with no count limit, and no test asserts one). Both clauses are removed from the row's expectation text above rather than left to imply coverage that was never built; the row is scoped to what is real: vendor replacement, reason mandatory and audited.
- **ADM-070 stays `Specified`, evidence stays `—` — this is a genuine, confirmed gap, not an uncited one.** The task brief's own lead was investigated directly: `tests/Feature/Payment/VerifyManualPaymentTest.php` and `tests/Feature/Payment/VerifyManualPaymentRouteTest.php` are real, thoroughly tested (role authorization, mandatory audit reason, decide-once concurrency guard, re-authentication gating, rate limiting) — but they do **not** exercise the ADMIN-PANEL surface this row names. The route they cover (`admin.payments.manual-verifications.verify`, `routes/web.php`) carries its own doc comment stating exactly this: *"a plain controller route (not a Filament panel page) in the standard `web` group"* — confirmed by grepping `app/Filament` for `PaymentVerification`, which returns nothing. There is also no GET route or Filament resource anywhere to **view** payment/transaction references at all — only this POST-only decide endpoint — so `admin-operations` requirement 5's first half ("allow an admin to view payment/transaction references") has no surface whatsoever, built or tested. (Separately, `app/Domain/OrderWorkflow/Actions/ManualPaymentVerification.php`, wired into `BookingOrderResource`'s header actions and already claimed under ADMIN-01, is a different action entirely — it transitions a booking order to `MENUNGGU_VERIFIKASI_PEMBAYARAN`, not a decision against a `PaymentVerification` row, so it does not close this gap either.) Citing either test file here would overclaim a Filament admin surface that provably does not exist; the row is left exactly as v0.16 found it, with the specific reason now on record instead of unexplored.

**Scope of the claim.** This correction is scoped narrowly: it does **not** touch, raise, or otherwise affect ADM-090, ADM-100, or any row not named above.

### ADM-070 raised — 25 Aug 2026

The gap the entry immediately above confirmed — no GET/view surface anywhere for payment/transaction references, only the existing POST-only decide endpoint — is closed. `App\Filament\Admin\Resources\PaymentVerifications\PaymentVerificationsResource` (branch `feat/adm-070-payment-verifications-admin-view`, commit `01e2155`, not yet merged/no PR opened yet) is a real, read-only Filament admin resource: `getPages()` registers only `index`/`view`, never `create`/`edit`. It is gated by `App\Platform\Payment\Contracts\PaymentActionAuthorizer` (`FinanceOrRestrictedAdminPaymentAuthorizer`) — the same contract the existing manual-verification write path (`VerifyManualPaymentController`/`VerifyManualPayment`) already uses, reused directly rather than a new read-scope authorizer, since `payment_verifications` has no scopeable column. `canAccess()`, `getAuthorizationResponse()`, and `getEloquentQuery()` all fail closed on refusal (`getEloquentQuery()` `abort(403)`s rather than silently falling through to an unfiltered query). The write/decide path itself is untouched by this resource — it stays exactly where it was, a plain controller route, not a Filament panel page — this resource only adds visibility.

Evidence: `tests/Feature/Filament/Admin/PaymentVerifications/PaymentVerificationsResourceAccessTest.php` (10 test methods, 14 test executions once its 5-role data provider is expanded — verified directly via `grep -c "public function test_"` against the file, not taken from Task 1's own summary count, which undercounted this) proves the access boundary directly — guest and no-role actors refused, five non-admitted roles refused via a data provider that deliberately includes a plain `admin` (explicitly ruled OUT by the authorizer's own doc block, distinct from other RBAC rows that would admit `admin`), `finance`/`restricted_admin` admitted, the same admitted/refused split confirmed again at the HTTP layer (200 for finance/restricted_admin, real 403 for a plain admin), `getEloquentQuery()` proven to fail closed directly rather than only through `canAccess()`, and `getPages()` asserted to register no `create`/`edit` key. `tests/Feature/Filament/Admin/PaymentVerifications/PaymentVerificationsTableTest.php` (5 tests) proves the read surface itself — real seeded rows render their real field values including all three `PaymentVerificationStatus` values (`SUBMITTED`/`VERIFIED`/`REJECTED`), the list defaults to newest-`submitted_at`-first, the list offers no create action, the view offers no edit/delete action, and the view's infolist renders the full real detail (submission fields, decision fields, decided-by/decided-reason). Both files ran green against real Postgres 18 in Task 1's implementation (19/19 tests, 44 assertions, per Task 1's own report), plus a full existing-suite regression check (405 tests, 2059 assertions) confirming nothing broke — that run was local (Docker Postgres, pinned image), not yet an actual GitHub Actions CI run, since this branch has no PR open yet and CI confirmation of it specifically is still pending, matching this file's own established convention for locally-verified-but-not-yet-CI-confirmed evidence (see multiple entries above using the same caveat).

Row raised to `Covered` on this evidence.

**Scope of the claim.** This closes ADM-070 only. It does **not** touch, raise, or otherwise affect CARE-SUB-02 or CARE-SUB-06, whose partial-evidence gaps (see the v0.17 CARE-SUB-02 correction above and `docs/testing/release-gates.md` §A) are entirely untouched by this work and remain exactly as they were.

### Rows deliberately left `Specified` despite nearby test files

- **BOOK-01 (Lima kota awal).** *Resolved 09 Aug 2026 — see section D's booking-wizard entry; the row now names PUB-010's own test and the reasoning below is superseded, kept for the record.* Updated 08 Aug 2026. Stronger evidence now exists than the July note described — `CemeteryDirectoryIndexRouteTest::test_all_five_launch_cities_are_offered_as_filters` and `::test_a_launch_city_keeps_its_filter_even_with_no_published_cemeteries` assert all five on PUB-011, and `RenewalStartTest::test_all_five_launch_cities_are_offered_in_the_canonical_order` asserts all five in order on PUB-030. **The row still stays `Specified` on purpose:** BOOK-01 is a *booking-workflow* expectation keyed to PUB-010, the wizard's own city step, which does not exist (S4-T4 paused). Re-keying its Screen cell to PUB-011/PUB-030 would quietly change what the row means from "the booking wizard offers five cities" to "some screen does". Whoever owns this matrix may reasonably decide to split BOOK-01 or re-key it; that is a matrix-authoring decision, not one this sync should make unilaterally. Recorded rather than silently left stale — the understatement is deliberate and explained.
- **BOOK-03…BOOK-09.** *Superseded 09 Aug 2026 for BOOK-03/04/05 — the wizard's Steps 1–5 are built and those rows now name real tests; see the booking-wizard entry above. Superseded 14 Aug 2026 for BOOK-06…BOOK-09 as well — Steps 6–9 shipped with L6/L7 (PRs #27/#28) and those rows are raised to `Covered` in v0.9.* As written 08 Aug 2026: the booking wizard is not built; `/pemesanan-makam` is still a "Segera Hadir" stub. No test asserts any of these rows.
- **MKT-04 (Checkout/payment/vendor processing).** *Superseded 20 Aug 2026 (v0.17) — cart, checkout, order-placement, and vendor order processing all shipped and are tested; the row is split and raised to `Covered` (MKT-04) with a new gated `Specified` row for online payment specifically (MKT-05); see the section D entry below.* As written earlier: Browse shipped, but no cart, checkout, payment, or vendor processing exists — and this is enforced rather than merely absent: `MarketplaceIndexRouteTest::test_the_landing_page_offers_no_cart_or_checkout_affordance` and `::test_the_component_exposes_no_livewire_actions_to_call`. `ProductDetailRouteTest::test_the_detail_page_states_the_single_vendor_per_checkout_constraint_as_a_note` states AC4's constraint as user-facing copy; **a note is disclosure, not enforcement**, and does not move this row. **Note:** `MarketplaceIndexRouteTest`'s claim above is about the *browse* page (PUB-020) specifically, which still offers no inline cart/checkout affordance — that remains true and is not in tension with MKT-04's new evidence, which is a separate `/marketplace/keranjang` and checkout screen (PUB-022/023).
- **REN-04, REN-05, REN-06 (fee, payment, confirmation).** Journey steps 4–6 have no screen (Sprint 13). `tests/Unit/Domain/Renewal/RenewalJourneyStepTest.php::test_only_the_first_three_steps_are_implemented_in_sprint_4` guards the `LAST_IMPLEMENTED` constant's declaration against one class of deliberate edit — it does **not** pin the boundary and is **not evidence the steps are absent**. **Correction, 09 Aug 2026 (`Renewal` retrofit):** that last clause is withdrawn. The test compares a constant to its own definition (`assertSame(GRAVE_SEARCH, LAST_IMPLEMENTED)`) and nothing in the Renewal production path reads `LAST_IMPLEMENTED`, so it protects nothing beyond the declaration. The rows stay `Specified` because steps 4–6 are genuinely screenless (Sprint 13, per `routes/web.php` and `screen-inventory.md` PUB-033/PUB-034), not because this test proves it. **Superseded 14 Aug 2026 (v0.9):** steps 4–6 shipped with L8 on 12 Aug 2026 and merged 13 Aug (PR #29); the rows now name their screens' own tests, with REN-04 and REN-06 `Covered` and REN-05 `Specified (gated fallback)` — see the section D entry below. `RenewalJourneyStepTest` was rewritten with the six-step contract (`test_the_journey_has_exactly_six_visible_steps`, `test_the_six_labels_match_ac1s_order`), so the three-step boundary test this paragraph dissected no longer exists.
- **ADMIN-01.** *Superseded 15 Aug 2026 (v0.10) — the order-management half of the `admin-operations` module set shipped with P1 (PRs #60–#64) and the row is raised to `Covered`; see the section D entry above.* As written 14 Aug 2026: real admin-side tests exist — `tests/Feature/Filament/Admin/Faq/**` (six files) covers the FAQ admin resource; `CemeteryResourceCrudTest.php` / `CemeteryResourceAccessTest.php` / `CemeteryResourcePackagesTest.php` (PR #41), `ProductResourceTest.php` (PR #40), and `ServiceDefinitionResourceTest.php` (PR #42) cover the master-data resources shipped 13 Aug 2026; `MasterDataNavigationTest.php` covers the admin navigation wiring; and `tests/Feature/IdentityAccess/AdminPanelHttpAccessTest.php` covers panel access control. All are scoped to individual resources and to authorization respectively, not to ADMIN-01's claim of the full `admin-operations` dashboard module set (bookings, orders, payments, reports, and the audit surface are still unbuilt), so the row stays `Specified`.
- **VENDOR-01.** *Superseded 14 Aug 2026 (v0.9) — the vendor panel shipped 13 Aug 2026 (PR #30, `lane/l10-vendor-portal`) and the row is raised to `Covered`; see the section D entry below.* As written 08 Aug 2026: no vendor panel and no vendor panel test exists; the only vendor-named code in the repository was `app/Domain/Marketplace/VendorProcessingStatus.php`.
