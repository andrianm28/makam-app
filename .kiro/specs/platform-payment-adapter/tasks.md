# Tasks — Platform Payment Adapter

`_Requirements: N_` references the numbered acceptance criteria in [`requirements.md`](requirements.md), added 25 Jul 2026 to match Kiro's documented task-traceability convention.

- [ ] Implement server-resolved `PaymentMode` and expose it to consumers. _Requirements: 1_
- [ ] Implement the six-condition payment guard as a single Action. _Requirements: 2_
- [ ] Implement hosted-checkout session creation with merchant/amount binding. _Requirements: 3, 13_
- [ ] Implement durable webhook receiver: persist-then-ack within 2 seconds. _Requirements: 5_
- [ ] Implement signature, merchant, amount, currency, and replay validation. _Requirements: 6_
- [ ] Implement idempotent webhook application keyed on provider event identity. _Requirements: 7_
- [ ] Implement manual fallback: reference, proof upload, pending state, admin verification. _Requirements: 8_
- [ ] Require recent re-authentication on manual verification and payout actions. _Requirements: 9_
- [ ] Write the balanced journal entry in the same transaction as the paid effect. _Requirements: 10_
- [ ] Implement refund/chargeback/reversal as explicit non-destructive operations. _Requirements: 12_
- [ ] Add guard tests: closed gate, expired quote, expired reservation, amount mismatch, unauthorized opening. _Requirements: 2_
- [ ] Add webhook tests: bad signature, wrong merchant, wrong amount, duplicate, replay, out-of-order, dead dispatcher. _Requirements: 6, 7_
- [x] Add tests proving no paid state can be reached from a browser return. _Requirements: 4_

## Design system

Payment UI lives in the consuming specs (booking Step 8, renewal Step 5, marketplace checkout), but the **state contract** is owned here. Per [`docs/design/design-system.md`](../../../docs/design/design-system.md) §3.7 and §6.9:

- `MENUNGGU_PEMBAYARAN` → `pending`; `MENUNGGU_VERIFIKASI_PEMBAYARAN` → `pending`, **never `success`**; `DIBAYAR` → `success`.
- `MANUAL_COORDINATION` renders an `<x-mk.alert intent=info>` banner (§6.9) that is **not dismissible** — it changes how the user must pay. Step 8 is never removed.
- Provider unavailable follows §6.5: fallback path or truthful pending, never a dead end.
- Duplicate submission follows §6.6: the same confirmation, never a second order.
- Never surface a provider name, stack trace, or correlation ID to a public user; return a support reference instead.
- Resolve all states through the shared `StatusIntent` helper; never `match` on an enum in a view.

## NOT TESTED

Nothing here is implemented. The provider is **not chosen** and gate `G-PAY-01` status is unknown, so `docs/planning/sprint-plan.md` OQ-5 blocks this spec. `payment-webhook.md` describes the contract shape but no sandbox has been exercised. The FIN-DEC approvals in `release-gates.md` §H are prerequisites and are not granted.

**Correction, 10 Aug 2026 (Wave 1b ruling 1b-L3-01):** the paragraph above is no longer accurate and is superseded on two points; the rest of it still holds. Appended rather than rewritten, per this repository's correction convention.

1. **"Nothing here is implemented" is false as of commit `afe45fc`.** What landed on lane `lane/l3-payment-adapter`:
   - `GuardPaymentSession` — the six-condition payment guard as a single Action, **deny-only**. Condition 1 (server-resolved `PaymentMode` via `ModeResolver::paymentMode()`, backed by `G-PAY-01`) is genuinely evaluated. Conditions 2-6 (confirmation/reservation, accepted quote, authorized opening, amount vs quote total, merchant + `badan_usaha` binding) each deny as `UnavailableUpstream`, naming the missing upstream, because no such record exists in this repository yet.
   - `payment_intents` and `payment_sessions` tables and models. Every guard evaluation writes a `payment_intents` decision record; denials also write audit `PAYMENT_GUARD_DENIED` with outcome `denied`.
   - **There is no reachable PASS outcome**, and therefore no caller can create a `payment_sessions` row. `PaymentIntentDecision` deliberately has no `Allowed` case and the Postgres CHECK admits only `'denied'`. This is a fail-closed guard, not a working payment path.
   - **Not** implemented, deliberately: `CreatePaymentSession`, the `PaymentProvider` contract, and all provider/HTTP code.
2. **The provider IS now chosen** — ADR-0033 selects the SumoPod sandbox for dev/staging. The `G-PAY-01`-status and FIN-DEC points still stand: the gate stays **closed for production**, the FIN-DEC approvals in `release-gates.md` §H remain ungranted, and no sandbox has been exercised yet.

Still genuinely NOT TESTED: the Postgres-only CHECK constraints (the local suite runs on SQLite, so they are unverified until CI), and every provider/webhook/manual-fallback/reversal surface, none of which exists yet.

**Correction, 10 Aug 2026 (Task 4, Wave 1b ruling 1b-L3-04):** the paragraph above remains incomplete for the reduced Task 4 slice and is superseded on the following points; appended rather than rewritten, per this repository's correction convention.

1. **The durable webhook receiver and validation surface now exists** as of commits `0f35c54` through `76a1c93`, and the provider is chosen per ADR-0033. The receiver's session-bound `VALIDATED` path remains unreachable while the deny-only payment guard prevents `payment_sessions` creation.
2. **The Task 4 claim slice is implemented and tested in sequence:** `VALIDATED -> PROCESSING` under the async claim action, the apply-time `(provider, provider_transaction_id)` settlement guard, and the browser-return negative proof. The browser-return proof is covered by the existing task below; it is not an untested future item.
3. **Still genuinely NOT TESTED:** PostgreSQL row-lock/concurrency behavior and PostgreSQL-only CHECK/index behavior; the session-dependent paid/apply path, Journal write, order `DIBAYAR` transition, outbox effects, failure/expiry handling, and live provider delivery. No payment session or test-only pass fixture was fabricated for these unavailable upstream contracts.

**Correction, 11 Aug 2026 (Task 5, Wave 1c Append-Correction):** the paragraph above remains incomplete for the reduced Task 5 slice and is superseded on the following points; appended rather than rewritten, per this repository's correction convention.

1. **A reachable, self-contained manual-fallback submission and verification slice now exists**, on a NEW `payment_verifications` table with no foreign key to `payment_sessions` or any order/booking table (deliberately decoupled from both blocked state machines — see the migration's own doc block). `SubmitManualPayment` creates a row at `SUBMITTED`, optionally referencing a proof document uploaded through the document vault's quarantine-first seam (`DocumentKind::PaymentProof`) — never the file content itself — and writes audit `PAYMENT_MANUAL_SUBMITTED` (not sensitive, no reason required). `VerifyManualPayment` is AC8's "separate authorized action": it transitions the row to `VERIFIED`/`REJECTED` with a mandatory reason (enforced by `Audit::record()`'s existing `SensitiveActions` check on `PAYMENT_MANUAL_VERIFICATION`, not re-implemented) and can only decide a row once.
2. **AC9's re-authentication gate is real for manual verification**, wired for real at `POST /admin/payments/manual-verifications/{paymentVerification}/verify` behind `App\Http\Middleware\RequireRecentAuthentication` — this middleware's SECOND real attachment anywhere in this repo (the first is `/admin/mfa/disable`), following that route's exact precedent. The item at line 12 above stays unchecked because it also names **payout** actions, which this task does not touch — only the manual-verification half is covered. **Superseded 12 Aug 2026:** the route is no longer gated by re-authentication alone — it now also requires `Payment\Contracts\PaymentActionAuthorizer` to approve a real `finance` or `restricted_admin` grant before anything else runs, per `docs/superpowers/plans/2026-08-12-payment-controller-auth-hotfix.md`.
3. **Deliberately absent, and explicitly NOT TESTED:** the `MENUNGGU_VERIFIKASI_PEMBAYARAN` order/session pending-state write named in the original plan text (no `payment_sessions` row can exist under ruling 1b-L3-01, and `app/Domain/OrderWorkflow/` is empty — no write target exists anywhere in this repo yet), any `payment_sessions` transition or same-transaction `Journal::post()` write (AC10, still blocked by the same deny-only guard as Tasks 2-4), any admin UI screen for reviewing submissions (no Filament resource exists), and the Step 8 `<x-mk.alert intent=info>` `MANUAL_COORDINATION` banner (reads from session/order state that does not exist yet). PostgreSQL-only CHECK constraint and foreign-key behavior remain unverified locally (the suite runs on SQLite) pending CI.

**Correction, 11 Aug 2026 (Task 6, Wave 1d Append-Correction):** the paragraph above remains incomplete for the reduced Task 6 slice and is superseded on the following points; appended rather than rewritten, per this repository's correction convention.

1. **A reachable, self-contained refund/chargeback recording slice now exists**, on a NEW `payment_reversals` table with no foreign key to `payment_sessions`, any order/booking table, `Journal`, or even `payment_verifications` (deliberately decoupled from every blocked state machine — see the migration's own doc block). `ReversalService` dispatches to `Actions\RecordRefund`/`Actions\RecordChargeback`, each of which records a `PaymentReversal` row exactly once per `(reversal_type, reference)` pair (an ordinary UNIQUE index, enforced on SQLite too) and writes a mandatory-reason audit event (`PAYMENT_REFUND`/`PAYMENT_CHARGEBACK`, newly added to `SensitiveActions::ACTIONS` by this task).
2. **The re-authentication gate is real for reversals too**, wired at `POST /admin/payments/reversals/{reversalType}` behind `App\Http\Middleware\RequireRecentAuthentication` — this middleware's THIRD real attachment anywhere in this repo (after `/admin/mfa/disable` and manual verification), following that route's exact precedent. This is the item at line 12's "payout actions" reference, restated: item 12 covers manual-verification (Task 5) re-authentication, not this line; the item at line 14 below stays unchecked because the "general Reversal" category and the journal-referencing behaviour it names are not built (see point 3). **Superseded 12 Aug 2026:** the route is no longer gated by re-authentication alone — it now also requires `Payment\Contracts\PaymentActionAuthorizer` to approve a real `finance` or `restricted_admin` grant before anything else runs, per `docs/superpowers/plans/2026-08-12-payment-controller-auth-hotfix.md`.
3. **Deliberately absent, and explicitly NOT TESTED:** any real journal reversal batch (`Journal::postReversal()` has zero call sites in this repo), any `PaymentProvider::refund()` call or `Contracts/PaymentProvider` interface (not authored — new-contract scope creep, out of this task's ownership), any customer-balance effect (no such concept exists anywhere in this repository), a third "general Reversal" `PaymentReversalType` case (the plan text names one but never names a concrete audit action for it — left as an open design question, not resolved unilaterally), and any admin UI screen for reviewing/initiating reversals. PostgreSQL-only CHECK/UNIQUE-constraint behavior for `payment_reversals` was independently verified against a real `postgres:18` container this session (see `task-6-report.md` §4), unlike the still-pending-CI items above.

**Correction, 11 Aug 2026 (Task 8, sandbox smoke verification):** the paragraph above remains accurate; this adds the disposition of the plan's own Task 8 rather than superseding anything.

Task 8's Step 1 ("create a payment session against the real SumoPod sandbox using `SUMODOP_SANDBOX_API_KEY`, confirm `payment_link_url` resolves, simulate the webhook flow with the sandbox's test event") is **explicitly NOT TESTED** and was not attempted in this agent session. Two independent reasons: (1) there is still no `PaymentProvider` outbound integration anywhere in this repository — only the webhook *receiver* half (`WebhookController`, `SumoPodWebhookSignature`, `WebhookValidator`) is real — so "create a payment session against the sandbox" has no code path to exercise regardless of credentials; (2) this step requires live outbound network calls to a real third-party sandbox using real credentials (`SUMODOP_SANDBOX_API_KEY`, `SUMODOP_SANDBOX_WEBHOOK_SECRET`) from a dev host, not an agent session — the agent's own permission system correctly refused even a presence-check read of `.env`, and the coordinator ruled this step requires a human or ops-provisioned dev-host run rather than agent execution. A fallback of smoke-testing only the webhook-receiver half against a self-signed fixture was considered and rejected: it would not honestly satisfy "simulate the webhook flow with the sandbox's test event" and risked being misread as partial sandbox coverage it does not actually provide. This is carried into the merge sign-off bundle as a named NOT TESTED item, same treatment as the Postgres-only-checks and live-provider-delivery items already listed above.
