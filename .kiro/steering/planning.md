---
inclusion: manual
---

# Makam.co.id — Planning Reference (on demand)

Part of the v0.6 steering set. Manual-only — reference `#planning` (or the matching slash command) when a task specifically needs the Kiro-spec-level analysis, not on every interaction.

1. `../../docs/planning/kiro-specs-analysis.md`

This is the deep cross-spec analysis (dependency gaps, foundation ownership, design-system alignment) behind the platform-foundation specs referenced in `tech.md`. `docs/planning/sprint-plan.md`, `docs/planning/agent-execution-plan.md`, and `docs/planning/parallelization-analysis.md` are related but not part of the original 20-document canonical steering set — read them directly when doing sprint-execution work rather than through steering.
